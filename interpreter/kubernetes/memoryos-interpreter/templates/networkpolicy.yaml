{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "memoryos-interpreter.fullname" . }}
  labels:
    {{- include "memoryos-interpreter.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "memoryos-interpreter.selectorLabels" . | nindent 6 }}
  policyTypes:
    {{- toYaml .Values.networkPolicy.policyTypes | nindent 4 }}
  {{- with .Values.networkPolicy.ingress }}
  ingress:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.networkPolicy.egress }}
  egress:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
---
# Deny-all-egress policy for the executor/session pods, which run untrusted user
# code. Always applied (independent of networkPolicy.enabled, which only governs
# the API-server policy above) and pinned to the executor namespace.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "memoryos-interpreter.fullname" . }}-executor
  namespace: {{ include "memoryos-interpreter.kubernetesNamespace" . }}
  labels:
    {{- include "memoryos-interpreter.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      app: memoryos-interpreter
    matchExpressions:
      - key: component
        operator: In
        values:
          - executor
          - session
  policyTypes:
    - Egress
  egress: []  # No egress rules => all outbound traffic is denied.