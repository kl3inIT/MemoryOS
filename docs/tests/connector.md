# Connector verification matrix

Integrated verification — 2026-09-09: the combined `290357a` + `fb835f9` tree passed the final `corepack pnpm check` after browser fixture corrections: 70 tests in 16 files, generated OpenAPI/client and route drift checks, lint, formatting, TypeScript, production build and font checks. `corepack pnpm test:e2e --workers=2 --retries=0` passed all 44 Chromium scenarios in 1.9 minutes without retries. Desktop/mobile light-theme selection-tree captures were visually inspected. The paging regression now waits for the actual final-page contents rather than accepting a transient disabled Next button. These are integrated browser fixtures, not live Google or customer-data acceptance. Backend/migration evidence is in the [Ingestion matrix](ingestion.md); current runtime progress and limitations are in the [integration ledger](../increments/active/google-drive-structured-ingestion/plan.md#isolated-main-integration--2026-09-09).

Actual integrated UI acceptance used the genuine owner session against the isolated API, not routed responses: 1440px/390px and light/dark Source views, settled folder children, two indexed files with current timestamps and separate Search status, the Admin Source association, and expanded Source run history. The real completed run displayed Checked 2, Indexed 2, Unchanged 0 and inline duration 9s. Document widths remained 1440px/390px; wide mobile tables stay within their own containers. Representative settled captures and desktop/mobile history were visually inspected; ignored evidence is under `.tmp/main-integration-smoke/settled-*` and `live-history-*`. The controlled fixture's missing outside-parent metadata produced a truthful discovery warning; this run does not establish live linked-approval acceptance. The routed suites retain explicit approval/deselect and pending-receipt coverage.

Historical boundary: the earlier dated evidence below predates this isolated integration; the Source detail consistency section records a subsequent follow-up. Original commands and migration numbers in historical records are retained, not additional integrated runtime results. Branch V13–V24 maps to integrated V18–V29 (+5), while main V1–V17 stays unchanged. Historical Owner-only Google authorization predates the global `SOURCES_MANAGE` cutover. Retained-contract matrix rows track the current regression names.

Evidence below separates retained regressions, measured capacity and isolated runtime acceptance. PostgreSQL lifecycle tests use controlled provider/storage collaborators; OAuth tests use a local signed-token fixture. Routed browser tests are not live Google acceptance. Final MEM-76 backend/frontend/browser, controlled corpus and isolated runtime evidence is recorded here, in the [Ingestion matrix](ingestion.md) and the [current ledger](../increments/active/google-drive-structured-ingestion/plan.md#mem-76-verification--2026-09-08). Earlier owner-app/three-link live Google runs remain historical, not proof of the new contract. No JetBrains MCP/LSP was available; no IDE-clean claim.

## Source detail consistency — 2026-09-09

- Files and both indexing histories share `TablePagination` with Users/Groups, using real `current / total` pages. Files and Source-run APIs now require nonnegative `totalItems`; counts exclude cursor/limit and remain Tenant/Source-scoped. Run counts also preserve projected-status, trigger and half-open time filters. OpenAPI and generated clients were regenerated.
- Focused `PostgresSourceLifecycleTest`, `PostgresSourceRunHistoryTest`, both affected `SourceApiIntegrationTest` page cases and `OpenApiContractTest` passed with core/API compilation and `:api:bootJar` in 1m38s. The required count is not derived from successfully indexed Documents.
- Repository verification: an initial `clean check :api:bootJar` reached the configured ten-minute `core:test` deadline while `OpenSearchRetrievalIntegrationTest` was starting its container. After the completed clean/build phases, `gradlew.bat check --no-daemon --no-parallel --max-workers=1` passed in 8m21s: the complete core suite executed; API/connector/worker tests were reused from cache. No test timeout was increased and no test was excluded. JetBrains/LSP inspection was unavailable; no IDE-clean claim.
- Rendered-browser smoke found the stale-first-page cache bug after external removal/retention. The extended Files recovery scenario and `refreshed FILE history reloads the first page after retained attempts shrink` both failed before the fix with `1 / 2` instead of `1 / 1`, then passed together after zero-stale-time cursor queries were applied. The FILE regression also verifies the remaining row and disabled Next control.
- Actual Chromium against the Vite Source surface with controlled read-only HTTP responses verified 51 Files (`1 / 3` at 25 rows, exact second-page rows, `1 / 11` at 5 rows, Previous restoration); 12 Drive runs (5/10/50-row totals, terminal navigation and reset after retention shrank the count); Credentials keyboard open/close; and Group draft/Cancel without a write.
- A separate actual FILE Source smoke verified 31 attempts produce 7/4/2/1 pages at sizes 5/10/25/50, exact Next/Previous rows, the disabled terminal-page Next control and fresh first-page rows/count after retention. It made no Google configuration/run reads and no writes.
- Visually inspected Drive light/dark 1440px and 390px captures plus a 320px light capture confirm the 40px top Drive mark, icon-backed headings, key-backed Credentials section and matching plain Group surface. Navigation buttons stay together and document widths equal viewport widths. FILE desktop/mobile captures confirm Upload content, Files and File indexing attempts icons and contained table scrolling. Both smokes reported zero browser page errors and zero API writes. Evidence remains ignored under `.tmp/drive-review-private/source-ui-*` and `file-ui-*`.
- Final `pnpm check` passed after the cache fix: generated API/route stability, CI image consistency, lint, formatting, TypeScript, all 70 unit tests in 16 files, production build and emitted-font checks. `pnpm test:e2e tests/e2e/source-action-feedback.spec.ts tests/e2e/google-drive-enterprise.spec.ts tests/e2e/file-source-setup.spec.ts tests/e2e/identity-shell.spec.ts --workers=1 --retries=0 --global-timeout=600000` passed all 39 selected browser tests in 4.2m, without retries.
- This is rendered Chromium plus focused real PostgreSQL/API contract proof, not live Google, customer-data or Orca embedded-browser acceptance. Orca snapshots timed out/returned `runtime_unavailable`; its dedicated temporary tab was closed without changing the user's original tab. This follow-up did not reindex, retry extraction or alter user Sources, credentials, selections, schedules or Google content.

## MEM-76 selection and run-history boundaries

| Contract | Retained evidence |
| --- | --- |
| Admission creates only a receipt/reserved ID; restart reuses ancestor checkpoints | `GoogleDriveSelectionOperationTest.admissionReservesOnlyAReceiptAndRecoveryReusesAncestorCheckpoints` |
| Same request identity conflicts on changed content; superseded proposals do not replace active selection | `GoogleDriveSelectionOperationTest.requestIdentityConflictsAndSupersessionNeverReplaceTheActiveSelection` |
| Paged search retains hidden roots/approvals; cursor pins authority; stale discovery/credential drafts cannot admit work | `GoogleDriveSelectionOperationTest.pagedSearchDoesNotDropHiddenRootsOrApprovalsAndCursorsPinAuthority`, `staleDiscoveryAndCredentialDraftsCannotAdmitReplacementWork` |
| Overlap/revoke retains recoverable failed receipts without a Source; stale claims/owner loss cannot activate a partial create | `GoogleDriveSelectionOperationTest.overlappingAndRevokedProposalsKeepTheSourceAbsentAndReceiptsRecoverable`, `staleClaimsAndOwnerLossCannotPublishAPartiallyVerifiedCreate` |
| Same-mode root edits preserve schedules and owner/CSRF/preconditions; discovery approval stays atomic with roots | Existing `SourceApiIntegrationTest` selection/discovery cases, updated for receipt admission and worker activation |
| Controller-owned public schema includes policy, receipt recovery, selection pages/drafts, dedicated runs/errors and separate item history | `OpenApiContractTest`; final generated-contract drift gate is recorded separately, not inferred from targeted tests |
| Source-scoped tree lists actual children and fences parent/credential/revision changes, paging, General scope and cycles without writes | `GoogleDriveSelectionTreeTest` (12 passing tests); API/OpenAPI tree endpoint coverage |
| No-change runs, legacy unknown values, unfinished indexing and accessible times remain distinct | Five `source-history-presentation.test.tsx` regressions |
| Acquisition completion cannot take ownership of earlier indexing or double-count replayed publication | `PostgresSourceRunHistoryTest.acquisitionCompletionDoesNotStealEarlierIndexingAndDuplicateDeliveryCannotDoubleCountPublication` |
| Errors/cancellation survive item cleanup; inactive-Tenant bulk cancellation and true supersession settle owned counters once; manual reindex cannot rewrite closed runs | `PostgresSourceRunHistoryTest.terminalItemErrorsAndBulkCancellationSurviveItemCleanupWithoutRewritingHistory`, `inactiveTenantBulkCancellationClosesOwnedChildrenOnlyOnce`, `trueSupersessionSettlesOnceAndManualReindexNeverReattributesClosedRun` |
| Expired claims report recovery pending; reconciliation deferral does not consume retry budget | `PostgresSourceRunHistoryTest.reconciliationDeferralDoesNotConsumeRetryBudgetAndExpiredClaimsAreRecoveryPending` |
| Failed storage acquisition records a safe storage stage without pruning prior Documents | `PostgresSourceRunHistoryTest.failedStorageAcquisitionRecordsSafeStageWithoutPruningEarlierDocuments` |
| Bounded stable history pages remain tenant/Source/filter scoped and legacy facts stay Unknown | `PostgresSourceRunHistoryTest.stableBoundedPagesRemainTenantSourceAndFilterScopedAndLegacyFactsStayUnknown` |
| Retention compacts terminal details but protects current inputs and unresolved errors | `PostgresSourceRunHistoryTest.retentionCompactsTerminalDetailsButRetainsCurrentInputsAndUnresolvedErrors` |

### Folder tree and truthful history — 2026-09-09

This revision supersedes the earlier flat-list, removed-run-UI and expanded-saved-links presentations recorded below; those entries remain historical evidence.

- `gradlew.bat clean check :api:bootJar :worker:bootJar --no-daemon --max-workers=2` passed in 8m55s with JDK 25.0.3 and a reachable `DOCLING_TEST_ENDPOINT`. The tree suite reports 12 tests, no failures/errors/skips. OpenAPI and generated clients were regenerated; API and worker were rebuilt and restarted with their preserved configuration and observed readiness. No JetBrains MCP/LSP was available, so semantic IDE inspection is not claimed.
- The authenticated live Google Source has three selected folder roots. Opening VETC returned its actual aggregate spreadsheet; expanding that file returned one TARGET document and eight persisted locations, H2–H9. This is actual provider metadata plus retained discovery evidence, not routed fixtures or a new discovery scan. Technical paths are absent.
- Initial tree acceptance verified complete three-root draft loading, target selection, Save enablement and Cancel focus without writes. The initial automatic opening of File and folder links was later removed by the independent approval follow-up below. The separate Edit entry remains hidden while that disclosure is closed, becomes available with keyboard expansion, focuses the editor and restores Edit focus after Cancel.
- During the initial tree/history acceptance, real Files timestamps distinguished first addition from current-version indexing. VETC was added at `2026-09-09T01:44:55.915946Z`, while its then-latest successful attempt queued at `04:04:20.078896Z`, started at `04:04:21.105889Z` and completed at `04:04:21.484384Z`. Last indexed showed that completion. The later timestamp simplification below removes the disclosure, not the current-version success semantics.
- The actual five-row Drive history showed No changes, Checked 3, Indexed 0 and Unchanged 3 with persisted start/completion times and measured durations. It does not substitute current document totals or fabricate New Docs/Total Docs. Repeated Scheduled/indexing captions and all-zero auxiliary details are removed; unknown or nonzero auxiliary values and errors remain available.
- Keyboard history expansion and help in both collapsed/expanded states passed; Escape restored help focus without changing history state. Dark 1440px and 390px tree/history screenshots were visually inspected. Mobile document width remained 390px; wide tables use their own horizontal containers. Final pagination and gate evidence follows in the active increment ledger.
- Final `pnpm check` passed 61 tests in 16 files, generated-client/route stability, lint, formatting, TypeScript, build and font checks. The three Drive/Source/identity browser suites passed 33 scenarios with two workers and no retries in 1.5m. The extended Files scenario verifies page-size reset, real next-page contents and the final-page boundary alongside concurrent operations/upload recovery. These routed saves are separate from live read-only acceptance.
- The final Files footer exposes Rows, current page and previous/next icons, with no old page-count/order subtitle. Live size changes 5 → 100 → 25 preserved the three files and disabled both navigation buttons on the only page. Desktop/mobile screenshots were inspected; the final CSS-only mobile width fix passed targeted formatter/lint and produced a 256px filename column with approximately 77px file rows rather than word-by-word wrapping. Both table surfaces scroll internally without widening the 390px document.
- Final live draft equality passed against the baseline: selection revision 12, discovery revision 9, three roots, zero approved links and one-minute interval. No live selection, schedule or Google-content mutation was performed. Screenshot evidence remains ignored under `.tmp/drive-review-private/`; the increment stays active.

### Plain timestamps and inline duration — 2026-09-09

The later requested simplification removes Last indexed details entirely and places run duration beside completion. `pnpm check` passed all 60 tests in 16 files, generated-client/route stability, lint, formatting, TypeScript, production build and font checks in 37.55s. The obsolete file-detail component and its regression were deleted; backend/API contracts were not changed.

Actual authenticated desktop (1440px) and mobile (390px) smoke found zero Files detail controls and four plain indexing timestamps. For the observed completed run, `12:37:02 PM` and `6s` had overlapping vertical bounds with duration to the right at both widths. Screenshots visually confirmed inline 5s/6s/31s values and no document overflow. Mobile horizontal table scrolling exposed the same completion cell without wrapping. Evidence: ignored `.tmp/drive-review-private/plain-timestamps-desktop.png` and `plain-timestamps-mobile.png`. No live write action was invoked.

### Independent linked-document approval — 2026-09-09

Select for sync no longer opens the root-links disclosure/editor. The extended existing tree browser scenario covers a closed disclosure, visible below-tree Save, shared target checkboxes, reload without entering root editing, explicit editor entry retaining approvals, both Cancel focus paths, and an approval-only save containing all original roots and retained approvals.

Actual authenticated SAVICO smoke showed the target selected, Save enabled outside the disclosure, zero textareas and the disclosure still closed. Explicit Edit then focused the textarea without losing the target approval or duplicating Save; Cancel restored Edit, and a separate approval-only Cancel restored the original target control. Desktop/mobile screenshots were inspected; the mobile document remained 390px. The saved draft before/after was exactly equal; no live Save, Discover or Sync was issued. Evidence remains ignored under `.tmp/drive-review-private/approval-only-desktop.png` and `approval-only-mobile.png`.

Final `pnpm check` passed 60 tests in 16 files plus generated-client/route stability, lint, formatting, TypeScript, production build and font checks in 32.56s. The three Drive/Source/identity browser suites passed all 33 scenarios with two workers in 1.5m, including the extended approval-only save/reload/editor-transition scenario. No backend contract changed.

### Reversible saved linked approval — 2026-09-09

The saved linked row previously hid its action after approval. It now shows Deselect for sync; opening that draft removes only the target approval. The existing tree browser scenario additionally covers cancellation preserving the saved approval, complete-payload removal preserving roots and another approval, and the resulting Select action after activation. A separate unavailable-target regression covers removal without permitting reapproval, Cancel focus when the checkbox is disabled, and empty-approved-list save without dropping roots.

Actual authenticated VETC smoke found the persisted Deselect action, an unchecked focused draft checkbox, enabled Save and a still-closed root disclosure with zero textareas. Cancel restored Deselect; the saved draft was exactly equal before/after. Desktop/mobile screenshots were inspected, with a 390px document at a 390px viewport. No live selection was saved or synchronization requested. Evidence remains ignored under `.tmp/drive-review-private/deselect-desktop.png` and `deselect-mobile.png`. Frontend `pnpm check` passed 60 tests in 16 files and all generated-client/route, lint, format, TypeScript, build and font gates in 30.89s.

Final browser verification passed all 34 Drive/Source/identity scenarios with two workers in 1.5m, including saved deselection, cancellation preserving approval, other-root/approval preservation and unavailable-target removal without reapproval. No backend/API change was required.

### Measured selection and history capacity — 2026-09-08

Ignored `.tmp/mem76-selection-capacity.json` records actual services/processors against isolated Testcontainers PostgreSQL and a deterministic `GoogleDriveProvider` **port** fixture. It is not HTTP/OAuth or live Google proof. Each CREATE used mixed folder/file roots and shared ancestors; each APPROVE retained those roots and added one approved file, advancing scope revision 1 → 2 under discovery revision 2/credential revision 1. All eight operations succeeded.

| Explicit roots (folders/files), ancestor depth | CREATE elapsed ms / metadata calls | APPROVE elapsed ms / metadata calls | Batches each | Sampled maximum checkpoint rows each | CREATE heap delta bytes | Whole database bytes CREATE / APPROVE |
| --- | --- | --- | --- | --- | --- | --- |
| 20 (10/10), 40 | 5,636 / 60 | 9,241 / 61 | 2 | 64 | 12,058,624 | 10,303,155 / 10,409,651 |
| 21 (11/10), 40 | 5,344 / 61 | 9,971 / 62 | 2 | 64 | 6,291,456 | 11,212,467 / 11,272,719 |
| 100 (50/50), 40 | 12,393 / 140 | 20,812 / 141 | 5 | 168 | 13,631,488 | 11,679,411 / 11,826,867 |
| 1,000 (500/500), 100 | 85,335 / 1,100 | 149,896 / 1,101 | 35 | 1,188 | -5,002,344 | 13,107,727 / 14,055,091 |

The 1,001-root proposal was rejected as `SOURCE_INVALID_REQUEST` in 26 ms with zero provider calls. The 101-root overlap scenario failed as `SOURCE_GOOGLE_ROOTS_OVERLAP` in 415 ms, one call/one batch, with zero checkpoint rows. The processor object was reconstructed after every batch, exercising durable continuation, not JVM/container restart. Provider calls asserted no active SQL transaction.

The successful probe reused one thread-bound physical JDBC connection per scenario with the real transaction manager; it does not benchmark production pooling or connection concurrency. An earlier unpooled attempt exhausted Windows ephemeral ports during 1,000-root CREATE (`BindException`); earlier class cleanup also interrupted launch. Neither establishes the cause of unrelated full-suite failures. Final exit was 0; total probe wall time was 333,239 ms. Scenario timings exclude fixture setup/database-size queries and may overlap the backend suite, so are not isolated performance benchmarks. “Calls” are logical metadata calls, not network requests; checkpoint maxima are sampled after batches, not continuous peaks. Database bytes cover the entire accumulating fixture database. Heap deltas are signed used-heap observations without forced GC, not peak/retained heap or allocation measurements; approval heap was not measured.

The separate `.tmp/mem76-history-capacity.json` fixture seeded 100,000 no-change terminal summaries with zero owned children in 9,052.5294 ms. It ran on JDK 25.0.3 with 12 available processors, 805,306,368-byte maximum heap (768 MiB) and G1 collectors.

| History page size | Warmups / samples | p50 ms | p95 ms | Maximum returned / next-page rows | Observed heap delta bytes |
| --- | --- | --- | --- | --- | --- |
| 25 | 15 / 50 | 51.928 | 62.7886 | 25 / 25 | 26,738,688 |
| 100 | 15 / 50 | 59.5469 | 102.4794 | 100 / 100 | 36,175,872 |

Both measured p95 values meet the 500 ms target only for this controlled PostgreSQL fixture. Heap includes temporary garbage. Neither probe establishes a Google latency/quota SLO, 100,000-file traversal/acquisition, child/error-heavy history capacity or production throughput. The separate [controlled 100k-file traversal](ingestion.md#controlled-100k-file-traversal) records the actual corpus experiment and its distinct no-change, HTTP, connection and durability boundaries.

### Compact Indexing attempts UI — 2026-09-09

- User-approved revision removes the Run history component, its queries and its dedicated browser scenario; backend run data, APIs and retention are unchanged. The historical 39-scenario result below describes the earlier UI.
- `pnpm check` passed generated-client/route stability, lint, formatting, TypeScript, all 55 unit tests, production build and font checks. The three retained `google-drive-enterprise.spec.ts` scenarios passed with one worker and no retries. The final header alignment also passed focused formatting/lint and the browser smoke below.
- A throwaway Playwright program exercised the actual Vite Source page with the existing routed owner/Drive fixtures and 31 indexing attempts. Default 5 rows, Next/Previous exact row restoration, changing size from page 2 to 10 resetting to Page 1, 25 rows, 50 showing the final 31 rows with Next disabled, and returning to 5 all passed. Requests carried the selected size and real fixture cursor; no run-history requests, writes or browser page errors occurred.
- Dark desktop (1440px) and mobile (390px) screenshots were visually inspected. The final five-row desktop table/footer occupies 334px below the heading; mobile document width stays 390px and the 640px table scrolls within its 358px container. Success, failure, processing, superseded and unfinished timestamps remain distinguishable; errors are not replaced with success counters. Screenshots remain ignored under `.tmp/drive-review-private/indexing-compact-{desktop,mobile}.png`.
- This is rendered Chromium UI proof with controlled HTTP responses, not a new live Google/backend ingestion verification. Orca embedded snapshot timed out, so verification used an independent browser. No user Source, selection, database or runtime configuration was changed; the throwaway program was removed.

### Summary interval and exact history totals — 2026-09-09

- The Source summary owns the interval presentation as its fifth field; no duplicate interval remains beside/below Synchronization. Dark 1440px and 390px screenshots were inspected, with no document overflow. A fixture-backed Chromium smoke verified Edit focus, Escape returning focus without a write or value change, invalid zero disabling Save, and a successful revision-guarded schedule save. Saved-value changes occurred only inside isolated HTTP fixtures, not the user's Source.
- The current/total smoke verified 31 attempts render 1/7 at size 5, 2/7 after Next, 1/4 when switching from page 2 to size 10, 1/2 at size 25, and 1/1 at size 50. Previous restores exact rows; Next disables on the final page. No run-history requests or browser errors occurred.
- `SourceApiIntegrationTest.itemPagesEnforceTheDefaultBoundAndFinishWithoutDuplicateRows` now exercises the authorized indexing endpoint against PostgreSQL: 26 retained attempts produce totalItems 26 on both first and later five-row pages, another empty Source reports zero, and a Member is denied. The focused test passed. `OpenApiContractTest` regenerated the required nonnegative int64 count; generated clients were refreshed.
- Full `pnpm check` passed again (55 unit tests, build, lint, format, type and generated-contract checks). Core/API compilation and worker compilation passed. JetBrains/LSP semantic inspection was unavailable; no IDE-clean claim is made. The API was rebuilt and restarted with its preserved local configuration, and readiness was observed. No user interval, selection or database content was manually changed.
- The existing Google Drive enterprise, FILE setup and Source action-feedback browser suites passed: 20 scenarios, one worker, no retries. Both throwaway browser programs were removed after verification.

### Unique selection and real linked discovery — 2026-09-09

- The authenticated review Source contained one target reached at eight distinct cells, H2–H9, in one source sheet. That target was also an explicit root. A real browser Discover linked documents action reached the configured Google provider and returned HTTP 200 in 19.818 seconds; discovery revision advanced from 6 to 7 with no discovery errors. This is bounded live-provider discovery evidence, not a full-corpus scan, new approval, acquisition/indexing run or production capacity claim.
- After rebuilding and restarting only the local API with its retained configuration, the real selection endpoint returned four unique rows: one folder and three explicit files. The directly selected target retained all eight origins; the linked-only filter returned zero additional rows. Selection revision 11 and the one-minute interval remained unchanged. No selection/approval save or Google-content write was performed.
- Actual authenticated Chromium surfaces were inspected at 1440px and 390px. The target appears once, with `8 references · 1 source document`; expansion renders one parent heading and eight locations, with technical paths collapsed. Search found the folder and matching target, FILE filtering returned three rows, LINKED showed its empty state, and clearing search restored all four rows. Edit loaded all four saved links and focused the textarea; Cancel restored Edit focus without a write.
- Selected content and Indexing attempts help sit eight CSS pixels after their titles, vertically centered. Pointer opening and Escape/focus restoration were exercised; history help leaves both collapsed and expanded history state unchanged. Native keyboard activation opens history, and Refresh renders the real five-row attempt table. Desktop/mobile selected-content widths and desktop document width stayed within the viewport; wide Files/history tables remain internally scrollable.
- The new `GoogleDriveSelectionOperationTest.directlySelectedTargetsAppearOnceWithAllOriginsAcrossCountsFiltersAndPages` passed against PostgreSQL, covering root precedence, retained origins, counts, search, kind filters, one-row cursor pages, approval preservation and tenant/Source isolation. The extended `PostgresGoogleDriveSyncTest.discoveryRequiresApprovalAndSelectedOrphansUseTheSameDeduplicatedSyncPipeline` also passed, retaining eight distinct origins while deduplicating an identical edge and the target. The initial two-class run exceeded its 300-second command deadline; only these two focused regressions are claimed as passing. Core production/test compilation, API/worker compilation and API packaging passed. JetBrains/LSP inspection was unavailable; no IDE-clean or new repository-wide `clean check` claim is made.
- `pnpm check` passed, including 55 unit tests, lint, formatting, types, generated-contract checks and production build. The existing Google Drive enterprise, FILE setup and Source action-feedback browser suites passed all 20 scenarios with one worker and zero retries on the final run. The first run had 19 passes and one blank-page initial-render timeout before its Source interactions; the same complete set passed without changing assertions or timeouts. Those routed fixtures are separate from the real discovery/UI evidence above. Orca embedded snapshot remained unavailable; an independent browser used the existing local authenticated session, with no authentication configuration changes.

### Expanded saved-links layout — 2026-09-09

- A user screenshot exposed the previously unverified expanded Copy saved root links state: it nested a second Selected content heading/help and scope description. The shared links component now renders creation-only heading/scope controls only with its scope picker; detail copy/edit states contain the labelled field without another section header. Read-only count text describes saved links rather than the editor quota.
- The real authenticated Source was inspected in dark mode at 1440px and 390px with saved links expanded and with Edit open. Both states expose exactly one section heading/help; all four saved links remain present. Copy mode is read-only; Edit focuses the editable textarea. Cancel removes editable fields and restores Edit selection focus. Mobile document/section widths remain within the viewport.
- The existing enterprise browser scenario now expands and loads saved links, verifies the full read-only value and unique heading, then checks the unique heading again in Edit before continuing its hidden-approval/search/paging/save coverage. All three enterprise scenarios passed with one worker and no retries; `pnpm check` passed all 55 unit tests, lint, formatting, types, generated-contract/route checks and build.
- This follow-up performed no discovery, selection save, backend change or service restart. The real Source remained at selection revision 11, discovery revision 7, one-minute interval and one folder/three file roots. Routed creation/save fixtures are separate from the real read-only UI checks.

### Frontend and browser acceptance — 2026-09-08

- `pnpm check` passed generated-client drift, lint, formatting, TypeScript, 55 tests in 15 files, generated-route stability, production build and font-asset checks.
- Final `pnpm test:e2e --workers=1 --retries=0 --global-timeout=600000` passed all **39** routed browser scenarios in 3.2 minutes, including enterprise selection/history and both new Files pagination regressions. This followed the complete `pnpm check` gate above; it is the post-pagination result, not the earlier 37-scenario run.
- Actual headless-browser desktop 1440px/mobile 390px light/dark surfaces were visually inspected against the isolated runtime, including selection, separate synchronization/item histories and the 100k-item Files fixture below. Document and main-content widths stayed within both viewports; mobile touch Next reached Page 2. Narrow tables scroll inside their own container. This is actual rendered-browser evidence, **not Orca embedded-browser proof**.
- The isolated owner login used Keycloak and real PostgreSQL/Redis/MinIO/worker, with controlled Google OAuth/Drive/Docs HTTP fixtures. Runtime run IDs, counters, storage recovery and real FILE upload/reindex evidence are canonical in the [Ingestion matrix](ingestion.md#mem-76-isolated-runtime--2026-09-08). No real user Source/provider content, shared runtime or DNS/hosts was changed.

### Real UI Files pagination over 100k items

The owner created FILE Source `53b321c3-9895-418c-94f5-cf6ef809ec65` through the UI and uploaded `mem76-files-page-proof.txt` as real item `64424a52-c465-489e-98ac-2b737abccb6d`. The fixture then added 99,999 metadata-only item/version/stored-object/current-Document rows with mappings, without synthetic bytes, artifacts or processing attempts. The real item was placed at position 41 to exercise page-two actions. This proves query/UI behavior over 100,000 item rows, not 100,000 acquired or indexed files.

The captured Source summary was 204 bytes with no `items`. The first and second item responses contained 25 rows each, 7,268 and 7,302 bytes, with zero overlap; the rendered table contained only 25 rows. Previous and forward navigation restored the exact respective row sets. Payload sizes describe these snapshots, not fixed response-size guarantees.

| Real UI action | Exact operation | Observed result |
| --- | --- | --- |
| Reindex the real file from Page 2 | `2ab25545-5657-4ea7-b4c0-01915f29d409` | `202`; observer kept polling while the worker was paused and the user paged away/back; after worker restart, SUCCEEDED and the row returned to INDEXED with its action enabled |
| Upload `mem76-files-after-page-two.txt` from Page 2 | `f9037dbe-b260-468c-8875-3ed185879cd0` | Real 174-byte upload/finalization; reset to Page 1 with item `29fb89d6-0641-45a8-9cd8-a306cae34380` first, then INDEXED; acceptance did not claim immediate indexing success |
| Remove the original real file from Page 2 | `2ebeb750-40ea-47c6-b98f-0433f2ff8728` | `202`; pending observation survived paging; after worker restart, SUCCEEDED, the row disappeared and the page refilled to 25 rows |

Final Source state was ACTIVE, `pendingWork=false`, 100,000 current fixture Documents (99,999 synthetic metadata rows plus the remaining real upload). Desktop light/dark and mobile light/dark inspection found no page-level horizontal overflow; a real mobile Next tap reached Page 2. Sanitized capture: `.tmp/mem76-final-ui.json`. The services and Google boundary are the isolated runtime described above.

## Ownership and FILE lifecycle

| Contract | Evidence |
| --- | --- |
| Global Source authority can list/read and execute the complete FILE lifecycle; ordinary active membership alone receives `403` on Source creation | `SourceApiIntegrationTest.indexesAndCleansUpOneFileThroughTheAuthorizedApi`, `rejectsMemberManagementAndBothValidationFailureShapes` |
| Scoped `SOURCES_READ`/`SOURCES_MANAGE` authority is resource-filtered: list, detail, Source Groups, Group Sources, and operation polling expose only Sources associated with the manager's own ordinary Groups; unrelated resources are `404` | `SourceApiIntegrationTest.enforcesScopedSourceHttpSurfacesAndImmediateAssociationRevocation`; `PostgresSourceLifecycleTest.scopedManagerReadsAndManagesOnlyAssociatedSources` |
| Scoped Source projections expose exactly `upload` and `reindex`; scoped managers cannot create Sources, replace associations, remove items, delete Sources, or query association options | `SourceApiIntegrationTest.enforcesScopedSourceHttpSurfacesAndImmediateAssociationRevocation`; `PostgresSourceLifecycleTest.scopedManagerReadsAndManagesOnlyAssociatedSources` |
| Source creation defaults an absent/empty Group list to Admin; non-default Group IDs must belong to the Tenant and fail atomically when invalid; association replacement rejects an empty set | `PostgresSourceLifecycleTest.sourceCreationAssociatesAdminByDefaultAndRejectsUnknownGroupsAtomically`; `SourceApiIntegrationTest.enforcesScopedSourceHttpSurfacesAndImmediateAssociationRevocation`; `V16__create_source_group_grants.sql` |
| Only global `SOURCES_MANAGE` can query Group options or replace Source–Group associations; replacement immediately removes scoped list/detail/operation access | `SourceApiIntegrationTest.enforcesScopedSourceHttpSurfacesAndImmediateAssociationRevocation`; `PostgresSourceLifecycleTest.scopedManagerReadsAndManagesOnlyAssociatedSources` |
| Upload finalize performs provider inspection outside the authority lock, then reauthorizes inside the commit transaction; association revocation after provider PUT/inspection prevents item/finalization adoption | `PostgresSourceLifecycleTest.associationRevocationDuringProviderVerificationPreventsUploadCommit`; combined runtime evidence in the [MEM-55 verification record](../increments/completed/mem-55-users-management/verification.md) |
| Reindex recovery derives Source error from the latest attempt per item: one recovered item no longer contributes its historical failure, while an unrecovered item's failure remains | `PostgresSourceLifecycleTest.recoveredAttemptsClearHistoricalErrorsWithoutHidingOtherItemFailures` |
| Concurrent FILE Source creation shares one `NO_AUTH` Credential | `PostgresSourceLifecycleTest.concurrentSourceCreationSharesOneNoAuthCredential` |
| Duplicate bytes converge on one item/version/live attempt | `PostgresSourceLifecycleTest.duplicateUploadConvergesOnOneItemVersionAndAttempt` |
| Source upload initiation binds generic object authorization to one Pair; finalization replay returns one receipt without adopting twice | `SourceApiIntegrationTest.indexesAndCleansUpOneFileThroughTheAuthorizedApi`, `PostgresSourceLifecycleTest.finalizeReplayReturnsThePersistedReceiptWithoutAdoptingTwice` |
| Duplicate discard and adopted removal release all live object, upload, version, and Source-upload references without violating restrictive foreign keys | `PostgresSourceLifecycleTest.duplicateDiscardAndAdoptedRemovalReleaseEveryObjectReference` |
| V16 makes every live Source association Tenant-qualified and seeds existing Sources to Admin; deleting an ordinary IAM Group removes only its association links, not the Source, Actor, or Document | `V16__create_source_group_grants.sql`; `GroupSchemaIntegrityTest.deletingAnOrdinaryGroupRemovesOnlyItsLinksAndGrants` |
| Existing document access mapping still invalidates immediately on item removal and is not replaced by Source management scope | `SourceApiIntegrationTest.indexesAndCleansUpOneFileThroughTheAuthorizedApi` |
| Browser initiation → direct object PUT → finalize, retry, remove/delete, and durable polling drive Source state without sending bytes through the API | `identity-shell.spec.ts` — `creates, indexes, removes, and deletes a FILE source` |
| FILE setup validates before writes, supports drop/browse/removal, suggests a name, submits once, retries create/upload/finalize failures without duplicating a known Source or repeating successful PUT, and fits mobile width | `file-source-setup.spec.ts` |
| Connector Source lifecycle, projections, locks, and IAM association joins remain concrete JDBC repositories owned by Connector; IAM exposes identifiers/authorization rather than JPA entities | `PostgresSourceLifecycleTest`; `CoreDependencyRulesTest.persistencePackagesAreVisibleOnlyToTheirOwningCapability`; Modulith verification |
| Files pages retain descending key order across uploads and count the authorized current-version corpus independently of cursor/limit, including pending/failed items without Documents and empty scopes; cursors reject other Tenant/Source/kind and malformed positions after authority checks | `PostgresSourceLifecycleTest.itemPagesUseStableDescendingKeysWhileNewUploadsArrive`; `itemCursorsRejectOtherSourcesTenantsKindsAndMalformedPositionsAfterAuthorityChecks` |
| HTTP defaults to a bounded Files page, complete traversal returns no duplicate rows and total metadata stays Source-scoped across pages | `SourceApiIntegrationTest.itemPagesEnforceTheDefaultBoundAndFinishWithoutDuplicateRows` |
| Paging preserves concurrent item operations; upload returns to the first page; a failed later page recovers and external removal refreshes first-page rows/count instead of restoring a fresh stale cache | `source-action-feedback.spec.ts` — `Files paging preserves concurrent item operations and uploads return to the newest page`; `a failed later Files page recovers and shrinking totals return to a valid page` |
| Retained FILE history shrinking below the current page refreshes the first page, its total and its terminal navigation state | `source-action-feedback.spec.ts` — `refreshed FILE history reloads the first page after retained attempts shrink` |
| Source-run totals preserve derived status, trigger, half-open dates, Tenant/Source isolation and retention independently of cursor/limit | `PostgresSourceRunHistoryTest.totalsUseDerivedStatusTriggerAndHalfOpenDatesWithoutCursorRestrictions`; `stableBoundedPagesRemainTenantSourceAndFilterScopedAndLegacyFactsStayUnknown`; `retentionCompactsTerminalDetailsButRetainsCurrentInputsAndUnresolvedErrors`; `SourceApiIntegrationTest.runPageTotalsRemainFilteredAndSourceScopedAcrossPages` |
| Duplicate discard and adopted removal release object/upload/version/source-upload references; pending removal cannot accept another upload | `PostgresSourceLifecycleTest.duplicateDiscardAndAdoptedRemovalReleaseEveryObjectReference`; `PostgresSourceLifecycleTest.uploadRejectsAnItemWhoseRemovalIsPending` |
| Reindex announces acceptance but waits for the requested operation, supports concurrent items, distinguishes success/failure/supersession and unknown status, and supports early keyboard dismissal; success/info/error notices expire even with hover, focus and window blur | `source-action-feedback.spec.ts` — reindex outcome and rejected/unavailable-status scenarios; expiry regression failed before the fixed-duration timer |

The [combined MEM-55/MEM-36 verification record](../increments/completed/mem-55-users-management/verification.md) adds passing final repository/frontend gates and isolated real PostgreSQL, Keycloak, MinIO, API, worker and browser proof for scoped reads, upload/reindex, association replacement, next-request revocation and latest-per-item recovery. JetBrains inspection was unavailable; no IDE-clean claim is made.

## Google authorization and connection authority

| Contract | Evidence |
| --- | --- |
| Callback uses PKCE/nonce, is consumed once, preserves the Actor and does not save tokens in the session | `GoogleDriveOAuthTest.completesPkceNonceBoundCallbackOnceWithoutReplacingActorOrSavingTokens` |
| Wrong state cannot consume a valid continuation; a different Actor cannot use it; initiation needs an existing browser session | `GoogleDriveOAuthTest.unrelatedStateDoesNotConsumeValidContinuationAndChangedActorCannotUseIt`; `GoogleDriveOAuthTest.requiresExistingActorSessionAndRejectsWeakRootRevisionPreconditions` |
| Signed identity for another nonce/client is rejected | `GoogleDriveOAuthTest.rejectsValidlySignedTokenWithWrongNonce`; `GoogleDriveOAuthTest.rejectsValidlySignedTokenForAnotherOAuthClient` |
| Uploaded Web JSON enforces the UTF-8 byte bound and exact callback, cannot select provider hosts, and token redirects cannot forward the client secret | `GoogleDriveOAuthTest.acceptsOnlyBoundedWebClientJsonWithExactConfiguredCallback`, `uploadedEndpointsCannotChooseTokenAuthorizationOrJwksHosts`, and `tokenEndpointRedirectDoesNotForwardClientSecret` |
| Audience validation follows each consent app rather than cached global app identity | `GoogleDriveOAuthTest.audienceValidationUsesEachConsentAppRatherThanCachedFirstApp` |
| Consent app data authenticates owner/Tenant/target/continuation; stored apps are encrypted, replacement is consent-pinned and cannot change the Google subject | `GoogleDriveCredentialAuthorityTest.consentSnapshotAuthenticatesOwnerTenantTargetAndConsentWithoutPlaintextSecrets`, `storedAppIsEncryptedAndReplacementIsPinnedToItsConsent`, and `replacingAppCannotChangePreviouslyAuthorizedGoogleSubject` |
| Legacy grants cannot reconnect implicitly; migration removes their authority while retaining Source/roots/content and FILE work | `GoogleDriveCredentialAuthorityTest.legacyCredentialWithoutAppCannotOpenOrReconnectImplicitly`; `GoogleDriveOAuthClientMigrationTest.legacyGrantsLoseAuthorityWhileSourceRootsDocumentsAndFileWorkSurvive` |
| Consent creates independent tenant/owner-bound credentials without Sources; incomplete Docs scope leaves no rows | `GoogleDriveCredentialAuthorityTest.completedAuthorizationCreatesIndependentOwnerTenantBoundCredentialsWithoutSources`; `rejectsMissingDocsScopeBeforeAnySourceOrCredentialRows` |
| Encrypted grant binds tenant, credential and key version and rejects tampering | `GoogleDriveCredentialCipherTest.encryptionBindsTenantCredentialAndKeyVersionAndRejectsTampering` |
| Refresh rotation persists a new token without changing publication authority; stale rotation/failure cannot defeat newer authority or a winning rotation | `GoogleDriveCredentialAuthorityTest.rotatedRefreshPreservesPublicationAuthorityAndPersistsTheNewToken`; `GoogleDriveCredentialAuthorityTest.obsoleteRefreshAndAuthenticationFailureCannotOverwriteReauthorization`; `GoogleDriveCredentialAuthorityTest.obsoleteRefreshFailureCannotInvalidateAWinningTokenRotation` |
| Local disconnect destroys usable authority and supports same-source reconnect; unavailable encryption key cannot prevent disconnect | `GoogleDriveCredentialAuthorityTest.disconnectDestroysLocalGrantAndCanReconnectWithoutDeletingSource`; `GoogleDriveCredentialAuthorityTest.lostEncryptionKeyCannotPreventLocalDisconnect` |
| Publication on one Source excludes shared reauthorization through another until commit | `GoogleDriveCredentialAuthorityTest.publicationOnOneSourceExcludesSharedReauthorizationThroughAnotherUntilCommit` |
| Unsafe authorization destinations and callback values are rejected in the browser | `google-drive-authorization.test.ts` — `rejects unsafe authorization destinations before attempting browser navigation`; `accepts only safe callback outcomes and drops unrelated callback values` |
| V18 preserves encrypted authority and existing Source/selection/item/Document data without reauthorization | `GoogleDriveOAuthClientMigrationTest.reusableCredentialMigrationPreservesEncryptedAuthorityAndAllSourceData` |
| One credential creates independent Sources and survives their cleanup; failed/concurrent validation leaves no partial Source | `GoogleDriveCredentialAuthorityTest.oneCredentialCreatesIndependentSourcesAndSurvivesTheirCleanup`; `failedOrConcurrentRootValidationCannotLeavePartialSourceRows` |
| Shared reconnect/revoke/authentication failure fences every attachment; inactive Tenant loses worker authority without blocking cleanup | `GoogleDriveCredentialAuthorityTest.sharedReauthorizationRevokeAndAuthenticationFailureFenceEveryAttachedSource`; `inactiveTenantLosesWorkerAuthorityWithoutPreventingSourceCleanup` |
| HTTP catalog/reuse/shared revoke and guarded deletion enforce owner, Tenant, CSRF and revision boundaries without secret leakage | `SourceApiIntegrationTest.oneCredentialCreatesIndependentSourcesAndSharedRevocationPreservesBoth`, `unattachedCredentialDeletionRequiresCsrfAndCurrentRevision`, `otherTenantOwnerCannotReadOrAttachKnownForeignCredentials`, and `credentialManagementDoesNotExposeAnOracleToNonOwners` |
| V19 preserves existing due timestamps and source data while enforcing positive stored schedule values | `GoogleDriveOAuthClientMigrationTest.scheduleMigrationPreservesDueTimestampsAndEnforcesPositiveStoredValues` |
| Schedule edits preserve shared credential authority, roots, Documents and live work; unavailable grants do not block local scheduling | `GoogleDriveCredentialAuthorityTest.scheduleEditsPreserveSharedCredentialScopeDocumentsAndLiveWork`; `scheduleCanChangeWithRevokedOrUnreadableCredentialsWithoutEnqueuingWork` |
| Schedule changes reject invalid values and inactive/deleting authority, including owner loss while waiting for the Source lock | `GoogleDriveCredentialAuthorityTest.scheduleRejectsInvalidIntervalsAndInactiveOrDeletingAuthority`; `scheduleRechecksOwnerAfterWaitingForTheSourceLock` |
| HTTP scheduling uses independent strong revisions, validates integer JSON boundaries and enforces authentication, owner and CSRF guards | `SourceApiIntegrationTest.scheduleWritesUseIndependentEtagsAndRemainAvailableWithoutAGrant`; `scheduleRequiresAStrongRevisionAndAPositiveIntegerJsonValue`; `scheduleRequiresAuthenticationOwnerAndSameOriginCsrf` |
| Interval persistence, status refresh, stale-save recovery and failures preserve interval/link drafts; invalid values and pending saves cannot submit | Scheduling cases in `google-drive-panel.test.tsx`; mobile save/reload and desktop keyboard cancellation in `google-drive-source-setup.spec.ts` |
| Selection and interval saves show visible transient notifications on mobile; interval save restores focus and both notices disappear without a reload | `google-drive-source-setup.spec.ts` — existing Google setup scenario; `google-selection-saved-mobile.png` and `google-interval-saved-mobile.png` |
| Source mutations distinguish accepted, completed, failed and unknown outcomes using the returned operation; removal/deletion close their confirmation dialogs after acceptance and permit navigation | `source-action-feedback.spec.ts`; FILE lifecycle coverage in `identity-shell.spec.ts` |
| Notifications expire after five seconds despite hover, focus or window blur; ordinary notices clear on route changes while named creation/deletion results survive the redirect without restarting their deadline | `source-action-feedback.spec.ts`; creation scenarios in `google-drive-source-setup.spec.ts` and `file-source-setup.spec.ts` |
| Partial creation/finalization success retains the known Source and upload state so retrying navigation or finalization does not create or upload again | Recovery scenarios in `google-drive-source-setup.spec.ts` and `file-source-setup.spec.ts` |

## Google source selection and synchronization

| Contract | Evidence |
| --- | --- |
| V20 leaves existing Sources SPECIFIC with identical roots, intervals, credentials, Items and Documents | `GoogleDriveOAuthClientMigrationTest.scopeMigrationDefaultsExistingSelectionsToSpecificWithoutChangingDataOrSchedules` |
| Discovery requires explicit approval; duplicate paths converge and retained orphan approvals use the same sync pipeline | `PostgresGoogleDriveSyncTest.discoveryRequiresApprovalAndSelectedOrphansUseTheSameDeduplicatedSyncPipeline` |
| Covered targets cannot exclude folder content; an approved file becoming a folder cannot grant traversal or prune an incomplete generation | `PostgresGoogleDriveSyncTest.coveredTargetsAreDeduplicatedAndRemovingApprovalDoesNotExcludeFolderContent`, `approvedTargetBecomingAFolderCannotGrantTraversalOrPruneOnAnIncompleteGeneration` |
| Discovery bounds preserve the previous snapshot; candidate failures and fresh metadata cannot authorize arbitrary target IDs | `PostgresGoogleDriveSyncTest.discoveryBoundsFailExplicitlyAndKeepThePreviousCompleteSnapshot`, `candidateFailuresAndFreshMetadataCannotBeTurnedIntoArbitraryApprovals` |
| Authority loss and older discovery cannot publish; fresh approval rechecks credential authority after metadata | `GoogleDriveCredentialAuthorityTest.discoveryRejectsAuthorityLostDuringAcquisitionWithoutPublishingCandidates`, `olderDiscoveryCannotOverwriteANewerDiscoveryAndDoesNotAdvanceScopeRevision`, `freshApprovalRechecksCredentialAfterMetadataAndRevocationPreservesOnlyExistingApprovals` |
| API discovery starts unselected, saves roots/approvals atomically and rejects missing preconditions, unauthorized/unknown targets and General discovery | `SourceApiIntegrationTest.linkedDiscoveryReturnsUnselectedCandidatesAndSavesApprovalAtomicallyWithRoots`, `linkedDiscoveryAndSelectionRejectMissingPreconditionsUnauthorizedAndUnknownTargets`, `generalCannotDiscoverOrApproveAndDiscoveryFailureDoesNotPretendToBeComplete` |
| Tree expansion is read-only; drafts survive rediscovery/polling/conflicts, shared choices stay synchronized, and orphan cycles terminate | `google-drive-panel.test.tsx` — target-draft and retained-orphan cases; `google-drive-source-setup.spec.ts` — `linked document trees expand at rest and save only explicit eligible approvals`, including desktop/mobile captures |
| General resolves the credential's actual My Drive root without exposing it as a user selection; invalid modes, mixed links and unverifiable roots cannot expand scope; authority is rechecked after provider validation | `GoogleDriveCredentialAuthorityTest.generalCreationUsesTheCredentialsVerifiedRootWithoutExposingASyntheticSelection`, `invalidScopeAndMixedLinksCannotCreateOrReplaceAnAcceptedSelection`, `generalRootValidationRejectsUnverifiedShapesWithoutChangingSelection`, and `generalCreationRechecksCredentialAuthorityAfterRootResolution` |
| Both mode-change directions fail before provider access without changing roots/revision, scheduling, credentials, live work or published content; Specific link replacement still requires current scope revision and preserves scheduling | `GoogleDriveCredentialAuthorityTest.scopeModeChangesAreRejectedBeforeProviderAccessAndPreserveSourceAuthority`; `SourceApiIntegrationTest.scopeModeIsCreationOnlyAndRejectedChangesPreserveConfigurationAndSchedule`, `specificLinksRemainEditableWithCurrentRevisionAndPreserveTheSchedule`; existing owner/Tenant, CSRF and malformed-mode API cases |
| General follows only the actual My Drive hierarchy through nested folders, pagination and resumed claims; an incomplete provider search is not a completed page | `PostgresGoogleDriveSyncTest.generalTraversesOnlyMyDriveThroughNestedPaginatedAndResumedFrontiers`; `RestGoogleDriveProviderTest.myDriveAliasResolvesToARealParentAndIncompleteSearchCannotCompleteItsTraversal` |
| General root loss or changed authority cannot prune; incomplete traversal retains unseen content; Specific link replacement fences old work and prunes only after complete new-selection reconciliation | `PostgresGoogleDriveSyncTest.resumedGeneralTraversalCannotPruneWhenCurrentMyDriveRootCannotBeVerified`, `generalRootLossDuringEnumerationDoesNotTurnIntoAnEmptyCompletedGeneration`, `incompleteGeneralEnumerationRetainsUnseenDocumentsUntilACompleteGeneration`, and `replacingSpecificLinksFencesOldWorkAndPrunesOnlyAfterTheNewSelectionCompletes` |
| Both modes remain available at creation; detail has no mode controls, General has no selection editor/save/reload, and synchronization and interval editing remain available | `google-drive-panel.test.tsx` — `keeps General scope read-only through refresh and interval saves while allowing synchronization`; `google-drive-source-setup.spec.ts` — `General mode is chosen at creation and stays read-only while sync and interval editing remain available` |
| Specific detail is read-only until Edit selection; Cancel restores saved links without a write, successful Save locks again, and both restore Edit focus. Failed saves and explicit reload remain editable, with authority/conflict protection. Creation remains directly editable without a saved-selection summary; detail lists saved roots only when present | `google-drive-panel.test.tsx` — `guards Specific links until Edit selection, discards canceled edits, and locks after saving`, `preserves rejected link edits until an explicit reload of the newer saved selection`, and bounded-link/authority cases; `google-drive-source-setup.spec.ts` covers keyboard entry, cancellation without a root write, save and return to read-only; actual visual evidence in the increment ledger |
| Specific link parsing rejects untrusted/ambiguous/account-wide forms; duplicate, overlapping and whole-drive links leave selection unchanged; specific Shared Drive folders remain supported | `GoogleDriveLinkTest`; `PostgresGoogleDriveSyncTest.rejectsOverlappingAndUnsupportedRootsWithoutChangingTheAcceptedScope`, `rejectsDuplicateLinksAndWholeDriveRootsWithoutChangingSelection`, and `acceptsAnExplicitSharedDriveFolderAndIndexesOnlyItsChildren` |
| HTTP root preconditions reject weak/non-numeric revisions | `GoogleDriveOAuthTest.requiresExistingActorSessionAndRejectsWeakRootRevisionPreconditions` |
| Durable traversal checkpoints pages, resumes without restarting and fences stale claims; unrelated files are never read | `PostgresGoogleDriveSyncTest.checkpointsPagesAndResumesWithoutRepublishingOrRestartingTheTraversal`; `synchronizesOnlyExplicitRootsWithoutReadingUnrelatedFiles` |
| Unconfigured Specific sources cannot schedule whole-account sync, and persisted legacy whole-drive roots in Specific fail before enumeration/acquisition | `PostgresGoogleDriveSyncTest.unconfiguredSourceCannotScheduleAccountWideSynchronization`; `legacyPhysicalDriveRootsFailClosedBeforeEnumerationOrAcquisition` |
| Failed acquisition does not prune unseen input; confirmed snapshots index and failed files remain retryable | `PostgresGoogleDriveSyncTest.failedAcquisitionDoesNotPruneUnseenItems`; `itemFailureReleasesConfirmedSnapshotsAndRetainsTheFailedFileForRetry` |
| Folder trash reconciles descendants; scope replacement and explicit removal cannot be resurrected by old work | `PostgresGoogleDriveSyncTest.trashingASelectedFolderReconcilesItsPreviouslyIndexedDescendants`; `PostgresGoogleDriveSyncTest.scopeReplacementAndExplicitRemovalCannotBeResurrectedByOldWork` |
| Reconciliation defers unchanged in-flight indexing without losing its extraction retry budget | `PostgresGoogleDriveSyncTest.reconciliationDefersInFlightIndexingUntilTheUnchangedInputIsConfirmed` |
| Extraction uses adopted bytes offline; revoked authority rolls back current-Document publication; indexed Google data does not grant FILE PUBLIC clearance | `PostgresGoogleDriveSyncTest.indexesAdoptedBytesOfflineAndRollsBackPublicationAfterCredentialRevisionChanges` |
| Browser never requests candidate listings and only synchronizes a saved bounded selection; undoing edits restores saved-state actions | `google-drive-panel.test.tsx` — `never browses candidates and synchronizes only a saved, bounded link selection`; `restores saved-state actions when a link edit is undone` |
| Status refresh does not replace edited links or a newer saved selection; scope/credential conflicts require explicit reload rather than automatic retry | `google-drive-panel.test.tsx` — older-response, edited-link refresh, stale-save and credential-transition cases |
| Drive refresh failures share one Synchronization warning, retain refresh recovery and disable mutations even when only the Source summary is stale | `google-drive-panel.test.tsx` — `keeps synchronization guarded until the Source summary is fresh again`; all 11 panel tests passed. Live Source DOM after hot reload contained one refresh warning, no Refresh source button, and a disabled Synchronize now action. |
| Synchronize now announces acceptance and terminal acquisition outcome without claiming indexing completion; errors do not emit success | `source-action-feedback.spec.ts` — `synchronization completion does not claim that indexing has finished`; `synchronization-completed-indexing-pending.png` |
| Rejected overlap is reported only in Selected content, describes the invalid links input, survives status refresh, clears on editing and permits a corrected save without exposing provider detail; disconnect preserves named roots and stored-app reconnect needs no replacement JSON | `google-drive-panel.test.tsx` — `keeps rejected overlap links editable without exposing provider detail`, `retains named roots after confirmed local disconnect`, and `reuses the saved OAuth app without requesting replacement credentials` |
| Legacy/replacement app input is cleared and not cached/echoed; late consent responses cannot cross authority transitions | `google-drive-panel.test.tsx` — `requires an explicit OAuth app for a legacy source and never caches or echoes the submitted secret`; `clears replacement credentials and refuses a late consent response after an authority transition` |
| One consent creates two named Sources with independent roots; separate selection column supports keyboard selection and disclosed guarded management; contextual help opens by keyboard, closes with Escape and restores trigger focus; secret clearing, shared lifecycle, load failure and owner gating remain enforced | `google-drive-source-setup.spec.ts` — four routed-browser scenarios; live reuse evidence is recorded in the [increment ledger](../increments/active/google-drive-structured-ingestion/plan.md#reusable-credential-verification) |
| Expanded Credentials retains a visible Close action and upward chevron; pointer opening and keyboard collapse remain usable | `google-drive-source-setup.spec.ts` — existing Google setup scenario and settled `google-credential-disclosure-mobile.png` capture |

Raw reservation/adoption/cleanup evidence is in [object storage](object-storage.md). Execution and extraction evidence is in [ingestion](ingestion.md); current Document/artifact evidence is in [document](document.md).
