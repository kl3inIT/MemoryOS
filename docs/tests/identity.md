# Identity verification matrix

## Account language (MEM-74 / MEM-22 foundation)

- `BearerAuthenticationIntegrationTest.accountLanguagePersistsAndNeverChangesAuthorizationRevisionOrAnotherActor`: real HTTP + isolated PostgreSQL, idempotent language saves, authoritative reads, unchanged capabilities/revision and separate Actor isolation.
- `languageWriteRequiresAuthenticationActiveMembershipGuardAndValidValue`: anonymous/inactive denial, same-origin guard and stable invalid/required field codes.
- `ActorLanguageServiceTest`: current membership lock precedes preference writes; no admin permission dependency; invalid values never reach persistence.
- `JpaActorProfileRecorderTest.recordsLatestTruthfulProfileWithExactBindingProvenance`: default Vietnamese, DB rejects unsupported values, subsequent IdP observations preserve the saved preference.
- `JpaActorProfileRecorderTest.refreshFragmentReloadsAnAlreadyManagedActorUnderWriteLock`: the real Spring Data proxy composes the refresh fragment, reloads stale managed state under `PESSIMISTIC_WRITE`, and commits the subsequent preference change.
- `general-settings-page.test.tsx`: member settings, confirmation-before-display, guarded request, lost-response reconciliation and late-response Actor isolation.
- `application-session-boundary.test.tsx`: locale change preserves the same input node/draft/private cache; transient background failure preserves mounted data; existing authorization-revocation cases remain.
- `catalog.test.ts` and `problem-presentation.test.ts`: key/placeholder parity, both languages, status/placement/recovery classification, safe validation metadata and no raw diagnostic rendering.
- `user-action-errors.test.ts`, `users-table.test.tsx`, `confirm-dialog.test.tsx`: feature code mapping, email validation versus unknown-field summary, existing error retranslation without replacing the row/dialog, and no implicit mutation retry.

These identity checks do not establish live model-language behavior. Full-app catalog/browser coverage is documented in [localization verification](localization.md) and the active increment's verification record.


| Requirement | Durable verification |
| --- | --- |
| Unified `io.memoryos.iam` lifecycle writes use JPA and one transaction; bootstrap and invitation acceptance roll back Actor, binding, Tenant membership, Group membership, and grant changes together | `DefaultInitialTenantBootstrapperTest.createsTheExactInitialIamAggregateAndReplaysTheSameConfiguration`, `rollsBackMixedJpaWritesWhenGroupProvisioningFails`; `DefaultInvitationServiceTest.acceptsVerifiedIdentityAtomicallyAndAddsOnlyBasicMembership`, `rollsBackActorBindingMembershipAndGroupEdgeWhenBasicProvisioningFails` |
| Exact issuer/subject identity resolution is case-sensitive, does not conflate issuers, and locked resolve-or-create reuses one stable `STANDARD` Actor | `JpaExternalIdentityRegistryTest.resolvesExactIssuerAndSubjectWithoutCaseOrIssuerConflation`, `resolveOrCreateReusesTheStableActorAndKeepsStandardAccountType` |
| An external identity binding prevents its Actor from being deleted underneath it | `JpaExternalIdentityRegistryTest.databasePreventsBindingDeletionFromOrphaningAnActorRelationship` |
| Missing, malformed, invalid-signature, wrong-issuer, wrong-audience, expired, not-yet-valid, missing-subject, or blank-subject bearer tokens fail | `BearerAuthenticationIntegrationTest.rejectsMissingBearerToken`, `rejectsMalformedBearerToken`, `rejectsInvalidSignature`, `rejectsWrongIssuer`, `rejectsWrongAudience`, `rejectsExpiredTokenBeyondClockSkew`, `rejectsTokenBeforeNotBeforeTimeBeyondClockSkew`, `rejectsMissingSubject`, and `rejectsBlankSubject` |
| An unknown exact bearer binding fails; matching provider email never substitutes for that binding or performs JIT registration | `BearerAuthenticationIntegrationTest.rejectsValidTokenWithoutIdentityBinding`, `rejectsMatchingEmailWithoutExplicitIdentityBinding` |
| An active Basic-only member receives no capability from inflated provider roles/scopes; bearer requests create no session, override another Actor's cookie only for that request, preserve the browser identity afterward, and reject invalid bearer without cookie fallback | `SessionSecurityIntegrationTest.userDirectoryConvergesAfterAdmissionAndLiveMembershipRevocation` uses a signed token with privileged realm/client roles and scopes, anonymous and owner cookie jars, persisted session counts and subsequent cookie-only requests |
| The same already-issued bearer token and open browser session observe null Tenant/empty capabilities after application deactivation, and protected Users access is denied | `SessionSecurityIntegrationTest.userDirectoryConvergesAfterAdmissionAndLiveMembershipRevocation` |
| A bound bearer Actor without active Tenant membership receives stable Actor identity, null Tenant, no global/scoped capabilities, and revision zero | `BearerAuthenticationIntegrationTest.returnsEmptyTenantAuthorityForBoundActorWithoutMembership` |
| A bound owner receives durable Tenant presentation and expanded implemented capability tokens | `BearerAuthenticationIntegrationTest.returnsDurableTenantAuthorityForBoundOwner` |
| Active owner/member projection and inactive member/Tenant exclusion use the unified JPA Tenant aggregate | `JpaTenantAccessResolverTest.resolvesActiveOwnerAndMemberMemberships`, `excludesInactiveMembershipAndInactiveTenant` |
| Anonymous current-identity probes return `401` without creating a JDBC session or emitting a session cookie | `SessionSecurityIntegrationTest.rejectsAnonymousIdentityWithoutCreatingASession` |
| OAuth2 login replaces provider identity with `ActorId`, stores no provider token marker, and guarded sign-out invalidates the session | `SessionSecurityIntegrationTest.authenticatesAndSignsOutTheInitialOwnerWithoutProviderState`; `identity-shell.spec.ts` — `signs out from the account menu with the same-origin guard` |
| A bound identity without active Tenant authority and without trusted JIT or an eligible invitation gains no application session; admitted owner/member sessions read current authority from `/api/identity/me` | `SessionSecurityIntegrationTest.rejectsABoundIdentityWithoutTenantMembershipAndInvalidatesItsSession`, `authenticatesAndSignsOutTheInitialOwnerWithoutProviderState`, and `acceptsInvitationThroughPkceAndPersistsOnlyTheMemberActorSession` |
| Latest nullable profile observations retain exact binding provenance and reject Actor/binding mismatches | `JpaActorProfileRecorderTest.recordsLatestTruthfulProfileWithExactBindingProvenance`, `rejectsAnActorAndBindingMismatchWithoutCreatingAProfile`; `SessionSecurityIntegrationTest.userDirectoryConvergesAfterAdmissionAndLiveMembershipRevocation` |
| Users projections expose `STANDARD` account type and real system/ordinary Groups while invitations expose neither an admitted account type nor memberships | `DefaultUserQueryServiceTest.presentsAuthoritativeRowsWithAccountTypeGroupsAndExactProfileProvenance`; `users-table.test.tsx` — `renders identity, real groups, account type, owner presentation, and invitation boundaries` |
| Users filtering remains bounded and server-authoritative, including Group filtering without changing global counts | `DefaultUserQueryServiceTest.filtersByRealGroupWithoutChangingGlobalCounts`, `preservesSearchStatusRoleAndStablePagination`, `requiresFreshUsersManageCapability`; `users-search.test.ts` |
| Only current `USERS_MANAGE` authority can activate/deactivate ordinary members; transitions are idempotent and preserve memberships, while the configured owner remains protected | `DefaultTenantMemberManagementTest.transitionsExistingMemberIdempotentlyWithoutReplacingMembershipOrGroups`, `protectsConfiguredOwnerEvenWhenRequestedStatusAlreadyMatches`, `requiresCurrentUsersManageCapability` |
| Locked concurrent invitation acceptance creates one member; accepted members receive active MEMBER, Basic-only authority with no manager/Admin edge | `PostgresInvitationAcceptanceConcurrencyTest.concurrentAcceptanceSerializesOnTenantThenInvitationAndCreatesOneMember`; `DefaultInvitationServiceTest.acceptsVerifiedIdentityAtomicallyAndAddsOnlyBasicMembership` |
| Capability implications are centralized and transitive: `IAM_ADMIN` implies every implemented capability; Group and Source management/delete imply their read capability | `IamCapabilityTest.expandsCentralImplicationsTransitivelyWithoutInventingGrants` |
| Effective global authority is the fresh union of all active Group grants followed by central implication expansion; removing a contributing grant takes effect on the next read | `PostgresIamAuthorizationTest.unionsFreshGrantsAndExpandsImplicationsCentrally` |
| Authority resolves as `GLOBAL`, `SCOPED`, or denied; scoped capability exists only for an active manager of an ordinary Group and never yields `USERS_MANAGE` | `PostgresIamAuthorizationTest.derivesScopedAuthorityOnlyFromActiveOrdinaryGroupManagement` |
| Scoped Group reads expose only the manager's own ordinary Groups, never unrelated or system Groups | `PostgresIamAuthorizationTest.scopedGroupProjectionReturnsOnlyTheManagersOwnOrdinaryGroups` |
| Scoped managers cannot delegate capabilities they do not hold, remove peer managers, change manager flags, or use global Users/Group-grant administration | `DefaultGroupServiceAuthorizationTest.scopedManagerCannotAddAMemberWhoWouldReceiveCapabilitiesTheManagerDoesNotHold`; `PostgresGroupMembershipReplacementTest.scopedManagerCannotRemoveAnotherManagerButGlobalAdministrationCan`; combined runtime evidence in the [MEM-55 verification record](../increments/completed/mem-55-users-management/verification.md) |
| `IAM_ADMIN` is reserved to Admin, Basic cannot carry capability grants, and system Group provisioning is idempotent without reactivating members or making them managers | `PostgresIamAuthorizationTest.rejectsReservedOrBasicGroupGrantsAtTheDatabaseBoundary`; `PostgresGroupProvisionerTest.provisionsSystemGroupsAndBasicMembershipIdempotentlyWithoutChangingTenantMembership` |
| The configured owner and final active `STANDARD` Admin survive lifecycle changes; Admin deletion is rejected at runtime | `PostgresGroupAdministrationGuardTest.configuredOwnerAndFinalActiveStandardAdministratorSurvive`; combined runtime evidence in the [MEM-55 verification record](../increments/completed/mem-55-users-management/verification.md) |
| `IAM_ADMIN` Users edits replace ordinary Group memberships only, preserve system edges and retained manager flags, and advance the authorization revision | `PostgresGroupMembershipReplacementTest.ordinaryMembershipReplacementPreservesSystemEdgesAndRetainedManagerFlags` |
| Group membership/grant foreign keys are Tenant-qualified; deleting an ordinary Group removes only its memberships, grants, and Source associations | `GroupSchemaIntegrityTest.membershipAndCapabilityGrantsRequireTenantQualifiedGroups`, `deletingAnOrdinaryGroupRemovesOnlyItsLinksAndGrants` |
| V14 follows the V13 profile migration, classifies existing Actors as `STANDARD`, adds revision zero, clears incompatible serialized sessions, rejects unsupported account types/legacy `ADMIN` writes, and fails actionably rather than silently reclassifying historical `ADMIN` rows | `IamSchemaMigrationTest.v14ClassifiesActorsAddsAuthorizationRevisionAndInvalidatesSerializedSessions`, `v14FailsActionablyRatherThanReclassifyingHistoricalAdminMemberships` |
| V15 seeds Admin + `IAM_ADMIN`, owner Admin membership, and Basic membership for every existing active or inactive Tenant member without changing activation | `GroupMigrationSeedTest.seedsAdminOwnerAndEveryExistingMembershipIntoBasicWithoutChangingActivation` |
| Exclusive authority mutations increment `authorizationVersion` only on commit; shared resource writes serialize with revoke and the next request fails closed | `PostgresIamAuthorizationTest.advancesAuthorizationVersionOnlyWhenTheExclusiveMutationCommits`, `sharedResourceWriteSerializesAConcurrentRevokeAndNextRequestFailsClosed` |
| A revision-only identity change resets mounted active private queries before cache removal, so stale Source UI converges even when capability tokens are unchanged; actor-local state remount remains keyed to Actor change rather than every revision | `application-session-boundary.test.tsx` — `drops mounted private data when authority revisions change without capability changes` and `remounts private children when the actor changes without changing authority` |
| Identity, private-query, and private-mutation `401` responses purge private client state; a private `403` refreshes exact identity and purges only when durable authority changed | `application-session-boundary.test.tsx` — `purges private client state when the identity query becomes unauthenticated`, `resets active identity after a private query returns unauthenticated`, `resets active identity after a private mutation returns unauthenticated`, `refreshes an inactive fresh identity and purges private state after a forbidden mutation`, and `keeps private state when an ordinary forbidden operation leaves identity unchanged` |
| Public not-provisioned state remains distinct from signed out; transient identity failure remains retryable | `application-session-boundary.test.tsx` — `renders the provisioning state when durable membership is absent`; `identity-shell.spec.ts` — `keeps unprovisioned access separate from signed-out state`, `recovers from an unavailable identity endpoint without treating it as signed out` |
| The committed browser API snapshot includes nullable Tenant context, global/scoped capabilities, `authorizationVersion`, Groups, Users Group replacement, and Source associations while excluding non-API routes | `OpenApiContractTest.committedContractDescribesOnlyTheLiveBrowserApi` |

The API/browser integration uses a local OIDC issuer and JDBC-backed Spring Session; IAM lifecycle uses JPA and bounded projections/locks remain concrete SQL. It covers Authorization Code + S256 PKCE, exact identity resolution, admission, provider-state disposal and revocation isolation. The [combined MEM-55/MEM-36 verification record](../increments/completed/mem-55-users-management/verification.md) contains the passing final repository/frontend gates and isolated PostgreSQL, Keycloak, MinIO, Mailpit, API, worker and browser evidence. JetBrains inspection was unavailable; no IDE-clean claim is made.

## MEM-59 browser JIT verification

Targeted and repository gates passed; see [verification evidence](../increments/completed/mem-59-tasco-jit/verification.md). `DefaultTrustedIdentityAdmissionTest` covers durable admission, exact binding reuse, authority preservation, inactive denial, rollback and concurrent first login. `ActorSessionLoginSuccessHandlerTest` covers ID-token versus UserInfo trust, issuer pinning and empty opt-in. `SessionSecurityIntegrationTest.admitsTrustedBrowserIdentityWithoutEmailVerificationOrConsumingInvitationAndDeniesReactivation` and `rejectsUntrustedOrMalformedBrowserProviderClaimsAndNeverJitsBearerClaims` exercise the real Spring HTTP callback/session boundary with a signed synthetic issuer and isolated PostgreSQL. The requirements below include remaining live Keycloak mapper/broker and actual Tasco acceptance; synthetic OIDC evidence does not prove those external integrations.

| Requirement | Required verification |
| --- | --- |
| JIT defaults off and trusts only a strict allowlisted String from the configured Keycloak issuer's validated ID token | Empty allowlist; missing/blank/non-String/unlisted claim; wrong issuer; matching claim only in UserInfo/access token must not provision. Preserve eligible invitation fallback when JIT is not selected. |
| Trusted first browser admission creates/reuses the exact STANDARD identity and active MEMBER/non-manager Basic only | Verify durable Actor/binding/membership/Basic state, no Admin/ordinary Group elevation, nullable or unverified email allowed, and no email linking. |
| Tenant-before-Actor locking makes first/repeat/concurrent admission atomic and idempotent | Concurrent exact-identity admission yields one aggregate; Basic failure rolls back new Actor/binding/membership; inactive Tenant and inactive/incompatible membership deny without reactivation. |
| JIT and invitation authority remain separate | Trusted JIT with a pending invitation never writes/consumes it; local invited users still require exact verified-email acceptance; existing active-member path retains precedence. |
| Browser token state is discarded and bearer never provisions | Exercise actual callback with session rotation, ActorId-only persistence and profile recording; unbound bearer with matching provider claim fails without Actor/member/session creation. |
| Keycloak maps only the broker session note to the browser ID token | Reconcile create/replay/drift correction on isolated `memoryos-web`; verify one stable mapper UUID, strict String claim and no access-token/UserInfo emission, with no upstream or other-client changes from the addition. |
| Application deactivation remains authoritative after trusted admission | Existing session loses current authority; a subsequent trusted login does not reactivate the member. |
| Simulator and actual Tasco acceptance remain distinct | Record isolated broker/browser runtime evidence first; record the actual approved Tasco provider/account flow separately. Neither upstream revocation nor absolute session lifetime is covered by MEM-59. |
