# Meetings verification

| Contract | Verification |
| --- | --- |
| A meeting is owner-private: another member gets 404 for read, ticket and delete and sees an empty list; titles and participant lists are trimmed and de-duplicated; an in-person meeting refuses a `TAB` ticket; a ticketed `MIC` socket stores the provider's utterance at the requested offset, answers `ready`, `utterance`, `finished` and closes normally; a spent ticket is refused; speaker naming, stale-revision notes (409), end, the ended-meeting ticket refusal (409 `MEETING_ENDED`), list duration and cascade delete | `ChatSessionApiIntegrationTest.meetingsAreOwnerPrivateAndStoreUtterancesFromATicketedTrackSocket`: full API/security, real PostgreSQL, WebSocket and a loopback OpenAI-compatible provider |
| Soniox live streams send diarization, endpoints, language and context terms; final tokens become segments split by speaker change and `<end>` with the offset applied; finalize ends with an empty frame; a provider error reconnects, replays the last five seconds and shifts times; exhausted retries report one failure | `SonioxLiveTranscriptionTest`: controlled JDK WebSocket boundary |
| Providers without a live protocol get utterances cut at pauses, silence never reaches the provider, the recording clock is kept and a provider error is reported | `ChunkedLiveTranscriptionTest` |
| An uploaded recording runs the whole way: the reservation answers a presigned upload and holds the meeting in `TRANSCRIBING`; `end`, a second recording and another member are refused while it waits; finalizing queues it; the job asks the provider for timed segments under the file's own name, stores them as `MIC` utterances with one speaker, ends the meeting, queues its minutes and forgets the recording | `ChatSessionApiIntegrationTest.uploadingARecordingTranscribesItAndThenDeletesTheAudio`: real PostgreSQL, the scheduled job and a loopback OpenAI-compatible provider |
| A Tenant with no connection that reads a file, a container that is not audio and a file past the limit are all refused at the reservation, and leave the meeting as it was | `ChatSessionApiIntegrationTest.aRecordingIsRefusedBeforeItIsSentWhenNothingCanReadIt` |
| Soniox tokens group into one utterance per speaker, broken at a speaker change and at a pause, with the mean confidence and an end derived from a duration; control tokens are dropped; OpenAI verbose segments become one speaker with times and a confidence read from its log probability, and a server without them still gives one utterance; only Soniox separates speakers and only OpenAI caps the file | `BatchTranscriptionTest` |
| A meeting is unreachable to another member until it is shared, and then readable: the transcript, the speakers and the minutes arrive, the owner's notes and the reader list do not, and the list row says it is not theirs. Renaming a speaker, writing notes, rerunning the minutes, sharing, deleting and taking a ticket all answer 404 for a reader. Naming somebody outside the Tenant is refused and leaves the readers as they were; emptying the list makes the meeting unreachable again | `ChatSessionApiIntegrationTest.aSharedMeetingIsReadByItsReaderAndChangedByNobodyButItsOwner` |
| Ending a meeting queues its minutes; the API job writes them with the transcript's numbered lines and the owner's speaker names; a decision and an action point back at the utterance they rest on; an item with no text is dropped; ticking an action off persists; an unknown item and another member's rerun answer 404; the call is billed to the owner's Tenant as `MEETING_MINUTES` | `ChatSessionApiIntegrationTest.endingAMeetingWritesItsMinutesFromTheTranscriptWithTheLinesTheyRestOn`: real PostgreSQL with the scheduled job and a stubbed model |
| The prompt numbers every line, carries the title, participants and owner notes, refuses invention and unowned work, and keeps both ends of a transcript past the budget while saying part is missing | `TranscriptSummarizerTest` |
| The published minutes carry the summary, the decisions and the work with owners, deadlines and quotes, are named after the meeting with no character a file system refuses, render a heading even when nothing was decided, and never carry a line of the transcript | `MeetingMinutesMarkdownTest` |
| `POST /api/meetings/{meetingId}/library` puts written minutes into the owner's library as one Markdown file through the server-write lifecycle, and asking again returns the same file; another member gets `404` and a meeting without written minutes `400` | `ChatSessionApiIntegrationTest.publishingWrittenMinutesPutsOneFileInTheOwnersLibraryAndRefusesOthersAndUnwrittenMinutes`: full API context and PostgreSQL, object storage stubbed |
| The biên bản follows mẫu 1.9 (quốc hiệu, cơ quan, số, về việc, I–IV and the two signature columns), reads an action back as an assignment with its owner and deadline, prints an empty heading field as an ellipsis, uses Times New Roman at the decree's sizes and draws no table borders | `MeetingMinutesDocumentTest`: the rendered bytes are read back with `XWPFDocument` |
| The meeting module stays closed and depends only on IAM's Group interface, `ai`, `voice`, `library`, `objectstorage` and `usage`, never on `chat`; its persistence is private | `ModulithArchitectureTest`, `CoreDependencyRulesTest` |
| Browser support follows Nojoin (shared audio on desktop Chromium, microphone-only on mobile Chrome, otherwise unsupported); a shared tab keeps only its audio, a missing tab audio and a closed picker are reported | `meeting-capture.test.ts` |
| The track socket addresses the meeting at a whole-millisecond offset, opens on `ready`, relays previews and stored utterances, finishes after `finished`, rejects a refused open with its code and reports a failure once | `meeting-socket.test.ts` |
| The recorder queues audio until a track connects, reconnects at the delivered offset and replays the queue, stops on terminal codes, drops audio while paused and resumes the clock without the pause, and flags a quiet shared tab | `meeting-recorder.test.ts` |
| A member uploads a recording, sees the chosen provider's speaker separation and size limit before sending it, cannot send it without the consent statement, and the meeting then says it is being transcribed with no stop control | `tests/e2e/meetings.spec.ts` |
| A member creates an in-person meeting with consent, records with Chromium's fake microphone through the real AudioWorklet, sees stored utterances and the live preview, names a speaker, stops and ends the meeting, exports the biên bản with the heading the meeting pre-filled, at 1440 and 390 px without horizontal overflow | `tests/e2e/meetings.spec.ts` with a mocked API and WebSocket |
| An utterance keeps the stretches the provider was unsure of, as offsets into the line it is stored with: a token below the threshold is marked at the word and not at the space before it, neighbouring uncertain tokens become one stretch carrying the worst of them, a token at the threshold is left alone, and a leading space on the first token does not shift the marks | `SpokenTextTest` |
| Both Soniox adapters mark the same way: the live stream marks within the segment a token lands in, and an uploaded recording marks within its grouped segment | `SonioxLiveTranscriptionTest`, `BatchTranscriptionTest` |
| A stretch survives the whole way — Soniox tokens, the transcription job, `jsonb`, the REST read — and still indexes the characters the reader sees | `ChatSessionApiIntegrationTest.aStretchSonioxWasUnsureOfReachesTheTranscriptAtTheRightCharacters`: real PostgreSQL and a loopback Soniox |
| The transcript marks exactly those characters and nothing else, reads whole with or without marks, and ignores a stretch that falls outside the line | `transcript-text.test.tsx` |
| A quiet recording is pinged every 20 seconds so no proxy in front reads it as dead, and a track whose audio stops altogether is still closed rather than pinged back to life | `MeetingStreamWatchdogTest` |
| A proposal is an offer: a pass changes nothing, a reader of a shared meeting reaches neither the pass nor its proposals, accepting rewrites the line and records the provider's own words as the first event, deciding twice answers 409, and reverting puts the words and the mark back | `ChatSessionApiIntegrationTest.aProposalIsOnlyAnOfferUntilTheOwnerTakesItAndCanBeTakenBack`: real PostgreSQL and a stubbed model |
| A voice that introduces itself is offered its name at the earliest line that says so, at 0.97 when the name is a participant and 0.9 otherwise; a role, a lower-case name or an ordinary sentence is refused; a named or dismissed voice is never asked about | `SpeakerIntroductionsTest` |
| The owner sees the offered name with the line it came from, accepts it into the ordinary rename or dismisses it, and the offer does not come back | `tests/e2e/meetings.spec.ts` |
| A mark on part of a word covers the whole word and pieces of one word join at their lowest confidence; a pass asks about the word and its answer replaces the word; a legacy proposal on half a word replaces the whole word once, and reverting puts back exactly that word | `MeetingCorrectionSpansTest`, `ChatSessionApiIntegrationTest.aMarkOnHalfAWordIsAskedAboutAndReplacedAsTheWholeWord` |
| The owner writes what was said at a marked word: refused while recording, for an unmarked stretch (409) and for a reader (404); the line becomes `HUMAN` and loses that mark, the change appears as an accepted correction and a revert puts the provider's words and the mark back | `ChatSessionApiIntegrationTest.theOwnerWritesWhatWasSaidAtAMarkedWordAndCanTakeItBack` |
| Clicking a highlighted word opens a field holding the word; Enter sends its range and the new words, and the transcript shows them | `tests/e2e/meetings.spec.ts` |
| Marks that read as one phrase are asked about together and a full stop keeps them apart; the ask carries the words either side, the speaker, the meeting's terms, the marked window and the same words said elsewhere; the instructions put the burden on replacing rather than on keeping, defend against instructions inside the transcript, and ask nothing about naturalness | `TranscriptCorrectorTest` |
| Accepting moves the line's remaining marks: one covering the replaced words is dropped, one before it stays and one after it lands on the same word | `MeetingCorrectionSpansTest` |
| A star and a bookmark belong to the reader who left them: the owner reading the same meeting sees neither, an unnamed bookmark is numbered, a time outside the recording is refused, a line of another meeting cannot be starred through this one, and one reader cannot take back another's mark | `ChatSessionApiIntegrationTest.starsAndBookmarksBelongToWhoeverLeftThemAndNobodyElseSeesThem` |
| Searching the transcript counts its hits and filtering to starred lines hides the rest | `tests/e2e/meetings.spec.ts` |
| The Word file and the PDF carry every line with its time and speaker, a named speaker keeps their name, an unnamed one is numbered in the meeting's language, the clock grows an hour only past an hour, and Vietnamese marks survive into the PDF page | `MeetingTranscriptExportTest`: the bytes are read back with `XWPFDocument` and `PDFTextStripper` |
| The owner corrects the summary and one action's words, owner and deadline; each change is an event carrying what the model wrote; a decision may not be given an owner; a reader of a shared meeting is refused; and a rerun answers 409 until it is told to discard the corrections, after which the minutes are the model's again | `ChatSessionApiIntegrationTest.theOwnerCorrectsTheMinutesAndARerunAsksBeforeThrowingThatWorkAway` |
| The chosen typeface is named on every run of the biên bản, tables included, and a face nobody offered falls back to Times New Roman | `MeetingMinutesDocumentTest.theChosenFaceIsNamedOnEveryRunAndAnUnknownOneIsNot` |
| Topics come back in the order the meeting reached them, and one on a missing line or on a line already taken is dropped | `ChatSessionApiIntegrationTest.endingAMeetingWritesItsMinutesFromTheTranscriptWithTheLinesTheyRestOn` |
| Choosing a topic in the timeline scrolls its line into view | `tests/e2e/meetings.spec.ts` |
| The PDF biên bản carries every section and every Vietnamese letter, embeds the open face that stands in for the one picked (Tinos, Arimo, Carlito, and Tinos for anything unknown), and never leaves the signatures alone on a page at any length from ten to forty actions | `MeetingMinutesPdfTest` |
| Exporting with `format=PDF` answers `application/pdf` under a `.pdf` name | `ChatSessionApiIntegrationTest.endingAMeetingWritesItsMinutesFromTheTranscriptWithTheLinesTheyRestOn` |
| The owner renames a meeting and replaces its participants after it started (trimmed, de-duplicated, a blank title refused), and a reader is refused | `ChatSessionApiIntegrationTest.meetingsAreOwnerPrivateAndStoreUtterancesFromATicketedTrackSocket`, `ChatSessionApiIntegrationTest.aSharedMeetingIsReadByItsReaderAndChangedByNobodyButItsOwner` |
| The biên bản heading opens unsaved, is stored trimmed with an unknown typeface dropped, reads back saved, is refused to a reader who writes and readable to one who reads; a second meeting starts from the last organization, parent and typeface only | `ChatSessionApiIntegrationTest.meetingsAreOwnerPrivateAndStoreUtterancesFromATicketedTrackSocket`, `ChatSessionApiIntegrationTest.theOwnerCorrectsTheMinutesAndARerunAsksBeforeThrowingThatWorkAway` |
| The owner writes in an action after the others with its owner and deadline, may not give a decision an owner or add a topic, and takes a decision out while its words stay in the events; a topic is neither ticked, rewritten nor taken out (404 each, the row unchanged); a reader can do none of it | `ChatSessionApiIntegrationTest.theOwnerCorrectsTheMinutesAndARerunAsksBeforeThrowingThatWorkAway` |
| The biên bản opens with labelled *Thời gian bắt đầu* and *Địa điểm* lines and does not repeat the subject | `MeetingMinutesDocumentTest` |
| The quốc hiệu holds on one line in the PDF | `MeetingMinutesPdfTest.itSaysWhatTheWordFileSaysAndDrawsVietnameseRatherThanQuestionMarks` |
| Stopping turns the microphone off at once and finishes once the last words are stored; the confirmation closes at once | `meeting-recorder.test.ts`, `tests/e2e/meetings.spec.ts` |
| The start form hides the title, participants, terms and sharing behind *Thêm chi tiết*; the page renames the meeting and edits its participants; the export dialog draws the API's PDF beside the heading, stores the owner's heading, and the page adds and removes a piece of work | `tests/e2e/meetings.spec.ts` |
| Meeting routes are documented and CSRF-protected in the committed OpenAPI | `OpenApiContractTest` |
| A small change answers with what it changed and never the transcript: notes and the name and people answer the revision the next notes save names, a speaker rename answers that speaker, a star or a bookmark the caller's own marks, a share the readers, a tick, rewrite or written-in item the item, a summary edit `{summary, edited}`, and taking an item out 204 | `ChatSessionApiIntegrationTest.meetingsAreOwnerPrivateAndStoreUtterancesFromATicketedTrackSocket`, `…starsAndBookmarksBelongToWhoeverLeftThemAndNobodyElseSeesThem`, `…theOwnerCorrectsTheMinutesAndARerunAsksBeforeThrowingThatWorkAway`, `…aSharedMeetingIsReadByItsReaderAndChangedByNobodyButItsOwner`, `…endingAMeetingWritesItsMinutesFromTheTranscriptWithTheLinesTheyRestOn` |
| Accepting or reverting one proposal and writing a word by hand answer the one line as it now reads with the proposal as it now stands, and never the transcript; declining answers the proposal alone and leaves the line as the provider heard it; the answer names the correction the list holds | `ChatSessionApiIntegrationTest.aProposalIsOnlyAnOfferUntilTheOwnerTakesItAndCanBeTakenBack`, `…aMarkOnHalfAWordIsAskedAboutAndReplacedAsTheWholeWord`, `…theOwnerWritesWhatWasSaidAtAMarkedWordAndCanTakeItBack` |
| Rewriting a line returns it as a reader reads it, and accepting or declining a proposal returns it as the list holds it | `MeetingRepositoryTest.decidingOneStretchAnswersTheLineAndTheProposalAsTheyNowStand` |
| The browser folds one correction into the cached meeting and the cached proposals without reading either again, leaves the transcript alone when a proposal is declined, appends a word written by hand, and never changes a topic from an item answer | `meetings-api.test.ts` |
| Naming a speaker answers only that speaker; taking the name off offers the name the voice gave itself again, read from its own lines; a dismissed voice is not offered one; an unknown voice answers 404 | `ChatSessionApiIntegrationTest.namingASpeakerAnswersWithThatSpeakerAndAnUnnamedVoiceIsOfferedItsOwnNameAgain` |
| Saving notes or the name and people returns the revision the row now holds; ticking an item returns it as stored and finds nothing in another meeting or on a topic; one voice's lines are read in order without the rest of the meeting | `MeetingRepositoryTest` |
| The browser folds a narrow answer into the cached meeting and keeps its transcript, leaves an uncached meeting for its next read, replaces one speaker or item in place, adds a written-in item after its kind, and marks the minutes the owner's only when an item carries their words or one is taken out | `meetings-api.test.ts` |
| Every WebSocket path the browser opens lands in an nginx location that forwards the upgrade, resolved the way nginx resolves it (exact `=` first, then regex) | `web/scripts/nginx-websocket-routes.test.mjs`: parses the shipped `nginx.conf` |

Live Soniox acceptance with real meetings, real microphones and shared tabs remains open, as for MEM-91.
| A recording that is done with — transcribed, failed on its last attempt or abandoned with its last lease — keeps its upload on the meeting until the sweep retires it; the sweep retires each once, forgets only that upload, leaves a recording still waiting alone, forgets an upload an earlier pass already retired, and retries one whose retirement failed | `MeetingRecordingRetirementTest`, `MeetingRepositoryTest.onlyFinishedRecordingsAreHeldAndForgettingOneLeavesANewerUploadAlone`, `MeetingRepositoryTest.aRecordingWhoseLastLeaseLapsedFailsEndsTheMeetingAndIsHeldForRetirement` |
| An uploaded recording is streamed into the provider's multipart upload: opened once, read in chunks of at most 64 KiB, never read whole, with an exact `Content-Length` | `SonioxAsyncTest.aRecordingIsStreamedToTheUploadInSmallReadsAndNeverReadWhole` |
| A transcribed recording's 300 lines and their speakers, the minutes items, the shares and the correction proposals are each written in a fixed number of statements, with every column intact | `MeetingRepositoryTest.aTranscribedRecordingItsMinutesAndItsSharesAreWrittenInAFixedNumberOfStatements` |
| Every voice is named one way — a name, the owner on an online microphone, otherwise numbered in the meeting's language — and times read `mm:ss` then `h:mm:ss`, in the exports and in the minutes prompt | `MeetingTranscriptExportTest.everyVoiceIsNamedTheOneWayThePageNamesIt`, `ChatSessionApiIntegrationTest.endingAMeetingWritesItsMinutesFromTheTranscriptWithTheLinesTheyRestOn` |
| PDF text wraps greedily, cuts a word longer than a line without losing a character, and draws a character the face lacks as `?` | `PdfTextTest` |
