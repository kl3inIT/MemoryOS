# MEM-142 plan — my file library

- [x] **Schema.** `V90__chat_file_library.sql`: `deleted_at`, `cleanup_token`, `cleanup_until`, `owner_actor_id`, `session_id` on both artifact tables; `filename` and `size_bytes` on `chat_image_artifact`; backfill from `chat_message`/`chat_session` and `stored_objects`; `NOT NULL` and the owner listing indexes after the backfill. Both insert paths take owner, conversation and the image's name from the answer row itself.
- [x] **Artifact deletion.** `deleted_at` under the owner check, idempotent; `DELETE /api/chat/file-artifacts/{id}` and `DELETE /api/chat/image-artifacts/{id}`.
- [x] **Durable cleanup.** `ChatArtifactCleanupService` with the claim/lease/token shape of `DefaultExtractionArtifactService`; `markDeletePending` → `storage.delete` for the artifact and the V73 preview → `releaseAdopted` + `remove` + row deletion in one claim-fenced transaction; `memoryos-chat-artifact-cleanup-v1` on a one-minute fixed delay in `ControlPlaneConfiguration`. `attachPreview` requires `deleted_at IS NULL`, so a late conversion discards its staged object.
- [x] **Read paths.** `deleted_at IS NULL` on `ownedArtifact`, `ownedChart`, image `owned`/`inSession`/`byMessage` and the `TurnContext` generated-image lookup; conversation history returns deleted artifacts with `deleted: true` and the answer shows a deleted card.
- [x] **Files in use.** `usedByWorkspace` became `usage`, a batch lookup of Project and Agent names; `ChatFileInUseException` carries them into a `409 CHAT_FILE_IN_USE` with `usedBy`.
- [x] **Listing.** `JdbcChatLibraryRepository` union with the derived category, name search (image rows also match `revised_prompt`, wildcards matched literally), source/category filters, four sorts, window totals; `ChatLibraryService` for authority and paging; `GET /api/chat/library`; `openapi.yml` and the generated client.
- [x] **Page.** `/library` route and sidebar item; table and grid views, filters, search, time grouping, total size, selection; preview through `ChatFilePreviewModal` with the new image-artifact target; download, open conversation, confirmed deletion, per-item results for a selection; vi/en keys, responsive layout and labelled controls.
- [x] **Tests.** `ChatPersistenceIntegrationTest.theFileLibraryUnionsEverySourceForItsOwnerAndHidesWhatWasDeleted`, `ChatArtifactCleanupIntegrationTest`, `ChatSessionApiIntegrationTest.fileLibraryListsOwnUploadsAndNamesWhatBlocksDeletingOne`, `ChatFileLibraryMigrationTest`, `chat-library-page.test.tsx`, the deleted-card case in `chat-code.test.tsx`.
- [x] **Docs.** `docs/specs/chat.md` file-library section and HTTP rows, `docs/tests/chat.md` matrix, `AGENTS.md` active increment entry.
- [ ] **Evidence.** `:core:test`, `:api:test`, `:connector:test` and `:worker:test` pass locally (2026-09-20), as do the web lint, format, i18n, typecheck and unit suites. One uninterrupted `./gradlew clean check` and the staging verification with an upload, a `run_python` file and a generated image remain open; the local machine ran out of memory during the full gate.
