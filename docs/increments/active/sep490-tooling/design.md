# SEP490 documentation and diagram tooling

Import the eight supplied academic templates into `docs/academic/sep490/templates/`, retaining their bytes and recording SHA-256. Provide a guide mapping R1–R7 to existing project knowledge and identifying missing templates.

Import Visual Paradigm MCP as an independent Gradle build in `tools/visual-paradigm-mcp/`. Team members obtain its source with the MemoryOS checkout. Runtime modules are unchanged. Keep local Visual Paradigm installations, generated output and credentials outside version control. Ignore root `/output/` without deleting local files.

Track report preparation in MEM-85 and diagram work in MEM-86. Import does not complete those academic deliverables.
