# Verification — MEM-74/MEM-22, 2026-09-12

## Completion batch: application localization and renderers

- Implemented vi/en catalogs/consumers across current app surfaces, registry capability names, provider labels, safe Source/Chat upload errors, toast descriptors, a11y labels and locale dates/numbers. Static JSX/key audit reports zero findings. Catalog parity and explicit registry/user-data tests supplement (not replace) browser coverage.
- Final frontend `check` passed with **176 tests / 31 files**, generated API/routes stable, zero static i18n audit findings, lint/format/typecheck/build/font checks. This includes the final recent-file picker/native-composer safe upload error unification and its additional no-raw-diagnostic/no-auto-retry test.
- Backend focused core/API run passed in 2m34s: artifact native tool binding, allowlist/bounds, terminal winner persistence, follow-up context budget, turn service, actual native `render_gui` provider continuation, saved HTTP history, foreign-Actor denial and terminal metadata flag.
- Final full browser rerun: **86/86 passed in 8.4m**, one worker, after the backend/frontend gates had finished. Earlier diagnostics are retained for transparency: 73/86 initially passed; locale expectations and a real JSX whitespace regression were corrected, then 12/13 failed cases passed. The long edit/regenerate/rate/share/revoke/delete scenario timed out at 30s under concurrent load, then passed alone in 22.8s and in the final whole suite in 19.2s. No timeout/assertions were weakened. The final result is one all-green 86-case run, not a sum of reruns.
- English desktop / Vietnamese mobile renderer cases passed: Shiki tokens, real Mermaid image, zoom dialog, inert model strings, artifact panel, no horizontal overflow, draft/focus preservation and reload with one inference. Screenshots `output/playwright/renderers-en.png` and `renderers-vi.png`.
- Production compiled assets were separately served locally with the checked-in CSP. Default Shiki WASM failed `script-src 'self'`; switched to cached JavaScript engine without CSP changes. Recheck observed five colored tokens (three distinct computed colors), loaded Mermaid SVG image and **zero WASM resource requests**. Artifact opened in the existing sidebar; screenshot `output/playwright/renderers-production-csp.png` visually inspected. Model-produced script-looking text remained literal. Existing lazy bundle-size/runtime-config build warnings remain; no network/model quality claim.
- First whole backend `clean check` hit the existing 15-minute core-test timeout while browser/frontend suites were running. No timeout, heap or tests were weakened. The fresh whole backend run without browser suites **passed in 9m22s** (24 tasks: 14 executed, 10 from cache). XML totals: **678 tests, 8 skipped, zero failures/errors** (API 134, core 413, worker 27, connector 104). Skips remain the opt-in live-provider/resource/Docling cases described below. JetBrains tools are unavailable in this completion session, so compilation/tests are the static-analysis fallback, not an IDE-clean claim.
- No OCR, CSP relaxation, model HTML execution, worktree, commit, PR, deploy or Linear mutation. Fixtures do not establish staging/live-provider acceptance. Historical checkpoints below describe earlier incomplete batches, not the implemented completion scope.

## Publication preflight — 2026-09-12

- User subsequently authorized scoped commits and a PR, not merge/deploy or issue closure. Feature branch `feat/mem-74-22-i18n-chat-renderers` starts at current `origin/main` `dd4ff00` in the existing checkout. Unrelated README/Drive/provider/Search/vision documentation and `output/` remain outside the commits; mixed Chat spec/test wording edits are excluded by hunk.
- `clean check --no-daemon --console=plain` passed again in 8s: 24 tasks, 11 executed, 12 from cache, one up-to-date. This reuses the already successful full suite; it is not a second uncached run.
- Backend and frontend staged-diff secret scans passed with redacted output. Source code and generated contracts match the prior verified implementation. The publication frontend `check` passed again: 176/176 tests across 31 files, generated API/routes stability, zero i18n audit findings, lint/format/typecheck/build/font checks.

## Historical foundation checkpoints

Not ready to close MEM-74 or MEM-22. This checkpoint implements account language, settings/session localization, a shared problem-presentation foundation, stable validation metadata and per-turn Chat language hints. Users/invitation/group-edit errors now consume safe shared descriptors; remaining application surfaces and other feature consumers still use their prior presentation paths.

## Verified so far

- `:core:compileTestJava :api:compileTestJava`: passed after adding the account preference and HTTP contract.
- Focused core/API run: `ChatLanguagePromptTest`, `ActorLanguageServiceTest`, `ChatTurnSetupTest`, `ChatPersistenceIntegrationTest`, `JpaActorProfileRecorderTest`, `BearerAuthenticationIntegrationTest`, `ApiExceptionHandlerTest`, `OpenApiContractTest`: passed. Tests use isolated migrated PostgreSQL and real HTTP for bearer/guard/validation cases. Preference survives profile observations, unsupported DB values fail, other actors and authorization revision remain unchanged, and running turns retain admitted language.
- OpenAPI regenerated from the running test context; browser client generated using the pinned existing toolchain, not hand-edited.
- `pnpm --dir web check`: foundation run passed with 154 unit tests; final expanded run is recorded below.
- New focused frontend tests cover catalog key/placeholder parity, both HTML languages, safe status/validation mapping, confirmed save, lost response, late response after Actor change, unchanged input node/draft/cache on language changes and preservation on transient background identity errors.
- Playwright CLI, local fixture server, desktop plus 390x844 mobile: real General settings route in the existing AppShell, Vietnamese → English, reload retaining confirmed preference, English → Vietnamese. Both mobile screenshots visually inspected with no horizontal overflow or clipped controls. Screenshots: `output/playwright/mem74-settings-mobile-en.png`, `mem74-settings-mobile-vi.png` (ignored artifacts). The fixture API does not prove live production identity acceptance. The local fixture lacks `/runtime-config.js` and reports its existing 404; this was not treated as application success evidence.
- JetBrains inspections with warnings enabled ran on all changed/new Java files, including final prompt/test edits; no reported errors. Existing JPA field/schema-resolution warnings remain on ActorEntity, plus the test's deliberate unknown `X-TenantId` header. New deprecated Jackson test calls were corrected and re-inspected cleanly. Lockfile inspection was clean; OpenAPI inspection timed out without findings, so no complete IDE-clean claim.

## Final gate results

- Final backend `clean check` after the validation OpenAPI schema/test addition: passed in 2m02s (24 tasks, 13 executed/11 from cache). XML totals: 670 tests, 8 explicitly skipped, zero failures/errors. No test limits or infrastructure requirements were weakened.
- Skips: four opt-in live Chat cases in `ChatSessionApiIntegrationTest` (corpus timing, grounded answers, real provider, vision), `ChatFileResourceMeasurementTest`'s opt-in process measurement, and three `DoclingServeIntegrationTest` live-service cases (Vietnamese/table, scanned PDF OCR, PPTX). None establishes fresh live-provider/resource/OCR acceptance for this increment.
- Validation projection test verifies stable EMAIL/SIZE codes, numeric limits, and absence of rejected values. OpenAPI declares the closed validation-entry shape and generated client matches it.
- Users error slice: row/form/group-edit failures store descriptors and use the common status taxonomy; known capability codes map to feature messages. Email field validation is linked to the input; unknown fields stay in the form summary. Confirmed actions show failure only in their dialog; rotation uses its row. ConfirmDialog supports safe descriptors and its generic failure/Cancel are localized. Legacy unmigrated callbacks may still return fixed strings; raw causes are not retained by this component.
- New tests change locale with an existing row error and failed confirmation open, assert the same DOM/error surface survives, and verify no second mutation or raw diagnostic rendering.
- After the Users error/ConfirmDialog changes, all 16 identity/Users browser scenarios passed in 46.1s. Final complete frontend `check` passed: 158/158 unit tests across 26 files, API/route generation stability, lint, format, TypeScript, build and font checks. Existing bundle-size/runtime-config build warnings remain.

- Earlier uncached full `gradlew.bat clean check --no-daemon`: passed in 13m22s (24 tasks, 19 executed/5 from cache), before the final additive OpenAPI validation schema/test refinement. Core's existing 15-minute timeout and 1 GiB heap were not changed. API/worker finished earlier; a bounded thread inspection observed the final core phase performing real OpenSearch bulk requests. The final gate above reran the changed API and reused unchanged core/connector/worker test results.
- Existing browser suite: 83/84 passed in 5.6m; the remaining test expected an English public not-provisioned heading although the new default is Vietnamese. After correcting that assertion, all 15 identity browser tests passed in 1.0m. The other 69 browser cases were unchanged after their passing run. This is not reported as a single all-green 84-test rerun.

## Spring Data follow-up — 2026-09-12

- Replaced the new handwritten language repository with aggregate-level `JpaActorRepository`. Routine reads use Spring Data; the only custom fragment reloads an Actor under `PESSIMISTIC_WRITE`. Active-membership authorization and transaction ownership remain in the service. Existing IAM lifecycle migrations and JDBC authority/worker mechanics are outside this follow-up.
- Focused core/API tests (`ActorLanguageServiceTest`, `JpaActorProfileRecorderTest`, `ChatPersistenceIntegrationTest`, `BearerAuthenticationIntegrationTest`) passed in 1m38s. The added PostgreSQL case verifies stale managed state is refreshed, the write lock is held and the subsequent change commits; real HTTP verifies Spring repository/fragment discovery and the existing preference security contract.
- All nine changed/new Java files inspected with warnings enabled; no errors or unresolved references. Existing weak extract-method suggestion in the test harness is retained to avoid unrelated fixture refactoring. Core/API test compilation and worker compilation passed.
- Follow-up full `gradlew.bat clean check --no-daemon`: passed in 9m32s, 24 tasks (18 executed, 6 from cache). XML totals: 672 tests, 664 passed, the same 8 opt-in tests skipped, zero failures/errors. All four module test tasks executed; cached tasks were compilation. A bounded diagnostic while waiting observed core executing IAM test fixture setup, not a stalled migration. The API telemetry fixture logged an OTLP receiver connection warning during shutdown; it did not fail the gate. Frontend is unchanged and was not rerun in this follow-up; its earlier evidence above is not represented as a fresh browser run.

## Remaining scope after the follow-up

Remaining Users/invitation labels and success/empty/loading states, full shell/shared component accessibility, Chat/attachments/Persona/Project, Search/reader, Sources/Groups translation and locale formatting; replacing remaining feature-local error switches and frozen toast/error strings; operation-specific recovery and safe diagnostics/Retry-After integration; no-new-hardcoded-string lint coverage; two-locale feature acceptance. Live model-language quality is unverified: prompt and snapshot tests only prove request assembly.

No OCR files changed. No commit, PR, deployment, worktree or Linear mutation. Existing `output/` content preserved; only run-owned browser artifacts added. The manual fixture browser/server are stopped after use; automated test fixtures own their cleanup.

## Chat Elements/example follow-up — 2026-09-12

- Unified message-file opening with the existing source panel and authority-scoped readers; desktop aside/mobile Radix Dialog. Horizontal native attachment cards show bounded names, full tooltip, actual size where known and an adjacent remove action. No permanent Ready line in the composer. Native preview hooks and their Zustand selector remain in use; no extra runtime/store/package was installed.
- Added localized ErrorState/connection notices and reader presentation; feedback reason IDs remain stable while labels change, failed submissions retain note/reason, and translated errors do not replay mutations. Full application translation and renderer integration remain incomplete; see [catalog/example audit](chat-elements-audit.md).
- Final `corepack pnpm@11.22.0 --dir web check`: passed after all runtime edits, including generated API/route drift, lint, formatting, TypeScript, 163 unit tests across 28 files and production build. Existing runtime-config script and >500kB chunk build warnings remain; no bundle/load optimization claim.
- `test:e2e tests/e2e/chat.spec.ts tests/e2e/chat-workspace.spec.ts --workers=2`: **38 passed in 3.2 minutes**, no retries. Covers native composer/IME, session promotion, edit/regenerate/branching, source panel desktop/mobile, markdown/copy, streaming/scroll, Stop/replay/denied streams, sharing and feedback persistence/retry. Local synthetic HTTP fixtures, not staging/live provider acceptance.
- New native-runtime reader tests verify file panel desktop/mobile, Escape/focus restoration, unchanged composer node/draft, in-place locale change without a second read and private cache release on close. Feedback test verifies localized failure preserves server reason ID/comment and makes exactly one submission.
- Playwright CLI checked the actual local application at 1440px desktop and 390px mobile; panel tab focus remains contained on mobile, Escape restores the file trigger and draft remains unchanged. Additional 320px composer check measured document width 320px and card width about 243px. The long-name tooltip exposes the full name; the finalized reused-file card displays the API-provided size. Existing fixture `/runtime-config.js` 404 remains separately acknowledged.
- Screenshots inspected: `output/playwright/attachment-card-{desktop,mobile}.png`, `output/playwright/file-panel-{desktop,mobile}.png`. Final card capture disables finite animations to avoid recording an insertion-transition frame. Panel content/identity are test fixtures, not evidence of real file parsing or permission provisioning.
- No Java/YAML/configuration file changed in this UI slice; earlier backend/IDE results above were not rerun or relabeled as fresh. `git diff --check` passed. No OCR changes, commits, PR, deployment or Linear updates.
