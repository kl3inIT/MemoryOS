# Basic Access capability bundle

## Goal

User-approved change: give Basic a persisted enum-backed grant following Onyx's code-defined permissions and database-backed Group grants. Main now supplies Chat at home and richer Search at /search. This increment enforces direct Search access and carries granular Chat capability vocabulary without adding new Chat enforcement; existing Chat membership/resource authorization remains. Image generation and LLM gateway remain reserved.

## Contract

- SYSTEM_BASIC implies SEARCH_READ, CHAT_READ, CHAT_WRITE, IMAGE_GENERATE and LLM_GATEWAY_USE; CHAT_WRITE implies CHAT_READ. Chat is implemented by merged main, but the two Chat tokens remain reserved for granular capability enforcement; image/gateway features remain absent. SYSTEM_ADMIN expands to every enum value, including main MODELS_MANAGE.
- Basic stores exactly SYSTEM_BASIC, Admin exactly SYSTEM_ADMIN. Basic children and Group/Source leaf capabilities cannot be granted directly. Preserve main's MODELS_MANAGE alongside USERS_MANAGE, GROUPS_MANAGE and SOURCES_MANAGE.
- Main V1–V36 are immutable. Local V37 seeds historical BASIC_ACCESS; V38 renames it and IAM_ADMIN to SYSTEM_BASIC/SYSTEM_ADMIN; V39/V40 make Group read and Source leaves derived-only. Preserve identities, memberships and Source associations.
- Bootstrap reconciles Basic to the same persisted grant. Invitation and JIT still add non-manager Basic membership only.
- Require global `SEARCH_READ` at both Search and document-passage application entry points before external provider IO. Keep active Tenant checks and current Source/document eligibility filters; Search permission is not permission to administer Sources or read every document.
- The latest UI decision keeps Onyx's collapsible permission card but system Groups show exactly their own bundle row: Admin shows only SYSTEM_ADMIN (Administrator access), Basic only SYSTEM_BASIC (Basic access). Both are readonly, with checked state from persisted explicit grants; no other administrative, Source or derived permission rows. Ordinary Groups retain editable-only options. Backend metadata still owns labels/descriptions and effective authority is unchanged.
- Preserve actor-only sessions and current live authority/revision convergence. No Keycloak change, role mirroring, JIT policy change or new endpoint.
- Full Admin refers to all application capability tokens, not bypassing active Tenant/membership, protected account invariants, document eligibility or token validation. The permission-list presentation adds no feature enforcement.

## Production-compatible system grant rename

V38 renames the protected grant strings in place after V37 seeds Basic, preserving identities, memberships, statuses, other grants and timestamps. No session invalidation is needed for ActorId-only principals. Back up and stop old API/worker writers before the coordinated cutover; rollback requires the matching pre-cutover database/release. The former local V18–V21 numbering is not compatible with main's Chat/Drive migration history: do not auto-repair or reset a database already using that layout.

## Groups-list UI alignment

The user additionally requested the outer Groups page match the supplied Onyx screenshot. Follow the actual Onyx GroupsPage/GroupsList/GroupCard, shared semantic-dark/primitives tokens and user/users SVG icons: centered 840px content, stacked Groups icon/title, blue information banner with a real documentation link, borderless search/New Group toolbar, black compact cards with Default badges, member counts and chevrons, and a separator between system and ordinary groups. Scope palette changes to this page, preserve light-mode readability, server-filtered search/pagination and authorized inline rename. Ordinary descriptions must not fabricate connector/document-set/agent counts absent from GroupSummary.

## System-group detail alignment

Follow Onyx `EditGroupPage.tsx` (header/body at lines 587–769): centered 840px Edit Group header with users icon and Cancel/Save Changes controls, blue System group banner, readonly Group Name, divider, compact search/Add toolbar and Name/Account Type member table with pagination. Do not render or fetch Source/connector sharing for system groups. Keep real server actions, member protection, candidate search, mutation guards and errors; do not copy unsupported account types or pretend membership mutations are staged. Existing membership commands remain immediate; Save Changes applies dirty editable settings only, disabled when there are none. Ordinary groups retain Source administration and editable grants.

Latest visual correction: Group Name, Group Members and Group Permissions share the section-heading typography. Member identity cells show only email, without display names or Owner/Manager/Inactive badges; absent email is explicitly unavailable, not fabricated. Account Type and authorized actions remain. Remove the Group Permissions subtitle, not individual permission descriptions. Protection and manager-state logic remain unchanged.

Member browsing and candidate selection are mutually exclusive modes of the same section. Share one mode-aware search toolbar and show only the active list and pagination. Add opens candidate mode; Done exits without mutation; confirmed addition returns to members. Preserve independent server filters/pages and selected candidate IDs without stacking a second panel over the current members.

## Derived-only Group read permission

GROUPS_READ remains an enum/read API check but is noneditable and excluded from direct-grant allowlists. GROUPS_MANAGE implies it; Admin includes it; managers retain scoped reads. V39 revokes direct GROUPS_READ without elevation and advances affected revisions. Basic/USERS_MANAGE gain no global Group visibility.

Picker audit: Users membership editing is SYSTEM_ADMIN-only, with derived read authority already present; Users filters query Groups only when current GROUPS_READ authority exists. Source association options use the existing SOURCES_MANAGE-authorized identity projection, not Group/member-list access. No new endpoint or broader implication is needed.

## Unified Source administration bundle

SOURCES_MANAGE covers global viewing, creation/configuration, upload/reindex and deletion, implying SOURCES_READ/SOURCES_DELETE; leaves are noneditable. Ordinary grants are USERS_MANAGE, GROUPS_MANAGE, SOURCES_MANAGE and main's MODELS_MANAGE. The scoped-manager set is not expanded to deletion.

V40 revokes direct Source leaves without promotion and advances revisions for revoked grants or existing SOURCES_MANAGE grants, including manager-only Tenants. Main V1–V36 remain immutable; matching binaries/schema must deploy together.

## Main merge integration

Main and feat/basic-access-capabilities fast-forward to 00a42f7. Local work is recovered from stash 5a8e57ae881f846ad8ff91e770c63ae8f98109af without committing it. Preserve Chat/Google Drive, the relocated Retrieval service and model administration. The original stash remains a safety copy. Renumber only unpublished local migrations V18–V21 to V37–V40 and verify against isolated databases.

Follow-up: use exact Onyx user/users/user-manage/user-plus/user-check/user-x/user-shield SVG paths from the local reference throughout Users/Groups-related surfaces. Keep one shared MIT-attributed module at components/icons/identity-icons.tsx; preserve accessible labels, interaction sizes and authority behavior. Models/Source and unrelated action icons remain unchanged.

## Onyx-style scoped managers for implemented Sources

The user approved applying the researched manager model to Groups/members and existing FILE/Google Drive Sources only; Document Sets, Skills, Agents and spending limits are out of scope. The user explicitly chose complete FILE private Search: active members of an associated group may read a RESTRICTED FILE document; outsiders may not. Google Drive remains excluded from document Search until its provider ACL contract is implemented. Existing PUBLIC Sources and unknown legacy ownership are preserved.

- Manager flags grant scoped authority, not global capabilities. Managers may rename their ordinary groups, add/remove existing Tenant members and assign/revoke other existing members' manager flags. Group creation/deletion, workspace invitations/account lifecycle and grant editing retain their global gates. Preserve owner/final-admin safeguards, enforce last-group membership on removal/deletion, and reject scoped self-removal/self-demotion that loses management. Do not copy Onyx's UI-only self-demotion gap. Non-Admin global Group administrators also cannot add members to a group whose grants they do not hold.
- Source browse visibility and editability are separate. Scoped managers may see PUBLIC Sources, Sources linked to a group they belong to and their own non-public groupless Sources. Scoped writes require non-public access and every current/requested group managed by the actor, with at least one group; the creator exception only covers an existing non-public groupless Source. Being creator never bypasses public or outside-group restrictions.
- Creation requires explicit managed ordinary groups for scoped callers and forces RESTRICTED access. Global FILE creation retains PUBLIC default and may explicitly choose RESTRICTED; Google remains RESTRICTED. Global creation may omit associations and never defaults to a system Group. Creation records immutable actor ownership. Scoped association replacement checks the union of current/requested groups and refuses an empty result; global replacement accepts an empty result. No migration silently publishes/restricts an existing Source or guesses its creator.
- Implement applicable Source rename, upload/reindex, Google manual sync/schedule and pause/resume of future automatic synchronization. Pause does not pretend to cancel active work or disable manual sync. FILE has no invented crawler pause/prune control. Existing Google root/approval/discovery configuration edits remain global-only. Global FILE access changes are explicit; scoped callers cannot publish. Shared Source deletion remains global-only; a scoped creator may delete its own non-public groupless Source, with durable authorized operation polling.
- Scoped Google credential use is owner-filtered; legacy unknown-owner credentials remain global-only. Credential mutation requires ownership and scope over all attached Sources for a scoped caller. Consent completion, provider IO and asynchronous selection activation recheck current authority. Pending creation persists selected groups and activates only if they remain valid; no fallback to Admin for a scoped request.
- Private FILE search scope binds the actor, rechecks current Tenant/group eligibility after provider IO and before/after passage expansion, and filters inaccessible Source metadata even for documents with multiple origins. Management authority alone is not document-read authority. Existing Chat reuse keeps its current membership/resource authorization, but cannot bypass the new private FILE ACL.
- V41 adds nullable Source creator and credential owner, persisted creation group IDs, Google automatic-sync pause state and scoped-owner cleanup receipt authority. Existing rows remain unchanged apart from the authorization epoch advance; no actor/credential ownership backfill. IAM/source mutation locks serialize role/group/access revocation; no shared-to-exclusive lock upgrade in async activation.

Backend Source action projections are authoritative for UI controls. Source/Group pickers, pending/error/readonly states and all same-origin mutation guards must follow the same policy; hiding a button is not authorization.

Ordinary Group details mount Group Permissions and request the capability registry only when the backend projects manage_grants. Scoped managers and global Manage Groups users without grant administration see no permission section, including no readonly switches. Preserve existing system-group readonly bundle presentation; backend grant mutation authorization remains SYSTEM_ADMIN-only.

Source configuration follow-up: existing Source root/approval/discovery and FILE visibility changes remain global SOURCES_MANAGE-only, while rename/reindex/sync/pause/schedule and group associations retain their scoped policy. Replacing an existing Google credential's OAuth app is deep configuration, not ordinary reconnect: require global SOURCES_MANAGE, including after consent/provider IO. A scoped owner may reconnect using the unchanged saved OAuth app if all attached Sources remain manageable; new scoped credential/Source creation stays available. UI configuration controls require both current global authority and backend actions, without using Source creator identity as a substitute.

Source catalog follow-up: the row's Manage/settings shortcut is present only when that Source projects at least one mutation action. Read-only/public Sources remain discoverable through their name link, without a settings affordance. Do not infer management from visibility or creator identity; scoped and global actionable Sources retain their shortcut.

Persistent local database follow-up: the user approved moving this workstation from the API-owned disposable PostgreSQL Dev Service to installed loopback PostgreSQL. Preserve the live dev database in a protected backup before cutover, restore into a separate `memoryos_dev` database, and run API/worker as a non-superuser application role. An ignored `.memoryos-local.yaml`, imported after the managed Infisical cache by both development run configurations, owns workstation-only datasource settings and disables PostgreSQL Dev Services. Infisical synchronization must not overwrite it. Keep default Dev Services for developers without the local override; do not change staging credentials/databases, Flyway history, or unrelated local databases.

Source associations accept ordinary Groups only. Admin/Basic remain IAM system Groups and are not Source-sharing targets, including for global administrators. Remove the default Admin association on global creation; global Source administrators may create or save an empty ordinary-Group selection, while scoped managers still require at least one managed ordinary Group. V42 removes existing system-Group Source links and advances affected Tenant authorization revisions without changing Sources, ordinary links, memberships or capability grants; future system-Group associations are rejected. Admin obtains tenant-wide Source management through SYSTEM_ADMIN expansion, never through association membership. This does not introduce a document-content ACL bypass or change Source visibility. Legacy pending Google proposals containing system Group IDs fail current validation rather than silently rewriting their submitted intent.

Main refresh to 3e4e318: preserve upstream private Chat attachments/artifacts, account uiLanguage and vi/en presentation, 100 MiB binary admission, OCR improvements and documentation restructuring alongside the local Basic/scoped-manager/ordinary-Source-group contracts. Local work is backed up in stash a1ff98dad89ee1255314b638009ea8093e64986b. Main's published V37–V42 remain unchanged; remap only local feature migrations V37–V42 to V43–V48 and adjust their fixtures/current documentation. Existing local databases that ran the former V37–V42 layout must not be started against the merged layout or automatically repaired/reset. This merge changes code only, not runtime data.

PR publication is now authorized for the Basic/scoped-manager/Source-access implementation and its tests, translations, generated contracts and canonical documentation, targeting main. Workstation `.run` edits, ignored runtime YAML/credentials, and unrelated user document/output/tmp artifacts are excluded. This authorization permits committing the approved scope, pushing the feature branch and opening a PR; it does not authorize merging, deployment, live migration, or resetting/reconciling a divergent development database.

## Boundaries and verification

IAM enum/provisioning/persistence own grants. Retrieval application service owns the Search gate. API/OpenAPI and generated web types consume the same enum; no compatibility aliases. Regression coverage: migration/bootstrap, exact protected grant boundaries, enum-wide Admin implications, complete metadata, Basic access versus no-grant denial before IO, preserved document ACL, invitation/JIT identity projection, read-only system permission lists, ordinary grant editing and Groups-list search/navigation/rename. Run repository clean check, frontend check, browser tests/visual smoke. The PR #106 repair request authorizes integrating the omitted related local backend and pushing verified fixes; deployment and merge remain unauthorized.
