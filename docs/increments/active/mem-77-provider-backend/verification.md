# Backend verification — 2026-09-09

Implementation is on `feat/mem-77-provider-backend`; the checks below were run against the initial main base `287ca9c4bb79878bf49855934bddd4781f1c931a`. Publication and CI evidence are tracked on the pull request. This evidence does not close MEM-77, certify a local provider, or establish deployment of this change.

| Check | Observed result |
| --- | --- |
| `gradlew.bat clean check --no-daemon` | PASS, all 23 tasks across core/API/connector/worker; 5m47s. Existing optional live checks remain separate. |
| Final focused Chat/catalog + API/OpenAPI tests | PASS: core 35/35; API 21 passed, 1 optional live check skipped. Includes PostgreSQL constraints, access/defaults, secret encryption, configuration races, second adapter and native SDK HTTP request assertions. |
| Live OpenAI check with `MEMORYOS_CHAT_LIVE_TEST=true` | PASS, 1 test, zero skips. Credential read from Infisical dev only into the child process environment; normal send, native runner and persisted history were exercised. |
| OpenAPI generation and typed success-response assertions | PASS; generated Hey API client includes concrete catalog results rather than `unknown`. |
| `corepack pnpm@11.22.0 --dir web check` | PASS: 64 tests, lint/typecheck/generated stability and production build. Existing Chat chunk-size advisory remains. No UI feature was added. |
| JetBrains file inspection and project build | All changed Java/Kotlin DSL/configuration metadata files inspected with warnings included; IDE build successful with no build problems. Custom HTTP-header/internal-HTTP hints and framework/Mockito ownership conventions were reviewed. The unchanged application YAML has existing IDE classpath-resource resolution diagnostics; both shared resources load successfully in the real Gradle API/worker contexts. |
| Infisical configuration | Created `MEMORYOS_CHAT_CATALOG_ENCRYPTION_KEY` independently in dev/staging and verified readback. Values were never printed or written into repository files. Environment resolution uses the existing Spring property mechanism; preserve each key with its database backups. |

The first broad gate exposed excessive heap use from constructing a separate O200K tokenizer vocabulary for each native binding. The immutable vocabulary is now shared across configurations/revisions. The subsequent full gate passed. Test HTTP clients and native provider clients have explicit cleanup; factory leases are released on dispatch rejection and execution completion/cancellation/failure.

The HTTP provider/issuer tests use local fixtures and real application filters, database transactions and the native OpenAI SDK. The additional live model check proves the existing OpenAI model path, not real Keycloak login, arbitrary local model behavior, hosted web search, image generation or UI model selection.

Temporary scripts, process diagnostics and raw local test logs stayed under ignored `.tmp/`. No QA profile, test credential, provider fixture or operational bypass was added to production runtime. Production source includes the real OpenAI adapter only; the second adapter exists exclusively in the test source set.

## PR #88 review and main integration — 2026-09-10

The review fixes passed focused catalog/setup/configuration tests, API/OpenAPI integration, IDE build and `clean check` (23 tasks, 5m52s) before main advanced. The accepted internal HTTP policy is documented in the canonical catalog spec and reviewer instructions; it does not claim encrypted transport for HTTP.

Main `274bc783b7a7a3d7097eb85fcdcb2db52a800835` adds Google Drive migrations V21–V32. The unpublished catalog migration is now V33. OpenAPI/client were regenerated from the combined runtime. The combined API/OpenAPI tests passed (2m20s), web check passed (93 tests, lint/typecheck/generated stability/build), and IDE inspection/build passed with reviewed HTTP/header and unattached SQL datasource hints. The first combined `clean check` reached the 10-minute core-test timeout without an assertion failure; a separate core retry and final CI evidence are tracked on PR #88. Do not substitute the earlier base's full-gate result for this integration's CI.

## Phase 1 candidate baseline — 2026-09-11

Scope: the first remaining phase only, on `anhnd` at source base `4ecd916bb2f18b896a0224858abdc681fd9d7146` with pre-existing uncommitted work. This change adds the [candidate manifest](candidate-manifest.json) and [measurement protocol](design.md#phase-1-measurement-protocol), not serving activation, a tokenizer profile implementation, Models UI or target acceptance.

Verified manifest SHA-256: `247f25a35b2b4f799f8da8366abe4f1a41752024dd4287574ae4c5d43931eadf` (8,351 bytes). Keep this hash tied to the observations below; later envelope changes need another receipt.

### Asset and interpreter evidence

| Scenario | Observed result |
| --- | --- |
| Compare cached files to the [immutable upstream inventory](https://huggingface.co/api/models/HuggingFaceTB/SmolLM2-135M-Instruct/revision/12fd25f77366fa6b3b4b768ec3050bf629380bac?blobs=true) | PASS. Five metadata/tokenizer files were fetched from exact-revision `resolve` URLs, checked for exact size, SHA-256 against cached bytes and Git blob identity against upstream metadata. The streamed hash of the cached weights matches the upstream LFS SHA-256. No weights download or cache mutation. |
| Manifest-driven asset smoke in the pinned vLLM image | PASS. Python read the actual manifest bind mount, hashed all six listed files from the readonly `mem66-hf` volume, extracted and hashed the template, and checked the aggregate asset size. Six file checks plus template matched; total 271,170,888 bytes. The manifest hash inside the container matched the receipt above. |
| Exact template | PASS. The existing research template's 368 bytes match the pinned tokenizer configuration value exactly, including newline behavior. The candidate manifest specifies extraction from the upstream value rather than depending on an uncommitted research file in a future release. |
| Offline minimal tokenizer load | PASS. In the pinned image, `AutoTokenizer.from_pretrained(..., local_files_only=True, trust_remote_code=False)` loaded only the five allowlisted metadata/tokenizer files in a disposable tmpfs directory: `GPT2Tokenizer`, `is_fast=true`. English and Vietnamese sample encode/decode round-trips matched; the chat template produced token IDs. No `vocab.json`, `merges.txt`, weights or network were needed for this tokenizer smoke. This is not Java/native Chat framing or model generation evidence. |
| Interpreter identity and restrictions | PASS for these probes: UID/GID `1654:1654`, readonly root and model volume, no network, all capabilities dropped, no-new-privileges. Hash probes used 0.25 CPU/128 MiB; tokenizer loading used 0.5 CPU/768 MiB and 32 MiB tmpfs, equal memory-plus-swap limit and one OpenMP thread. No stderr warnings. These limits are probe limits, not a smaller serving envelope. |
| Pinned image inspection | Both exact digest references are locally present as Linux/amd64. Engine image size 1,779,899,934 bytes; gateway 21,015,643 bytes. Installed engine-image metadata reports vLLM `0.28.0+cpu`, torch `2.13.0+cpu`, transformers `5.15.1` and tokenizers `0.22.2`. Image sizes are Docker's image-size figures, not compressed download sizes or measured incremental disk consumption. |

Probes used the existing image's `/opt/venv/bin/python3 -B`, `hashlib.file_digest`, `os.statvfs` and package metadata; they did not import/start the vLLM serving engine. Commands ran with `docker run --rm --pull=never`; only the tokenizer probe had writable tmpfs. Throwaway source stayed in the tool process, not a permanent validator, test module or application profile.

### Candidate host observations and preflight

These are fresh workstation snapshots, not a sustained benchmark or staging measurements. Docker reports version `28.1.1`, Linux x86_64 and `docker-desktop`. The asset/CPU/filesystem probe timestamp was `2026-09-11T04:05:33.966977+00:00`.

| Observation / command | Result |
| --- | --- |
| PowerShell `Get-CimInstance Win32_Processor` / `Win32_OperatingSystem` | Intel Core i5-12500H, 12 cores/16 logical processors; Windows visible RAM 16,870,006,784 bytes, free RAM 4,993,605,632 bytes at the snapshot. |
| `docker info` and `wsl.exe -d docker-desktop -- free -b` | VM: 4 CPUs, 8,332,894,208 bytes RAM; guest available RAM 6,189,572,096 bytes. Guest swap is 2,147,483,648 bytes, with zero used. |
| Readonly image probe of `/proc/cpuinfo`, affinity and load averages | AVX2 and AVX-VNNI present; AVX-512F, AVX-512 BF16 and AMX BF16 absent. Affinity exposes CPUs 0–3; load averages approximately 0/0/0.016. Do not infer physical BF16 acceleration or future engine peaks. |
| `docker info --format` for cgroup/resource-control fields | cgroup v1 with `cgroupfs`; memory, memory-plus-swap, CFS quota/period and PIDs limits reported supported. Numeric limits still need acceptance under the actual serving workload. |
| `docker ps` / `docker stats --no-stream` | Only `schoolequipment-elasticsearch` was running: healthy, about 1.076 GiB charged memory and 1.11% CPU in the initial snapshot. It belongs to another project and was not stopped or modified. No MemoryOS application/Docling co-load exists in this Docker context. |
| `os.statvfs('/cache')` on the actual readonly model volume | Total 1,081,101,176,832 bytes, available 942,731,997,184 bytes. Direct WSL `df /var/lib/docker` could not resolve that daemon-namespace mount, so its failure was not used as disk evidence. |
| Physical Docker backing drive | The Docker data VHD is under `C:/Users/Gigabyte/AppData/Local/Docker/wsl/disk/docker_data.vhdx`. `Win32_LogicalDisk` reports C: free space 23,106,916,352 bytes; the repository's D: free space is not the Docker backing budget. |
| Manifest resource preflight against those snapshots | Guest memory and backing-disk checks PASS. Windows memory check **FAILS**: 4,993,605,632 available versus the conservative 5,469,372,416-byte engine + gateway + reserve floor. This is a no-go for starting the declared serving envelope at that snapshot, not proof that the workstation can never run the model. |
| Target access | No configured harness SSH hosts or OpenSSH config alias. Existing standard SSH identity and trusted-host checking were used for `ssh -o BatchMode=yes -o StrictHostKeyChecking=yes -o UpdateHostKeys=no -o ConnectTimeout=10 root@72.62.193.33 uname -s`; authentication was denied (`publickey,password`, exit 255). No target command ran. This is an access blocker, not a target resource or network-outage measurement. |

The numeric latency/startup/Stop/overload/co-load gates are predeclared in the manifest. None has been marked passed by these snapshots. The local interpreter/asset checks do not prove safe application co-location, gateway/Prometheus connectivity, full native prompt fit, non-root engine/nginx startup, generation admission or cancellation.

### Cleanup, documentation and remaining gates

- All probes were finite and used `--rm`; a subsequent `docker ps -a --filter name=mem77-manifest-probe` returned no container. The final running-container list still contained only the same healthy Elasticsearch workload. Model/cache mounts were readonly, temporary tokenizer files lived only in disposable tmpfs, and no secret was read, created or changed.
- Existing MEM-65/MEM-66 files, runtime configurations, hosted provider/defaults, database history and active services were preserved. No application source, generated client, Java/YAML runtime configuration or permanent test was changed. Gradle/IDE/browser checks were not rerun or claimed for this data/documentation phase; the proof is the actual image/asset/tokenizer smoke and measured preflight above.
- Architecture, capability specs and permanent verification matrices remain intentionally unchanged: managed serving and native-profile behavior are not implemented by this phase. Change-local facts stay in this increment; the roadmap links the updated status. No commit, push, PR, deploy or Linear mutation was performed.
- **Blocked:** authenticated access to an approved serving host and its named operational owner; measured target application/Docling headroom; actual private API-to-gateway and Prometheus-to-engine connectivity. A valid approved SSH user/key/alias must be configured outside chat. For local qualification instead, restore the declared memory headroom without this change terminating unrelated workloads.
- Phase 1 therefore has verified assets and a complete candidate protocol, but its safe-host/connectivity exit remains open. No larger model, new hardware purchase, secret bypass or temporary runtime mode was introduced to conceal those prerequisites.
- Final documentation check: 124 local links, including Markdown fragments, resolve across the changed design, plan, verification and roadmap. JSON parsing and the final manifest checksum also pass; no generated runtime or test artifact was retained.

## Implementation and controlled evidence — 2026-09-11

The receipts in this section predate integration of main `0310a24`. They preserve the exact earlier boundary, including the then-unshipped V34 tokenizer migration; the current migration is V37. They are not passing evidence for the newly merged tree. See [the latest-main continuation](#latest-main-integration-and-phase-1-recheck--2026-09-11) for current checks.

This continuation implements repository source after the Phase 1 checkpoint above; its statements that source/UI were absent describe that earlier checkpoint only. It does not publish or deploy a release. Existing MEM-65/MEM-66 work, hosted bootstrap/defaults, credentials, databases and unrelated services remain outside this change. The operational source manifest is now [infrastructure/inference/managed/manifest.json](../../../../infrastructure/inference/managed/manifest.json); the earlier candidate checksum is historical and must not be used as the current release identity.

### Implemented source

- Managed bounded pinned-file provisioning/atomic publication, readonly assets, private API/gateway/engine composition, environment-owned secrets/caches, one-slot/zero-queue gateway policy and separate readiness/generation probes.
- Reserved operation preflight, bounded maintenance/drain, image/manifest/operator receipts, explicit monitoring apply/check, guarded inference-key/BYOK rotation and compatible serving recovery. Rotation validates protected request/key/provider revision before changing credentials; drain timeout keeps admission blocked, and resume cannot bypass an incomplete rotation. The Flyway-history guard remains intact.
- Required installed tokenizer profile in catalog/DTOs, V34 legacy O200K backfill, API-owned CPU DJL assets, lease-aware shared tokenizer ownership and pinned text-only SmolLM2 framing before reservation, during history selection, on every converted native call and Validate. Native `.so` is installed readonly with API-only `libgcc`/`libstdc++`; `/tmp` remains noexec, licenses are shipped, and worker has no DJL assets or model weights.
- Raw HTTP cancellation before blocked-reader closure, with per-subscription state; bounded model-manager Persona list; Models-only provider/model/default administration with secret isolation, stale-result handling, coherent Access/revision snapshots and authorization cache purge. OpenAPI required primitive booleans reflect Jackson 3's pre-existing missing-field rejection, not unrelated new write strictness.

### Controlled local receipts

| Check | Observed result and exact boundary |
| --- | --- |
| Cold asset provision | **PASS:** actual pinned engine image used only as downloader/interpreter, no model loaded; completed in **37.70 s**, reporting `ASSETS_PUBLISHED_AND_VERIFIED`. This is download/publication timing, not cold model startup or generation. |
| Published asset verify-only | **PASS:** no network, readonly runtime/assets, UID **1654**, reporting `ASSETS_VERIFIED`. Not serving readiness. |
| Warm asset reuse and rejection boundaries | **PASS:** no-network/readonly warm provisioning returns `ASSETS_ALREADY_VERIFIED` in **5.93 s**, with no download or publication. Offline CLI rejects a same-size template byte change with `ASSET_CHECKSUM_MISMATCH` and a missing allowlisted asset with `ASSET_ALLOWLIST_MISMATCH`; restored assets verify successfully. These are asset checks, not warm model startup. |
| POSIX deployment operation regressions | **17/17 PASS** inside the pinned engine image without model import. Controlled operation tests, not a real deployed drain, rotation or rollback. |
| Managed probe fingerprint regressions | **3/3 PASS**. Establishes probe fingerprint behavior, not an attestation that real inference is running. |
| Production gateway launcher/configuration | **PASS in 1.63 s**, actual NGINX launcher at UID1654, readonly root, noexec `/tmp`, key0400, controlled upstream fixture and **no model loaded**. Unauthorized `/v1/models`401; authorized200; wrong method405; query404; `/metrics`404. An occupied generation slot rejects the second request429 without queueing. Maintenance rejects new generation503 while models remains200. An admitted slow-body upload remains busy under the actual production `/proc` TCP settlement predicate after maintenance; disconnect settles it, and reopening permits the fixture's204 response. This proves controlled gateway transport/settlement behavior, **not real vLLM drain/load or model generation**. |
| Unsafe key permissions | **PASS:** the production gateway launcher exits **1** with `KEY_OR_CONTROL_PERMISSIONS_INVALID` for a world-readable **0444** key, with otherwise valid control mount and writable private tmpfs. The smoke-owned key was restored to **0400**. |
| Native versus pinned HF tokenizer | **PASS:** all token IDs for eight corpora match pinned HF under Windows **Java 25.0.2** and the corrected Linux runtime **base Java 25.0.4**. Counts: **[4, 11, 34, 31, 50, 52, 1040, 10001]**. Each JVM also completed **1,000 shared/concurrent tokenizer encodes**. This is native/tokenizer and corrected runtime-base evidence, **not the final API candidate image** or provider prompt-usage/generation acceptance. |
| Vietnamese repeated sample correction | The exact twenty-repeat corpus measures **1,040**, not the earlier **1,041**. Do not carry the former raw-count or derived framing estimate forward as a current receipt; complete native/template qualification is distinct from raw-count parity. |
| `OpenAiCancellationTest` | **2/2 PASS**, real local sockets before headers and during active read, including independence of two subscriptions. No real vLLM work/slot-release claim. |
| Focused API group | Its earlier **30-test** run had one OpenAPI failure, subsequently repaired. The standalone contract and later complete repository gate below pass; the earlier failure is retained as chronology rather than counted as a passing run. |
| Models + identity component tests | **33/33 PASS**, including delete/refetch race and authorization-cache purge. Synthetic API/authority fixtures, not target IAM or model acceptance. |
| Actual Vite browser observation | Integration owner observed **1280×900** and **390×844** surfaces using synthetic API fixtures: no horizontal overflow; forms correctly show local **1024/128** profile. This is visual product-surface evidence, not normal-login target Chat. |
| Focused Models browser regression | **3/3 PASS in 12.0 s**, Chromium at **1280px/390px**: model manager reaches both navigation entries/credential editor; denied deep link emits no protected catalog fetches. No real API/model behind the UI fixtures. |
| Complete browser gate | **60/60 PASS in4.4min**, `pnpm --dir web test:e2e --workers=1 --reporter=list`, including Chat, identity/admin, Models, source workflows and Search. This suite uses the repository's fixture server/API routes; it is not protected target/model acceptance. |
| Complete backend repository gate | **PASS:** `gradlew.bat clean check --no-daemon --no-parallel --max-workers=1 --console=plain`, Windows Java **25.0.2**, **600.89 s**. JUnit: **532 total, 528 passed, 4 skipped, zero failures/errors**. API124 (one opt-in live Chat skipped), core329, connector53 (three opt-in real Docling cases skipped), worker26. An initial run exhausted the default test heap while retaining integration contexts and caused 24 Source API context-load failures. The API test cache is now bounded to two Spring contexts; the same 24 cases and the complete unchanged assertion set pass, without raising heap limits or changing application runtime behavior. |
| Frontend static/unit/build gates | **PASS:** generated API stability, pinned Playwright image policy, warning-denying lint, format check, TypeScript build, **110/110 unit tests in20 files**, generated-route stability and Vite production build/font-asset check. Unit runner used one worker and Node heap384MiB. Vite retains its advisory about a Chat chunk exceeding500kB; no threshold was raised to conceal it. |
| Monitoring syntax and rule transitions | **PASS:** pinned Prometheus3.14.0 `promtool check config` validates the configuration and **11 rules**; `promtool test rules inference-alerts.test.yaml` passes. Both run offline in bounded disposable containers. This does not prove live scrape/series/network behavior. |
| Staging Chat collection | **PASS:** `pnpm --dir web test:staging chat.spec.ts --list` discovers exactly one test while all six required environment values are empty. Protected target execution remains **UNRUN**. |
| IDE inspection availability | The configured JetBrains SSE service at `http://localhost:64342/sse` refuses connection (**WinError10061**); no LSP is configured. Wrapper, TypeScript/lint and configuration parsers are verified alternatives here, not an IDE/LSP-clean claim. |
| Workflow/shell/composition gate | **PASS:** actionlint1.7.12, checksum-verified ShellCheck0.11.0 with sourced deployment library followed, separate POSIX `bash -n` checks, managed release integrity/Compose checker, application+inference overlays and monitoring+inference overlays. The new CI Compose check initially failed because the deliberately blank example values were not supplied; interpolation-only values are now scoped to that check step, which never starts application services. |
| Final gateway source recheck | **PASS in2.00s**, repeating the same actual gateway transport/admission/slow-upload settlement smoke after explicitly escaping literal NGINX variables for ShellCheck and updating the launcher hash in the managed manifest. No lint warning was disabled and no model loaded. |

### Pending integration results

The final API candidate image identity/restricted offline native-load result is **BLOCKED / UNRUN**, separately from the corrected Linux base probe. The first normal Dockerfile build exceeded its1,800-second deadline downloading the unchanged pinned Infisical0.43.125 archive; no image was produced. The normal retry verified that archive and CLI version, then completed the Gradle9.7.0 distribution download at build-step elapsed2,163.8s. The supervised build subsequently ended without an exit code. Both image inspection and build-history inspection then failed because the `dockerDesktopLinuxEngine` named pipe no longer existed; `docker desktop status` could not retrieve a running Desktop instance. The cause of that engine loss was not established. No successful final image build/identity/native-load receipt exists, and Docker was not restarted automatically. Backend, complete frontend/browser and available static/configuration gates above remain passing receipts; none implies image publication or target/model acceptance.

The owning `web/tests/staging/chat.spec.ts` normal-login Chat smoke is **implemented, target execution UNRUN**. It reads six protected environment values only inside the test body, logs in through real Keycloak, verifies the expected manager identity, creates an owned session and reads its preconfigured Persona UUID selection without mutating defaults/grants. The current MEM-11 composer uses Auto to inherit that selection. Browser sends must return202 with the expected configured model UUID and no fallback; the smoke observes real SSE/visible reply and Unicode persisted-history reload. A second enumeration send must be observed RUNNING before actual UI Stop, then persist CANCELED and reload as Đã dừng. Early EOS or a completion-winning Stop race fails honestly without retry/pass waiver. Cleanup cancels/settles active replies only in the smoke-owned session and then requires204 from the current session-delete API; uncertain cleanup reports only its owned UUID/failure phase. Protected login, selected local profile/model and preconfigured manager Persona remain prerequisites.

### Unverified and blocked acceptance

- **BLOCKED model launch:** the last post-test, pre-engine-loss Windows `GlobalMemoryStatusEx` snapshot reports **2,324,688,896 bytes available** (about2.17GiB), below the declared **5,469,372,416-byte** host floor (5.09GiB). This supersedes the earlier596MiB snapshot; it is not a measurement after Docker became unavailable. Re-measure outer-host and guest capacity before any later launch. Guest headroom does not override the outer-host requirement. No model was launched and the floor was not weakened.
- **BLOCKED target:** the earlier staging SSH attempt was authentication-denied; no target access, approved deployment authorization, named target/operator decision or target application/Docling headroom receipt has been supplied. No rollout, publication, new grants or real inference startup was performed.
- **UNVERIFIED:** real vLLM startup under the declared serving envelope; cold/warm/offline model readiness and generation; API → actual managed model and current hosted regression; full native/provider-usage prompt fit; real upstream Stop/deadline slot release; load/concurrency/Chat-Validate collision/co-load and resource ceilings; real series/rules and monitoring connectivity; target normal-login Models/default/Chat; deployed rotation/new-key/old-key rejection and compatible rollback/recovery.

All A1–A20 gates in [the active plan](plan.md#acceptance-checklist) remain named. Source implementation and the controlled receipts above satisfy only their stated sub-boundaries; they do not close mixed implementation/live gates, Phase 8, deferred Access ownership reconciliation or MEM-77.

### Owned verification cleanup

Removed the smoke-owned asset/key volume `memoryos-mem77-managed-probe`, networks `memoryos-mem77-gateway-probe` and `memoryos-mem77-client-probe`, controlled gateway/upstream containers, and throwaway `memoryos-mem77-native-base` image before the engine became unavailable. Closed the browser surfaces and stopped the owned web fixtures. Removed `.tmp/mem77-main-runtime/` and `.tmp/mem77-main-native/`, including downloaded analyzer binaries, probe classes/JARs/native copies and temporary scripts. The final process roster contains no running owned process; the upstream fixture's exit137 followed explicit bounded stop, not an observed OOM. Shared images/build caches, unrelated workloads, MEM-65/MEM-66 work and reference checkouts were not pruned. No image was published and no target deployment was performed.

## Latest-main integration and Phase 1 recheck — 2026-09-11

The user requested fetching main, merging into `anhnd`, and finishing Phase1; subsequently selected the existing staging server as the target and chose to release workstation RAM personally. This authorizes repository integration and target qualification, not publication or rollout. No unrelated application or Gradle daemon was stopped.

- Fetched `origin/main` at `0310a24c03b162a34673b1e5c407aa6745bfc48e` and fast-forwarded `anhnd` from `4ecd916bb2f18b896a0224858abdc681fd9d7146`. All pre-refresh tracked/untracked work was captured in stash `410cbf4dad276b7fc264366394a42d3bc7e0b534`; it remains a safety copy during verification. Eighteen unrelated MEM-65/MEM-66/Keycloak/IDE files checked after restoration retain their pre-merge SHA-256.
- Semantic merge retains main's JPA catalog lifecycle, context revisions, retrieval/tool accounting, editor/regenerate/project/sharing behavior and MEM-77's required profile, native ownership/admission/cancellation and Models/default UI. The unshipped tokenizer migration moves to V37, leaving main V1–V36 intact.
- Main's `/api/chat/personas` assistant CRUD/list is distinct from the manager projection `/api/chat/model-personas` (`listChatModelPersonas`). The latter and model-default edits admit only nondeleted builtin/current actor-owned Personas; model writes also invalidate the Persona editor revision. Models mutations refresh the current workspace catalog caches, and the staging smoke cleans up via the now-available session-delete API.
- Docker is reachable again. Inspecting prior build `rh02d9vjuuq21qktot6t6rj2c` now returns **canceled**, `context canceled`; no final API-image identity/native-load receipt was produced. The restored engine does not turn that build into a pass.

### Current Phase 1 host and access observations

| Boundary | Observed result |
| --- | --- |
| Selected target | User selected existing staging `72.62.193.33`. Safe host fit and application/Docling/Prometheus connectivity remain unmeasured there; selection alone is not access or an operator-capacity receipt. |
| SSH | No configured harness hosts or OpenSSH alias; standard identity exists. Trusted-host, batch-only `root@72.62.193.33 uname -s` again exits255 with `Permission denied (publickey,password)`. No remote command ran. |
| Workstation physical memory | Initial recheck403,763,200 bytes; later635,674,624 bytes; before serialized contract generation1,591,656,448 bytes. Each is below the model's5,469,372,416-byte launch floor. No model/build-image workload was started; bounded repository verification is separate. |
| Docker guest and controls | Linux x86_64, four vCPUs, total memory8,332,890,112 bytes; measured available6,471,974,912 bytes and swap-used0. AVX2 is present; AVX-512/BF16 instruction flags absent. Docker reports cgroupv1, memory/swap, CPU CFS period/quota and PIDs limits supported. Guest headroom cannot override the Windows floor. |
| Actual Docker/cache disk | `df` resolves `/mnt/docker-desktop-disk` on `/dev/sdd`:941,975,617,536 bytes available. Physical backing is `C:/Users/Gigabyte/AppData/Local/Docker/wsl/disk/docker_data.vhdx`; C: available27,050,131,456 bytes, D:29,492,473,856 bytes at the Windows snapshot. Both exceed the17,179,869,184-byte provisioning floor. Sparse guest capacity is not physical capacity or a quota. |
| Competing workload | One unrelated running Elasticsearch container, approximately1.006GiB; IntelliJ, SchoolEquipment and two Gradle daemons also consume workstation RAM. None is an application/Docling co-load acceptance fixture and none was stopped. |
| Protected Chat environment | All six smoke variables absent from the current process environment. No login/model/credential validation was attempted using fabricated values. |

Phase1 remains **4/6** until target host fit and target operator/private connectivity are actually verified. Its pinned assets, provisional limits and measurement protocol remain intact; no capacity threshold was reduced to close a checkbox.

### Merged-tree verification

- Source merge and generated artifacts contain no remaining conflict markers. `anhnd` contains fetched `origin/main`; the index has no staged changes. The safety stash remains available rather than committing unrelated work.
- All36 main migration bodies match HEAD, with only the new V37 tokenizer migration added. Of159 pre-merge file fingerprints,106 retain exact bytes; the remaining53 paths are accounted for by integration edits or the explicit JDBC-repository removal/V34→V37 move. Stash normalization of the inference-dashboard line endings was reversed only after the reconstructed bytes matched the saved hash.
- Unified OpenAPI generation **PASS in38.93s** after repairing main's new assistant-model endpoint to use the same source-owned `AvailableChatModelResponse` as the catalog endpoint. Its raw core record had overwritten the shared schema's required fields; the contract assertion exposed that collision. Neither requiredness nor the assertion was weakened.
- Frontend **PASS:** pinned dependency install, SDK regeneration/stability, Playwright-image policy, warning-denying lint, format check, TypeScript, **123/123 unit tests in20 files**, production build/font validation and generated-route stability. Main's fixture model records were filled with the now-required nullable pricing, limits and capability fields rather than weakening generated types. The actual build emitted four external WOFF2 assets and no inline font URLs.
- Actual merged Models surface observed at **1280×900** and **390×844**, with only Models authority and synthetic API fixtures; both layouts render without horizontal overflow. This is browser UI evidence, not target IAM/model acceptance.
- Updated staging Chat smoke collection **PASS:** one test discovered with all six protected values empty. Target execution remains **UNRUN**.
- Managed release integrity/Compose checker **PASS**, with no model workload started.
- **Full merged backend gate: PASS.** Checked-in Java25 wrapper `clean check --no-daemon --no-parallel --max-workers=1 -Dorg.gradle.jvmargs=-Xmx384m`: `BUILD SUCCESSFUL in 12m33s`, 24 actionable tasks (20executed,4fromcache), including API/connector/core/worker test and check tasks. Existing JNA native-access/protobuf `Unsafe` warnings remain; no warning-free JVM claim.
- **Complete Chromium fixture gate: PASS.** `pnpm --dir web test:e2e --workers=1 --reporter=list`: **90passed,7.6m**, including latest-main workspace/editor/sharing/retrieval and Models scenarios. This is controlled browser acceptance, not the protected real-provider staging smoke.
- **Documentation references: PASS.** 249 relative links/anchors checked across the ten changed architecture/spec/matrix/increment/roadmap/runbook documents.
- **Cleanup:** the browser-suite process exited0 and its preview port4173 is no longer listening; the separately owned observation browser was released and stopped. All supervised verification processes are inactive. Docker lists only the unrelated `schoolequipment-elasticsearch`; isolated test containers are gone. No unrelated workload, database, credential or safety stash was removed.

## Phase 2 continuation — 2026-09-11

The user requested continuing the described managed private single-generation SmolLM2 implementation. No SSH permission, workload shutdown, publication or target rollout was performed. Model-free qualification used owned disposable resources; existing applications, credentials, review databases and MEM-65/MEM-66 resources remained untouched.

### Runtime and recovery changes

- **Interrupted provisioner reproduced and fixed.** Kill the actual CLI after it enters download, then immediately rerun it against the same owned asset root. Before the fix: interrupted process exit−9, retry exit1 in0.298s with `ASSET_IO_OR_FORMAT_ERROR`, because the exclusive-create lock survived. After the fix: interrupted process exit−9, retry exit0 in32.661s, `ASSETS_PUBLISHED_AND_VERIFIED`. Only provisioning receives download egress; no engine/model was imported. A persistent inode plus nonblocking POSIX `flock` releases ownership on process exit and does not delete abandoned/current/previous assets.
- **Application rollback network loss reproduced and fixed.** An isolated root deployment harness ran the actual `deploy-staging.sh` snapshot path with controlled accepted-runtime Docker IO, stopping before serving/application mutation. The real application Compose overlay resolved `memoryos-inference-client` instead of accepted `accepted-private-client` before the fix; it resolves `accepted-private-client` afterward. Actual observed image IDs still own image recovery; the accepted application environment owns its private-network binding. The harness was disposable rather than adding a test-only deployment mode or root-filesystem mutation to the permanent suite.
- **Monitoring rollback network loss reproduced and fixed.** Run the real monitoring command in an isolated fixture and resolve the actual observability Compose files with its resulting environment. Before the fix, previous serving's `accepted-private-backend` was ignored and Prometheus retained `candidate-private-backend`. After the fix, it resolves `accepted-private-backend`. Selected serving JSON now owns the command's backend identity; the two obsolete candidate-only environment appends were removed. This proves configuration selection, not real scrape connectivity.
- **Serving-only network mismatch reproduced and fixed.** The focused regression first failed because incompatible previous networks were accepted and retained configuration was changed. Recovery now rejects client/backend network drift before artifact preparation/drain/start; unchanged API and monitoring attachments are not silently stranded. The regression passes.
- Updated managed manifest SHA-256: `65fe44d94ed35bb2f2a4472b04eb497af6aeb907f7e51c40881e12f69cba6752`, including the revised provisioner hash. This identifies source, not an accepted running release.

### Executed checks

| Check | Observed result and boundary |
| --- | --- |
| Pinned CPU image CLI | **PASS**, `vllm serve --help=all`,23.99s, UID1654, readonly root, networknone, one CPU and1GiB memory/swap cap. CLI import/help only; no model assets mounted or model started. |
| Real interrupted-download recovery | **PASS**,32.661s retry, all pinned assets published and verified. Cold provisioning timing is not cold engine startup. |
| Published asset verification | **PASS**, networknone/readonly/UID1654: `ASSETS_VERIFIED`. |
| Warm operator reuse | **PASS**, networknone/readonly asset volume: `ASSETS_ALREADY_VERIFIED`, no download. |
| Controlled deployment regressions | **18/18 PASS**, final1.143s after the monitoring correction:12 drain/resume/network-recovery cases plus6 credential/catalog-HTTP cases. Not real preflight, rotation or serving recovery. |
| Controlled managed regressions | **5/5 PASS**,0.059s:3 fingerprint cases plus2 POSIX process-interruption/concurrent-publication cases. CI's existing discovery command includes the new file. |
| Release and shell checks | **PASS**, managed artifact/Compose checker, both Bash syntax checks, canonical CI `shellcheck -x deploy-staging.sh` and gateway entry point using0.11.0. No application/frontend source changed in this continuation; earlier merged `clean check`/123unit/90browser receipts were not rerun or relabeled as new runs. |

An initial128MiB ShellCheck container exited137 without retained OOM evidence. The published binary rejects optional threaded RTS flags; the canonical CI invocations subsequently passed with an unchanged source and a512MiB tool-only cap. An extra standalone analysis of the sourced library reported SC2016 on intentional remote AWK variables; no shell suppression or runtime code change was added for that advisory.

### Remaining real-runtime gates

- Workstation physical RAM:3,854,811,136 available bytes at10:30Z and2,879,692,800 at10:48Z, each below the unchanged5,469,372,416-byte model-start floor. Docker guest available memory was6,353,043,456 bytes, swapused0; guest memory is not a substitute for physical-host headroom. Physical disk free was26,035,494,912 bytes onC: and29,487,693,824 onD:, above the16GiB provisioning floor.
- No real vLLM launch/generation, cold/warm/offline engine restart, concurrent Chat/Validate, Stop/deadline release, co-load, secret rotation or deployed rollback was performed. Target execution/access and protected application/model credentials remain absent; SSH itself is not a feature acceptance requirement.
- Monitoring source has the private Prometheus-only attachment and bounded native scrape. Its existing automated apply receipt checks scrape availability, three gauges and rule health; it does **not** establish all histogram/counter names/labels/units or Grafana datasource/dashboard usability. Those remain explicit live/operator acceptance checks, not closed by fixture PromQL or generic Grafana health.
- Phase2's mixed implementation/live checklist and final target acceptance remain open. These fixes and controlled receipts do not authorize marking MEM-77 or Phase1 complete.

### Continuation cleanup

251 relative documentation links/anchors pass. Removed the six owned throwaway qualification files and their directory, plus the uniquely labeled asset volume after confirming no container referenced it. All Phase2 containers have exited and been removed; Docker lists only the unrelated `schoolequipment-elasticsearch` as running. Shared images/build caches, unrelated applications/resources and the existing safety stash were retained. No model service, public port, external credential change, commit, push or deployment remains from this continuation.

## Authorized branch publication — 2026-09-11

The user authorized committing and pushing the integrated MEM-77 work to `anhnd`. The publication scope includes managed serving/operations, native profiles/catalog contracts, Models consumers and their verification—not just the final recovery fixes. MEM-65 theme/identity changes and the MEM-66 research stack remain outside the commit; shared workflow/architecture/roadmap files are staged selectively without changing their working-tree content. The managed runtime's required inference dashboard and technical metrics configuration are included.

The selected index was materialized independently of the dirty workspace. Its managed release/Compose checker and serving-checksum generation pass; its POSIX deployment18 and managed5 tests pass, as do both deployment Bash syntax checks. The staged documentation check exposed an obsolete adapter-handoff anchor, corrected to the implemented profile contract. Private-key/token-pattern screening covers the selected changed files, not a comprehensive historical secret audit.

Branch publication is not an approved main release, target deployment or a real-engine acceptance receipt. To qualify Phase2, use the [managed operations runbook](../../../runbooks/ci-cd.md#managed-inference-operations), [candidate command/environment contract](../../../../infrastructure/deployment/inference.env.example) and the still-open [Phase2 checklist](plan.md#phase-2--implement-the-managed-inference-deployment). Preserve the resource floor and distinguish candidate runtime evidence from the final authorized target/Chat acceptance.

## Local application qualification — 2026-09-12

The user requested running the local model after observing `Saved connection reached the model` for the existing hosted GPT-5-mini. That screenshot is hosted validation evidence, not managed SmolLM2 acceptance. The local developer continuation keeps the normal API/UI integration and hosted catalog/default; it does not authorize staging deployment or replacing the accepted runtime/resource baseline.

### Source and executed preparation

- Added the explicit loopback-only developer Compose overlay: authenticated gateway127.0.0.1:18081, no engine/readiness publication, unchanged managed images/resources/networks. `check-inference-release.py --compose` passes both normal and local compositions. An isolated actual-Compose experiment accepts the correct overlay and rejects four mutations: public0.0.0.0 gateway, published engine, raised engine memory and serving-network egress. The throwaway copy was removed. This is configuration validation, not a live gateway/listener test.
- Docker Desktop was initially stopped. Its supervised `docker desktop start` completed with exit0, followed by successful `docker info`: Linux/x86_64,4CPUs,8,332,890,112 bytes guest memory. No unrelated workload was stopped or reconfigured.
- Created the independently labeled `memoryos-inference-local-data` volume with operator-owned assets/control, UID/GID1654 cache0700 and a newly generated inference key0400. Existing keys/state are checked rather than overwritten. A real readonly/networknone/UID1654 Python64MiB probe verified that the Docker-reported POSIX key/cache paths bind correctly through Docker Desktop. The key value was not printed, passed as an argument or stored in environment metadata.
- Stored only Compose project/path/network references in `%LOCALAPPDATA%\MemoryOS\inference\inference.env`, protected by a current-user-only Windows ACL. The project and networks are separate from MEM-66 and production.
- The first provisioning invocation failed before launch because the shell-mediated Windows bind path was not found; it did not start a model. The corrected forward-slash path ran the real managed provisioner in a128MiB/one-CPU tool container. In45.14s it returned `ASSETS_PUBLISHED_AND_VERIFIED` for revision`12fd25f77366fa6b3b4b768ec3050bf629380bac`. A separate64MiB, readonly, networknone, UID1654 run returned `ASSETS_VERIFIED` in1.34s.
- The managed manifest remains`65fe44d94ed35bb2f2a4472b04eb497af6aeb907f7e51c40881e12f69cba6752`. Model/context/output/resource limits and pinned engine/gateway images are unchanged.

### Capacity and unrun boundaries

Windows available physical memory was3,961,184,256 bytes before Docker startup, then887,324,672 and298,262,528 during bounded preparation. The01:51:17Z final snapshot is415,870,976 bytes versus5,469,372,416 required before model launch. Docker guest `MemAvailable` is6,487,818,240 bytes, with zero swap used; guest headroom does not waive the outer-host floor. AVX2 is present. Physical free disk is26,217,963,520 bytes onC: and29,484,756,992 onD:.

At01:49Z no process was listening on the development frontend8080 or API18080; local gateway18081 was also unused. No gateway/engine workload was started, no local provider/model was added, and no real generation, Validate, Chat, Stop or browser test was run. Restore sufficient host headroom **with Docker and the normal application running** before proceeding. Do not replace these missing runtime results with the configuration/provisioning receipts above.

The successful provisioning/verification tool containers used`--rm`. Retain the owned persistent state and protected environment file for continuation, without claiming a running service. Existing Docker images, research state, unrelated containers and working-tree changes remain intact. The local overlay and documentation changes are not newly committed or pushed by this continuation.

## Terminal bootstrap correction — 2026-09-12

The user's09:13 log reproduces the documented terminal command selecting Spring `default` and failing datasource initialization with `'url' must start with "jdbc"`. The application JVM is Java25.0.2 despite the shell prompt showing Java21; JNA/native-access, console-font and CLI-update warnings are not that startup failure.

The missing input is `ARCONIA_BOOTSTRAP_MODE=dev`, already present in the checked-in IntelliJ configurations. Infisical's `--env=dev` selects secrets, not Arconia bootstrap. Corrected every development `bootRun` example in the runtime runbook, including worker and debug launch variants; no datasource fallback, runtime profile, Java source change, credential replacement or health suppression was added.

Executed the normal Infisical development API command with the corrected environment under supervision. The API logged exactly one active profile, `development`, provisioned its PostgreSQL Dev Service, applied all37 migrations and reported `Started MemoryOsApiApplication in15.299 seconds`. The reported JDBC startup failure no longer occurs. No worker or model workload was launched.

HTTP verification distinguishes startup from complete environment health: liveness and readiness return `UP`, but aggregate `/actuator/health` returns HTTP503/`DOWN`. The configured object-storage service's public liveness route returns200. A read-only, canonically signed HEAD of the configured readiness object, using the same managed development credentials without printing their values, returns403/`InvalidAccessKeyId`. That credential is not recognized by the configured MinIO endpoint. Valid development object-storage credentials remain an external prerequisite; no remote bucket, key, policy or shared Infisical value was changed.

The owned API smoke was interrupted and stopped after verification to release port18080 and avoid competing with the user's terminal launch. Its supervisor exit1 follows intentional cancellation of the foreground `bootRun`; it does not replace the successful startup receipt or waive the separate health failure. The existing model assets/local environment, unrelated services and working-tree changes are preserved. These documentation corrections are not newly committed or pushed.

## Frontend terminal startup correction — 2026-09-12

The user's normal `corepack pnpm dev` command failed before Vite: pnpm11.22.0 attempted to fetch `https://registry.npmjs.org/--package` and returned404. The same invocation reported Node22.23.2 even though the terminal prompt showed Node26.7.0. These are separate command-parsing and runtime-selection problems, not missing registry authorization.

Moved both pinned `--package=...` options before `dlx` in `web/package.json`, preserving `@hey-api/openapi-ts@0.99.0` and its TypeScript6.0.3 runtime. On this workstation, `corepack.cmd` resolves into Hermes and explicitly chooses its adjacent `node.exe`; the shell's normal Node resolves to `C:\Program Files\nodejs\node.exe`. Executing the installed Corepack JavaScript entry point with that Node selects26.7.0 without altering Hermes, installing another global toolchain or changing the frontend's `>=24` requirement. The development runbook now documents this Windows distinction and launch command.

Executed the normal corrected `pnpm dev` path under supervision with the supported Node runtime. The pinned generator completed in327ms; all17 generated client files remain byte-identical to their pre-run content. Vite8.2.1 reported ready in1260ms at `http://127.0.0.1:8080/`, without the404 or unsupported-engine warning. A real browser displayed the MemoryOS sign-in card and company-account link; the Vite-proxied `/actuator/health/readiness` returned HTTP200/`UP` from the user's running API. No API responses were mocked.

Closed the owned verification tab and retained the requested frontend as supervised service `mem77-frontend-launch` for the user to open. The user's API, unrelated processes and credentials were untouched. No inference service or authenticated Chat was exercised; the separate aggregate-health/managed-model prerequisites remain open. This correction is uncommitted and is not a new branch publication.

At09:48 the user launched the corrected command in their own terminal. Generation succeeded, but Vite rejected port8080 because the retained supervised frontend already occupied it. Stopped only the owned `mem77-frontend-launch` service to hand control to the user's terminal. An initial connection probe returned Windows10035 and was inconclusive; a subsequent exclusive bind to `127.0.0.1:8080` succeeded, and its probe socket was immediately closed. The port is available for the user's normal launch; no port change, API restart or unrelated process termination was performed.

After the user confirmed their frontend launch succeeded, rechecked capacity with the normal API/UI running. API readiness through Vite returns HTTP200/`UP`; aggregate API health remains HTTP503/`DOWN`. Windows available memory is3,989,123,072 bytes (3.72GiB), below the unchanged5,469,372,416-byte floor by approximately1.38GiB. Docker guest available memory is7,361,200,128 bytes, with zero swap used, and passes its own floor. Docker lists only the API's PostgreSQL Dev Service and its Testcontainers resource reaper. No inference container was launched, no unrelated workload was stopped, and no catalog/default was changed. The current blocker is outer-host headroom with the application retained, not the historical absent API/UI listeners.

On the user's request to identify further memory savings, process-tree inspection found the owned headless Chrome verification browser still running after its tab was released (approximately 385 MiB working set). Stopped only the verified `omp.browser.headless` supervised tree; the browser close helper's ineffective `kill=True` with zero tabs was reported as a tooling issue. Afterwards Windows available memory measured 5,196,075,008 bytes (4.84 GiB), still 261 MiB short of the pre-start floor, while readiness through Vite remained HTTP200/`UP`. The Java processes are the active API/Gradle launch chain, not spare daemons. No user application, API/frontend process, Docker service or system process was terminated; further user-selected application closure remains necessary.

## Native engine and Windows gateway qualification — 2026-09-12

After the user confirmed sufficient host RAM, started the normal local Compose project with the existing pinned images, assets, secret and limits. The initial startup failed after loading weights: Docker mounted the engine `/tmp` tmpfs with implicit `noexec`, and the worker could not load a generated `.so` (`failed to map segment from shared object`). The container was not OOM-killed. This was an actual runtime failure, not slow downloading or an unverified memory diagnosis.

### Native loading correction

Explicitly enabled `exec` only on the engine's existing256MiB, UID/GID1654, mode0700, nosuid/nodev scratch. Gateway scratch remains noexec; readonly roots, dropped capabilities, CPU/memory limits, model revision and launcher bytes are unchanged. A disposable native `ctypes` smoke copying/loading the image's real `libm` under the exact Compose restrictions fails before the correction and passes afterwards at UID/GID1654. The release checker now rejects missing engine exec and executable gateway scratch.

The recreated engine became healthy. The old gateway health state briefly caused the Compose wait command to return failure before its next health check; subsequent actual readiness confirmed both containers healthy without another engine restart. Authenticated managed `ready` and `generation` probes pass. The first real SSE has first nonempty content at6.7577s and completes at6.8566s with3 output tokens and finish reason`stop`; the native idle probe then observes8 quiet seconds.

### Approved host-ingress correction

The original local port-only overlay did not provide Windows connectivity: the gateway's requested `HostConfig.PortBindings` existed but its actual `NetworkSettings.Ports` was empty and no Windows18081 listener existed. Both attached networks were internal. The user explicitly selected adding an ordinary bridge only to the local gateway, accepting that gateway's outbound route instead of moving the API into Docker.

Added the project-scoped `memoryos-inference-host` bridge only in the local overlay and recreated only the gateway; the engine stayed running. The gateway's health-wait command exits0. Its actual published binding is exactly127.0.0.1:18081→8080; engine ports remain empty and its only network remains the internal backend without a default gateway. The base composition's networks remain internal and unpublished. This exception does not authorize staging/production networking changes.

From the Windows host, authenticated `/v1/models` returns200 with the exact served model and1024 context; the same request without a key returns401. Real Windows-to-gateway SSE for `Say hello in one short sentence.` returns **`Hello!`** in0.61s, with37 prompt tokens,3 completion tokens and40 total tokens. The key was read into temporary process memory, never printed or passed in arguments/environment, and its captured buffer was cleared. The serving manifest remains`65fe44d94ed35bb2f2a4472b04eb497af6aeb907f7e51c40881e12f69cba6752`; Compose source changes are covered separately by the release integrity boundary.

### Scope, resource observations and cleanup

- The complete release/Compose guard passes. A disposable real-Compose fixture accepts the approved configuration and rejects eight mutations: public host binding, engine host-bridge attachment, internal-only host bridge, base-network egress, engine publication, changed engine memory, missing engine exec and executable gateway scratch. Fixture directories and transient native-probe containers were removed; no permanent fixture-only runtime was added.
- Current engine/gateway report healthy, zero restart counts and no OOM kill. Cgroupv1 engine memory is3,503,702,016 bytes with high-water3,625,279,488 bytes; `under_oom` and `oom_kill` are0, and memory-plus-swap equals current memory. These are current post-fix container receipts, not a claim that the failed initial startup never restarted.
- Windows available-memory samples during startup/generation/working ingress were3,197,280,256;1,237,303,296; and1,159,208,960 bytes, respectively, above the1,073,741,824-byte runtime floor but with little remaining headroom. Readiness through the normal frontend/API still returns200/`UP`. No unrelated application or user API/frontend process was stopped. These samples and two generations do not establish full cold/warm repeat, p95, sustained co-load or target capacity acceptance.
- The browser relay could not attach to the user's existing app session by either exact local URL or MemoryOS title; unauthenticated catalog access correctly returns401. No authenticated provider/model write, Validate, Chat persistence or UI Stop has been performed. The user's normal logged-in Models UI remains the next boundary; no SQL bootstrap, auth bypass or Tenant-default change was made. Aggregate dependency-health/MinIO remediation remains separate.
- Retain the requested `memoryos-inference-local` engine/gateway, persistent assets/cache/key volume, protected environment file and project-scoped networks. No new browser was launched. The startup fixes and approved local-network change remain uncommitted; no main/staging/production publication is claimed.

### User-confirmed Models and basic Chat

The user subsequently supplied the saved-provider screen: OpenAI adapter, local127.0.0.1:18081/v1 endpoint, enabled provider and configured credential. They then confirmed adding the model and using Chat; the next screenshot shows `SmolLM2 135M Local` selected and two user/assistant exchanges, including a Vietnamese user message. This is user-observed normal application integration evidence, superseding the earlier provider/model/Chat-unrun status. The reported successful flow was accepted without rerunning it merely for confirmation.

The screenshot shows completed responses, not event timing, selected UUID/no-fallback transport identity, retained history after reload, use of earlier context, upstream cancellation or subsequent slot availability. It does not supply a separate saved-model Validate result, production native-image evidence, sustained capacity measurements or target acceptance. Response factuality/language quality remains a declared small-model limitation, not proof of correct knowledge or a requirement to replace the baseline.

## Authenticated candidate acceptance and native Stop fix — 2026-09-12

### Identity and product paths

The user signed into an owned temporary browser through normal identity. No synthetic actor, SQL catalog bootstrap, new authority or provider Access promotion was used. Local provider `d004f9c4-e8bf-4288-915d-cc4402d15263` remains manager-only at `http://127.0.0.1:18081/v1`; model configuration `087ecd43-00a2-4afe-95d9-506d94b03686` serves `HuggingFaceTB/SmolLM2-135M-Instruct` with `smollm2-135m-12fd25f-v1`, context1024/output128 and unknown pricing. The serving-manifest SHA-256 remains `65fe44d94ed35bb2f2a4472b04eb497af6aeb907f7e51c40881e12f69cba6752`.

Saved-state Validate through Models returned200/`reachable:true`. An owned temporary Persona selected the local UUID. Ordinary Chat Auto returned202 with that exact configured UUID and no fallback; its real SSE returned200/`text/event-stream`. The first observed answer had73 distinct increasing visible lengths, first visible content at4,212.5ms and114 persisted characters. A follow-up completed with14 visible updates; UTF-8 questions and four selected-history rows survived reload unchanged. An oversized Unicode question returned400 without changing the selected tree/history.

The follow-up answered “John” instead of the supplied “Linh”. This is recorded as weak small-model output, not a language/reasoning-quality pass or a reason to replace the accepted baseline. Stored question/history integrity and completion do not establish answer factuality or every full-request boundary gate.

### Native cancellation defect and correction

The pre-fix first-content Stop returned202 and committed `CANCELED`, but an independent native observer showed generation continuing to the128-token length limit. Direct Windows loopback cancellation stopped early. The defect was below the Chat terminal transition: the SDK future had already completed at response headers, while closing its chunked keep-alive response body could drain rather than abort the HTTP call.

`OpenAiCancellation` now supplies an SDK `HttpClient` transport whose response retains the actual OkHttp call. Subscription cleanup cancels the call before closing the response; shared-client and other-subscription ownership remain separate. The existing post-content socket regression now sends real chunked keep-alive SSE and holds the connection open. It failed pre-fix with the expected socket timeout and passed after the correction. Both cancellation cases and the focused adapter, request-policy and provider-configuration classes pass.

A disposable native SDK smoke against the real gateway disposed after first content in2ms; native generation stopped at10 tokens and stayed idle for8 seconds. After the API restart below, the normal browser Send/Stop path returned202 for the exact local UUID with no fallback, observed real SSE, and clicked Stop with3 characters visible. The database committed `CANCELED` with16 retained characters. Native metrics advanced from baseline3174 to3180 tokens, changed running1→0 with waiting0, and then stayed quiet for8.087s. Reload retained the exact canceled text; the next UI turn completed on the same local model with five visible updates.

vLLM0.28 frontend cancellation does not necessarily increment its success/abort counter. Early token cessation and quiet running/waiting gauges—not that counter or a merely canceled transcript—are the native Stop evidence. The Windows headed verification window became occluded and could not reliably receive input/render streaming; its existing authorized session was copied only in process memory to an unoccluded automated browser. No cookie value was printed, and the copied session was removed during cleanup.

### Authorized data-preserving API restart

The user explicitly chose to preserve local data and restart the API. Before stopping the verified workspace API, checked managed dev-secret access, created a private-ACL PostgreSQL custom dump, rehearsed a transactional restore into an owned temporary database and retained count/content-hash receipts for all62 public tables. The dump SHA-256 is `252e65d8f9561a1d0c6ffd736b77cec9f6ac30f1c99f70f87ead2b9713d76d3d`.

Started the updated API using normal Infisical development `:api:bootRun` on18080, without a new application profile or standalone database mode. While browser activity was paused, restored the snapshot transactionally into its replacement PostgreSQL Dev Service; all62 table counts and complete row hashes matched. Readiness/liveness returned `UP`, the normal browser session remained authorized, and saved catalog, Persona and conversation identities were preserved.

The replacement API is retained as detached supervised service `mem77-updated-api`; the user's frontend remains untouched. PostgreSQL retains its ordinary Dev Service lifetime, so stopping this API still removes that development database. The separate aggregate-health MinIO credential failure remains unchanged; this restart does not certify object storage or change managed secrets.

### Selection precedence and inference-outage isolation

- Manager-only local configuration is excluded from Tenant defaults in the actual UI. A direct authorized attempt to set it as the Tenant default returned400 and left the existing selection/revision unchanged.
- An owned Persona default plus Auto selected the local UUID with no fallback. Inherit omitted the model ID, selected hosted `gpt-5-mini` (`aa6feb0f-95f8-4154-bcc7-1439bebdc44c`) with no fallback, and completed. Explicit selection through the real Chat picker then selected and completed the local model while the Persona inherited hosted.
- Temporarily disabled only the local provider using revision-checked `KEEP`, preserving endpoint, credential and Access. Auto fell back to the authorized hosted UUID with `SELECTION_UNAVAILABLE`, actual SSE and `COMPLETED`. Re-enabled the provider in `finally`; its revision advanced1→3, while manager-only Access and credential presence remained unchanged.
- With only the local gateway stopped, saved local Validate returned200/`reachable:false`/`PROVIDER_UNAVAILABLE`. Authenticated provider administration remained200, API readiness remained `UP`, and an explicit real hosted request completed. Local outage did not prevent hosted execution.
- Existing Tenant default stayed hosted at revision2; builtin Persona stayed Inherit at revision1. No ordinary non-manager browser actor was available, so that distinct real-actor fallback scenario remains unverified; manager fallback caused by a disabled selection is not mislabeled as that scenario.

### Native workload and remaining recovery boundary

A disposable native workload exercised the real authenticated gateway/model: wrong key401; unknown model404; rejected query/method variants; `n>1` rejection; oversized body413; context overflow400; and maintenance generation503 while model listing remained200. Omitted and raised `max_tokens`/`max_completion_tokens` remained capped at128.

Thirty serial English/Unicode generations had native first-content p95=0.9278s, completion p95=3.0363s and maximum completion3.0393s. Ten three-way bursts each produced one200 and two429 responses; authenticated model-list requests while busy took approximately0.002–0.022s. The19-check native receipt passed and the engine returned idle. These are native candidate measurements, not browser latency, a full application/Docling co-load benchmark or target-capacity acceptance.

After the outage check, stopped the idle engine for an offline warm-cache restart. Docker guest available memory was7,242,027,008 bytes, but Windows available memory was2,063,249,408 bytes. Stopped both owned verification browsers; after all gates and cleanup, the final Windows sample was3,960,545,280 bytes, still1,508,827,136 bytes below the unchanged5,469,372,416-byte pre-start floor. The post-gate Docker guest sample was7,255,392,256 bytes. Engine and gateway remain intentionally stopped, both exit0, with assets/cache/key/environment preserved. The user chose to free additional RAM, but the measured host prerequisite remains unmet; no unrelated application was stopped and no threshold was lowered.

### Verification and cleanup boundary

Deleted exactly five owned test conversations and their temporary Persona through normal authorized endpoints. The user's `hello` conversation `5206c77b-b6b5-400d-bcfb-6c6fd5808944`, saved local provider/model, Tenant default and builtin selection remain intact. Removed disposable native monitor/Java smoke sources, both owned browser process trees, the private browser profile and copied session cookie. After all gates, rechecked the preserved user conversation and removed the sensitive temporary database backup. The data-bearing API and user's frontend remain running; final fresh readiness returns200/`UP`.

The ordinary final `clean check` attempt stopped at Windows' lock on the running API's connector JAR, before tests. Restored the API's classes/resources without rewriting its locked dependency JARs; a fresh live readiness request remained200/`UP`. Then ran the full wrapper `clean check` with a disposable init script redirecting each project's `layout.buildDirectory`, `--no-configuration-cache --no-daemon --max-workers=1 -Dorg.gradle.jvmargs=-Xmx512m`. It passed in1m39s without another API stop or data restore.

Gradle reports contain585 tests, zero failures and six explicit skips: API135/3 skipped, connector53/3 skipped, core371/0 and worker26/0. The API suite executed; the unchanged connector/core/worker test results came from Gradle's cache. The skipped API cases are optional live-provider, real grounding and real-corpus measurements; the three skipped connector cases need a live Docling endpoint. They are not counted as newly executed live coverage. Native-access/deprecation JVM warnings remain; no JetBrains/LSP inspection capability is configured, so no IDE-clean claim is made.

Using Node26.7.0 with the installed Corepack JavaScript entry point, `pnpm --dir web check` passed: generated API and routes stable, CI image contract, lint, formatting, types,123 unit tests, production build and four external WOFF2 assets. `pnpm --dir web test:e2e --workers=1` passed all90 Chromium fixture scenarios in6.2m; its isolated4173 server released its port while the user's18080/8080 listeners remained. These browser fixtures are separate from the real provider/identity smoke above. `check-inference-release.py --compose` passed without launching a model. The disposable Gradle init script/build tree were removed after retaining the receipts. Warm restart remains resource-blocked, not passed.

No commit, push, main merge, staging deployment, API-image/native-load acceptance, credential rotation or target rollout is claimed by this continuation. The remaining broader Phase7/target matrix is not waived by these local candidate results.

## Authorized publication and main refresh — 2026-09-12

### Scope and integration

The user authorized two ordered `anhnd` publications followed by a Linear MCP comment, not a PR, main push, deployment or issue closure. First publication is `b26555a34ebf2564f56ab2b6f5b25d0f30aefc02` (`fix(chat): abort native generation on Stop and qualify local inference`), pushed successfully before fetching main `24db3885ab84663560d1d1b11462fb31403e693e`.

The merge was prepared in an isolated detached worktree so the data-bearing development API and unrelated working files were not used as the integration/build surface. Sixteen files required conflict resolution. Native composition retains the immutable `ChatRequestPolicy`, installed profiles and fixed per-subscription transport alongside main's attachments, vision, private-file evidence and automatic titles. The canonical binding includes both policy and vision; callers were migrated without a legacy estimator shim. Media admission retains the existing conservative 4,096-token allowance, not measured provider usage; text-only SmolLM2 does not gain vision or tool capability.

Main migrations V1–V40 were compared against the fetched commit and are unchanged. The branch-only tokenizer backfill becomes V41 with identical SQL. The existing catalog preservation regression now exercises V40 → V41 and retains configuration identities/revisions, selections, associations and history. The retained local candidate database carrying tokenizer V37 was not restarted, reset or repaired against this layout.

CI/CD keeps main's independent landing publication, browser sharding, Sentry inputs and separation of deployment from business acceptance, plus MEM-77's managed serving/release reservation and recovery guards. Finalization still checks runtime identity/readiness and ownership; it does not certify business acceptance. Source-text-pinning deployment tests were replaced with isolated behavioral transaction checks. OpenAPI was regenerated from the integrated Spring source, then the browser client was regenerated; generated conflict sides were not accepted as the final contract.

### Integrated verification

- Normal checked-in wrapper `clean check --no-daemon --max-workers=1 -Dorg.gradle.jvmargs=-Xmx512m`: **PASS**, final run10m4s. Reports contain678 tests,670 passed,8 explicitly skipped,0 failures: API141/4 skipped, connector104/4, core406/0, worker27/0. API/connector executed in the first attempt and were reused from cache in the successful final run; core/worker executed in the final run. The first attempt exposed the catalog test's old repository signature and migration targets; those were migrated before the successful gate.
- Source-owned OpenAPI generation: **PASS**. `pnpm --dir web check`: **PASS**, generated API/routes stable, lint/format/types,153 unit tests, production bundle and four external WOFF2 assets.
- `pnpm --dir web test:e2e --workers=1`: **87/87 PASS** in7.7m with zero retries. These are integrated browser/API fixtures, not a fresh live-provider acceptance run; unrelated untracked Keycloak-theme scenarios are outside this published tree.
- `pnpm --dir landing check`: **19/19 PASS**, lint/format/types/build and font checks. Actual built landing observed at1280×900 and390×844 with no horizontal overflow; dark/light rendering was confirmed through the theme control. This is local UI proof, not a landing deployment.
- Isolated POSIX execution: deployment transaction/credential/serving lifecycle **24/24 PASS** and managed probe/provisioning **5/5 PASS**. Bash syntax, actionlint1.7.12, canonical ShellCheck0.11.0 and `check-inference-release.py --compose` pass. No native model or deployment was launched by these checks.

Warnings remain explicit: JVM native-access/Unsafe warnings, the frontend's approximately511kB minified entry chunk warning and no configured LSP/IDE verification. Repository gates do not attest the final API image, actual target capacity, deployment, credentials rotation or live-provider behavior on this merged tree.

### Publication and runtime boundary

Twelve unrelated tracked working files and24 untracked files were recorded separately before advancing the primary checkout; they are not part of the MEM-77 commit. The validated merge is published only to `anhnd`, with exact push/comment receipts recorded on [MEM-77](https://linear.app/memory-os/issue/MEM-77) after the ordered Git operations. Linear MCP was reached through the existing OAuth-configured Codex app-server direct tool-call protocol; no model turn or new credential was required. Issue status/assignee are not changed by the requested comment.

The earlier real hosted/local and six-token upstream Stop receipts above remain candidate evidence at their original revision, not a rerun against this merge. Local inference remains intentionally stopped with assets/cache/key retained; warm restart, full co-load/target qualification, final API-image verification, rotation/recovery and broader acceptance remain open. The running development API still uses its earlier migration/runtime history. Do not restart it against the newly integrated source without an authorized data-preserving cutover.
