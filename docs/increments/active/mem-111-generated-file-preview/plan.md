# MEM-111 plan — generated file preview

- [ ] **Spreadsheet route.** `ChatFileArtifactController` `GET /{id}/preview`; `SpreadsheetPreview` in `chat.interpreter` with POI `XSSFReader`, CSV quoting, the 500 000-character row-boundary cut; OpenAPI and generated client.
- [ ] **Preview dialog.** Variant resolver in the Onyx order; image, pdf (`DocumentPdfView`), csv, xlsx tabs, markdown, docx (`docx-preview` + DOMPurify), text/code/json, unsupported.
- [ ] **Card entry point.** The file name opens the dialog; download remains.
- [ ] **Tests.** Java: CSV quoting, row-boundary cut, sheet order and names, non-xlsx rejected, owner-only. Web: variant resolution, csv parser with quoted commas and newlines, docx sanitizer drops `javascript:` and keeps `data:` images, dialog opens from the card in both languages.
- [ ] **Docs.** Chat spec generated-files section and verification matrix.
- [ ] **pptx (step 2).** Measure `libreoffice-impress-nogui` image size and conversion time in the executor sandbox; if accepted, convert in the executor, store the PDF beside the artifact and preview it with the pdf variant.
- [ ] **Evidence.** Local tests; a staging preview of a Vietnamese xlsx, docx and pdf generated by `run_python`.
