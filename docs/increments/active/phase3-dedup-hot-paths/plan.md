# Plan

- [x] Rename the integration bundle to `sources/` and move its packages to their owners (ADR 0016).
- [x] Search: index ensured once per generation, no alias HEAD per search, row-value cursor, bulk projection maintenance, logged gateway failures.
- [x] Chat send: persona and capabilities once, bulk file reads; bulk branch copy and persona reorder.
- [x] Meeting: one job runner, batched writes, streamed recordings, recording-upload sweep.
  - `shared.LeasedJob` runs the minutes, recording, chat export, library archive and usage-report passes (corrections are a window, not a leased job). Utterances, minutes items, shares and correction proposals are written with `INSERT … SELECT unnest`. `Recording` carries an `AudioSource` and its size and is streamed into the multipart upload. Retiring a recording is one sweep over meetings still holding the upload of a `DONE`/`FAILED` recording (V129); delete retires first. One row/utterance/item/correction column list, one speaker and reader mapper, `SpeakerNames` for key, fallback name and clock.
- [x] Shared helpers: `PdfText`, SHA-256, LIKE-escape; provider-connection helper; voice synthesis on a shared client.
  - `shared.PdfText`, `shared.Sha256`, `shared.LikePattern` in the root package (no named interface to declare). `ai.ProviderConnections` for image, Web and voice. `HttpAudioStream` cancels its own `sendAsync` exchange. The remaining SHA-256 copies in `document`, `retrieval`, `connector` and `api/source` use `Sha256` too, byte-identical: the Google Drive request hash hashes the same length-prefixed bytes (pinned by `GoogleDriveLinkTest`), and `StructuredDocumentChunker.sha256` is gone. SHA-1 and MD5 users are unchanged.
- [x] One sync engine for Google Drive and SharePoint with Onyx failure semantics, `Retry-After`, the shared error column and one selection repository (V131).
- [x] SharePoint scope once per run, fewer lock queries per item, Google Drive membership from the run.

## Verification

All dated 2026-09-25. Each change was tested at its narrowest boundary; efficiency is asserted as request, statement or lock counts, not timings. The full `clean check` gate runs in CI.

- **`sources` bundle (ADR 0016).** Every moved test in `sources` (Docling, PaddleOCR-VL, Tika, spreadsheet, Google native and SharePoint page extraction, the provider clients), `SourceApiIntegrationTest`, and `SourcesDependencyRulesTest`, which also keeps `connector.adapter` off `ingestion` after the extraction model moved to `document`.
- **Search.** `OpenSearchGatewayTest` (failed requests logged once; `index_not_found_exception` recognised); `OpenSearchRetrievalIntegrationTest` (no alias HEAD per search; a repeated write sends 3 requests instead of 10; a 404 write re-ensures the index); `SearchIndexWorkIntegrationTest` (row-value reconcile cursor; a 32-document reconcile page costs 1 search request instead of up to 128 and 2 database reads per Tenant instead of 64); `SearchRebuildIntegrationTest`, `SourceSearchMetadataMigrationTest`, `SourceSyncAccessTest`.
- **Chat.** `ChatTurnServiceTest`, `ModelCatalogSelectionTest` and `ChatPersistenceIntegrationTest` with `StatementCounter` (persona and capabilities read once per send; context files in one query; reorder locks in one statement; the turn outcome from the terminal UPDATE); `ChatLifecycleIntegrationTest` (branch path copied in one statement).
- **Meeting and shared helpers.** `:core:test` for `io.memoryos.meeting.*`, `io.memoryos.voice.*`, `io.memoryos.shared.*`, `io.memoryos.usage.report.*`, `io.memoryos.audit.*`, `io.memoryos.library.*`, `io.memoryos.objectstorage.*`, `io.memoryos.iam.invitation.*`, `io.memoryos.iam.group.*`, `io.memoryos.chat.image.*`, `io.memoryos.chat.web.*`, `ChatExportIntegrationTest`, `ChatHistoryServiceTest`, `ModulithArchitectureTest`, `CoreDependencyRulesTest`; `:api:test` `OpenApiContractTest` and the meeting, recording, voice, speaker, minutes, correction, bookmark and Web methods of `ChatSessionApiIntegrationTest`; `:worker:test` `ControlPlaneIntegrationTest`. `HttpAudioStreamTest` (cancel before headers and mid-body on the shared client), `ImageConnectionServiceTest`.
- **SHA-256 in document, retrieval, connector and API.** `GoogleDriveLinkTest.theRequestHashIsTheOneAlreadyStoredOnSelectionOperations` pins the Drive request hash to the value the replaced incremental digest produced; `SearchGenerationsIntegrationTest` still matches the legacy index name computed independently; `OpenSearchRetrievalIntegrationTest` (an unchanged `metadata_hash` rewrites nothing), `SearchIndexWorkIntegrationTest`, `TextHelpersTest`, `DocumentChunkServiceTest`, `ExtractionArtifactLifecycleTest`, `GoogleDriveSelectionTreeTest`, `GoogleDriveSelectionOperationTest`, `PostgresSharePointSelectionTest`.
- **Sync engine (V131).** `PostgresGoogleDriveSyncTest`, `PostgresSharePointSyncTest`, `PostgresSourceRunHistoryTest`, `PostgresSourceLifecycleTest`, `RetryAfterTest` and the provider client tests: the rows in the [connector matrix](../../../tests/connector.md#shared-synchronization-engine--2026-09-25) (item errors and their resolution, the abort threshold, `Retry-After`, cancellation on pause and delete, one fence of four locks per unchanged file, scope resolved once per run).
- **Run-error resolution and new codes.** `PostgresSourceRunHistoryTest` and `SourceApiIntegrationTest.aRunCompletedWithErrorsListsEachFileErrorWithItsResolution` (`resolvedAt` null and set; a stored `COMPLETED_WITH_ERRORS` run filtered by that status); `source-history-presentation.test.tsx` and `source-errors.test.ts`. `openapi.yml` gains only `SourceRunError.resolvedAt` (required, nullable date-time), regenerated with `MEMORYOS_OPENAPI_WRITE=true` and `pnpm generate:api`.
- **Final pass on the merged branch.** `:core:compileJava :core:compileTestJava :api:compileJava :api:compileTestJava :worker:compileJava :worker:compileTestJava :sources:compileJava :sources:compileTestJava`; `:core:test` `ModulithArchitectureTest`, `CoreDependencyRulesTest`, `io.memoryos.connector.sync.*`, `SearchRebuildIntegrationTest`, `MeetingRepositoryTest`, `ChatTurnServiceTest`, `io.memoryos.shared.*` (51 tests); `:sources:test` `SourcesDependencyRulesTest`; `:api:test` `OpenApiContractTest` and `io.memoryos.api.source.*` (55 tests); `:worker:test` `ControlPlaneIntegrationTest`, `WorkerFileProcessingIntegrationTest`; web `pnpm typecheck`, `lint`, `format:check`, `i18n-audit --check`, `check:api`, `test:unit` (145 files, 713 tests). All passed. Flyway versions are unique; V130 is unused (V129 is followed by V131).
