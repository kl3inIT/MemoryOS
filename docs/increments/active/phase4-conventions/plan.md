# Plan

- [x] API contract hygiene: headers, `contract/` records, typed `ApiProblem` extensions, springdoc customizer and `@CurrentActor`.
- [x] Typed Chat failure codes.
- [x] Identity people-and-Group search; Google Drive account client and sign-in admission out of `api`; branch and meeting-publish API tests.
- [x] Imports instead of inline qualified names; fluent logging everywhere.
- [x] `deploy.sh` ERR trap; delivered increments to `completed/`; roadmap reconciled.
- [x] Principal search open to every active member (owner decision 2026-09-25).

## Verification

- API contract hygiene: `OpenApiContractTest` (with and without `MEMORYOS_OPENAPI_WRITE`), `SecurityResponseHeadersTest`, `SessionSecurityIntegrationTest`, `SourceApiIntegrationTest`, `SharePointSourceApiTest`, `SharePointCredentialApiTest`, `GoogleDriveOAuthTest`, `McpOAuthCallbackTest`, `DocumentOriginalResponsesTest`, `ChatEventStreamTest` and the library, meeting, feedback and file cases of `ChatSessionApiIntegrationTest`; web `check:api`, `typecheck`, `lint`, `format:check`, `test:unit` (714 tests).
- Ownership follow-ups: `GET /api/identity/principals` (IAM `PrincipalSearch`; first with `CHAT_WRITE` as the removed `/api/chat/persona-share-options` required, then opened to every active member, see below) with `openapi.yml` and the Hey API client regenerated; the Google account OAuth protocol moved to the provider bundle behind the connector port `GoogleDriveAccountClient`, its endpoints bound once in `GoogleDriveProviderProperties`; sign-in admission decided by IAM `SignInAdmission` in one transaction. Compiled `core`, `sources`, `api`, `worker` with tests; `:core:test` `ModulithArchitectureTest`, `CoreDependencyRulesTest`, `io.memoryos.iam.*` (121, including `DefaultSignInAdmissionTest`); `:sources:test` `SourcesDependencyRulesTest` and `*GoogleDrive*` (45, including `RestGoogleDriveAccountClientTest`); `:api:test` `OpenApiContractTest` (with and without `MEMORYOS_OPENAPI_WRITE`), `SessionSecurityIntegrationTest` (16), `ActorSessionLoginSuccessHandlerTest` (4), `GoogleDriveOAuthTest` (7), `SourceApiIntegrationTest` (29), `SharePointSourceApiTest`, `SharePointCredentialApiTest` and the principal-search, branch and meeting-publish cases of `ChatSessionApiIntegrationTest`; `:worker:test` `ControlPlaneIntegrationTest`; web `check:api`, `check:i18n`, `typecheck`, `lint`, `format:check`, `test:unit` (716 tests).
- Typed failures (task B): `TurnFailure` and `TurnFailureException` in `ai` replace `IllegalStateException("CHAT_…")` messages and the allowlist in `ChatTurnService`; stored codes pinned byte for byte first. `TurnFailureTest`, `ChatTurnServiceTest`, `ChatModelGuardTest`, `OpenAiRequestPolicyTest`, `OpenAiResponsesChatModelTest`.
- Sweep (task D), all dated 2026-09-25:
  - Imports: a masking script (comments, strings and text blocks untouched; `package-info.java` skipped) replaced inline qualified names in handwritten main and test Java of `worker`, `sources`, `api` and `core`, one commit each; collisions keep the less used name qualified. Before: 2,709 occurrences (core 1,779, api 503, sources 339, worker 88). After: 14, all collisions: Spring AI `Message`/`SystemMessage`/`UserMessage`/`AssistantMessage` beside Embabel's in `ChatTurnSetup` and `ChatTurnSetupTest`; swagger's `models.media.MediaType` beside Spring's in `OpenApiConfiguration`; swagger's `@RequestBody` beside Spring's in `InvitationController`. Two package annotations in `package-info.java` stay qualified because imports do not reach them.
  - Logging: every positional SLF4J call in main code (59: 58 in `core`, 1 in `sources`) is fluent with `event` = `<capability>.<subject>.<outcome>` in snake case (for example `chat.run.failed`, `chat.stream.write_failed`, `voice.transcription.usage_not_recorded`, `audit.event.store_failed`), `error_type` = the exception class name as the existing fluent logs record it, and `error_code` where a code exists (turn failure code, MCP rejection reason, `CHAT_WEB_UNAVAILABLE`, the SQLState of an audit write). Handled failures no longer attach stack traces; the presentation preview logs exit code and timeout instead of interpreter output. The audit SIEM line (`memoryos.audit.<class>`) stays one JSON message as the audit spec defines it. Remaining positional calls: 0.
  - `deploy.sh`: an ERR trap reports line and command; the 20 bare assertions and the free-space check print what they refused; the `images.env` line count follows the `images` array. `python -m unittest discover -s infrastructure/deployment` (78 tests) passes on Windows (20 skipped for POSIX tools) and in a `python:3.12-slim` container with `jq` and `flock` (78, none skipped), including the new trap and refusal-message cases.
  - Increments moved to `completed/`: OpenAI provider in core, Chat send lock scope, library copies through the server-write lifecycle, Chat defaults provisioned with the Tenant (PR #366), phase 3 (PR #372), search indexing content errors; roadmap and `AGENTS.md` reconciled. Audit quality fixes stays active: owner decisions in groups 2 and 6 and the web data layer of group 3 are open; items delivered by phases 3 and 4 are ticked.
  - Principal search: `DefaultPrincipalSearch` checks active membership through `TenantAccessResolver` instead of `CHAT_WRITE`; `openapi.yml` (description and 403 text) and the Hey API client regenerated. `DefaultPrincipalSearchTest` (2) and `ChatSessionApiIntegrationTest.principalSearchFindsActiveMembersAndOrdinaryGroupsOfTheTenantForEveryActiveMember`.
  - Final pass: compiled `core`, `api`, `worker`, `sources` with tests; `:core:test` `ModulithArchitectureTest`, `CoreDependencyRulesTest`, `io.memoryos.connector.sync.*`, `ChatTurnServiceTest`, `StreamBufferWriterTest`, `GoogleDriveMetadataCacheTest`, `io.memoryos.meeting.*`, `io.memoryos.audit.*`, `io.memoryos.voice.*`, `io.memoryos.usage.report.*`, `io.memoryos.iam.user.*` (204 tests); `:api:test` `OpenApiContractTest` (with and without `MEMORYOS_OPENAPI_WRITE`), `SessionSecurityIntegrationTest` (16); `:worker:test` `ControlPlaneIntegrationTest`; `:sources:test` `SourcesDependencyRulesTest`. All passed. The imports and logging sweeps leave `openapi.yml` unchanged.
