package io.memoryos.connector;

/** Verifies an accepted SharePoint scope request and activates it when every root resolves. */
public interface SharePointSelectionProcessor extends SourceSelectionProcessor {
}
