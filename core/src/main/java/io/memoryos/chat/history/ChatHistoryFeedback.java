package io.memoryos.chat.history;

/** How a conversation's readers rated its answers, rolled up as Onyx rolls a session's feedback up. */
public enum ChatHistoryFeedback { POSITIVE, NEGATIVE, MIXED, NONE }
