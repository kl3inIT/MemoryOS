openapi: 3.1.0
info:
  title: MemoryOS browser API
  description: The same-origin contract consumed by the MemoryOS browser application.
  version: 1.0.0
servers:
- url: /
  description: Same-origin MemoryOS runtime
paths:
  /api/chat/providers/{providerId}:
    put:
      tags:
      - Chat models
      summary: Replace provider settings at the expected revision; credential action
        is explicit
      operationId: updateChatProvider
      parameters:
      - name: providerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: revision
        in: query
        required: true
        schema:
          type: integer
          format: int64
          exclusiveMinimum: 0
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProviderInput"
        required: true
      responses:
        "400":
          description: Invalid configuration
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "403":
          description: "Model management, Tenant membership or CSRF requirement not\
            \ met"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: Resource not accessible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "409":
          description: Stale revision or duplicate model
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "503":
          description: "Provider, encryption key or client capacity unavailable"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: Authentication required
        "200":
          description: Successful result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProviderView"
      security:
      - bearerAuth: []
      - browserSession: []
    delete:
      tags:
      - Chat models
      summary: Delete a provider that is not the Chat default; transcript is preserved
      operationId: deleteChatProvider
      parameters:
      - name: providerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: revision
        in: query
        required: true
        schema:
          type: integer
          format: int64
          exclusiveMinimum: 0
      responses:
        "400":
          description: Invalid configuration
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "403":
          description: "Model management, Tenant membership or CSRF requirement not\
            \ met"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: Resource not accessible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "409":
          description: Stale revision or duplicate model
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "503":
          description: "Provider, encryption key or client capacity unavailable"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: Authentication required
        "204":
          description: No Content
      security:
      - bearerAuth: []
      - browserSession: []
  /api/chat/personas/{personaId}/model:
    get:
      tags:
      - Chat models
      summary: Read the Persona model selection and revision; requires model management
      operationId: getPersonaModel
      parameters:
      - name: personaId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "400":
          description: Invalid configuration
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "403":
          description: "Model management, Tenant membership or CSRF requirement not\
            \ met"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: Resource not accessible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "409":
          description: Stale revision or duplicate model
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "503":
          description: "Provider, encryption key or client capacity unavailable"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: Authentication required
        "200":
          description: Successful result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonaModel"
      security:
      - bearerAuth: []
      - browserSession: []
    put:
      tags:
      - Chat models
      summary: Set a Persona model or omit the model ID to inherit the Chat default
      operationId: setPersonaModel
      parameters:
      - name: personaId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: modelConfigurationId
        in: query
        required: false
        schema:
          type: string
          format: uuid
      - name: revision
        in: query
        required: true
        schema:
          type: integer
          format: int64
          exclusiveMinimum: 0
      responses:
        "400":
          description: Invalid configuration
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "403":
          description: "Model management, Tenant membership or CSRF requirement not\
            \ met"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: Resource not accessible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "409":
          description: Stale revision or duplicate model
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "503":
          description: "Provider, encryption key or client capacity unavailable"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: Authentication required
        "200":
          description: Successful result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonaModel"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/chat/models/{modelId}:
    put:
      tags:
      - Chat models
      summary: Replace a model configuration at the expected revision
      operationId: updateChatModel
      parameters:
      - name: modelId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: revision
        in: query
        required: true
        schema:
          type: integer
          format: int64
          exclusiveMinimum: 0
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ModelInput"
        required: true
      responses:
        "400":
          description: Invalid configuration
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "403":
          description: "Model management, Tenant membership or CSRF requirement not\
            \ met"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: Resource not accessible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "409":
          description: Stale revision or duplicate model
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "503":
          description: "Provider, encryption key or client capacity unavailable"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: Authentication required
        "200":
          description: Successful result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Model"
      security:
      - bearerAuth: []
      - browserSession: []
    delete:
      tags:
      - Chat models
      summary: Delete a non-default model and clear its Persona defaults; transcript
        is preserved
      operationId: deleteChatModel
      parameters:
      - name: modelId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: revision
        in: query
        required: true
        schema:
          type: integer
          format: int64
          exclusiveMinimum: 0
      responses:
        "400":
          description: Invalid configuration
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "403":
          description: "Model management, Tenant membership or CSRF requirement not\
            \ met"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: Resource not accessible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "409":
          description: Stale revision or duplicate model
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "503":
          description: "Provider, encryption key or client capacity unavailable"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: Authentication required
        "204":
          description: No Content
      security:
      - bearerAuth: []
      - browserSession: []
  /api/chat/model-default:
    get:
      tags:
      - Chat models
      summary: Read the Tenant Chat default and its revision; requires model management
      operationId: getChatModelDefault
      responses:
        "400":
          description: Invalid configuration
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "403":
          description: "Model management, Tenant membership or CSRF requirement not\
            \ met"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: Resource not accessible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "409":
          description: Stale revision or duplicate model
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "503":
          description: "Provider, encryption key or client capacity unavailable"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: Authentication required
        "200":
          description: Successful result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Default"
      security:
      - bearerAuth: []
      - browserSession: []
    put:
      tags:
      - Chat models
      summary: "Set a visible, publicly available Tenant Chat default"
      operationId: setChatModelDefault
      parameters:
      - name: modelConfigurationId
        in: query
        required: true
        schema:
          type: string
          format: uuid
      - name: revision
        in: query
        required: true
        schema:
          type: integer
          format: int64
          exclusiveMinimum: 0
      responses:
        "400":
          description: Invalid configuration
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "403":
          description: "Model management, Tenant membership or CSRF requirement not\
            \ met"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: Resource not accessible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "409":
          description: Stale revision or duplicate model
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "503":
          description: "Provider, encryption key or client capacity unavailable"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: Authentication required
        "200":
          description: Successful result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Default"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/users/{actorId}/groups:
    post:
      tags:
      - Users
      summary: Replace a user's ordinary Group memberships
      operationId: replaceUserGroups
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      - name: actorId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReplaceUserGroupsRequest"
        required: true
      responses:
        "204":
          description: The user's ordinary Group memberships were replaced
        "401":
          description: No accepted authentication is present
        "403":
          description: The actor lacks IAM_ADMIN authority or the same-origin header
            is missing
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: The user or a requested Group does not exist in the current
            Tenant
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
      security:
      - browserSession: []
      - bearerAuth: []
  /api/users/{actorId}/deactivate:
    post:
      tags:
      - Users
      summary: Deactivate an existing Tenant member
      operationId: deactivateUser
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      - name: actorId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "204":
          description: The member is inactive
        "401":
          description: No accepted authentication is present
        "403":
          description: "The actor lacks USERS_MANAGE authority, the target is protected,\
            \ or the same-origin header is missing"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: The member does not exist in the current Tenant
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
      security:
      - browserSession: []
      - bearerAuth: []
  /api/users/{actorId}/activate:
    post:
      tags:
      - Users
      summary: Activate an existing Tenant member
      operationId: activateUser
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      - name: actorId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "204":
          description: The member is active
        "401":
          description: No accepted authentication is present
        "403":
          description: "The actor lacks USERS_MANAGE authority, the target is protected,\
            \ or the same-origin header is missing"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: The member does not exist in the current Tenant
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
      security:
      - browserSession: []
      - bearerAuth: []
  /api/sources/{sourceId}/uploads:
    post:
      tags:
      - Sources
      summary: Authorize one direct FILE source upload
      operationId: initiateSourceUpload
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      - name: sourceId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InitiateSourceUploadRequest"
        required: true
      responses:
        "201":
          description: Created
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/SourceUploadAuthorization"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/sources/{sourceId}/uploads/{uploadId}/finalize:
    post:
      tags:
      - Sources
      summary: Verify and adopt one direct FILE source upload
      operationId: finalizeSourceUpload
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      - name: sourceId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: uploadId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "202":
          description: Accepted
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/SourceUploadReceipt"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/sources/{sourceId}/items/{itemId}/remove:
    post:
      tags:
      - Sources
      summary: Start durable source item removal
      operationId: removeSourceItem
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      - name: sourceId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: itemId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "202":
          description: Accepted
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/SourceOperation"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/sources/{sourceId}/items/{itemId}/index-attempts:
    post:
      tags:
      - Sources
      summary: Reindex one source item
      operationId: reindexSourceItem
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      - name: sourceId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: itemId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "202":
          description: Accepted
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/SourceOperation"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/sources/{sourceId}/groups:
    get:
      tags:
      - Sources
      summary: List groups associated with one source
      operationId: listSourceGroups
      parameters:
      - name: sourceId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          description: OK
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/SourceGroupList"
      security:
      - bearerAuth: []
      - browserSession: []
    post:
      tags:
      - Sources
      summary: Replace groups associated with one source
      operationId: updateSourceGroups
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      - name: sourceId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSourceGroupsRequest"
        required: true
      responses:
        "204":
          description: No Content
      security:
      - bearerAuth: []
      - browserSession: []
  /api/sources/{sourceId}/delete:
    post:
      tags:
      - Sources
      summary: Start durable source deletion
      operationId: deleteSource
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      - name: sourceId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "202":
          description: Accepted
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/SourceOperation"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/sources/file:
    post:
      tags:
      - Sources
      summary: Create a Tenant-owned FILE source
      operationId: createFileSource
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateFileSourceRequest"
        required: true
      responses:
        "201":
          description: Created
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/SourceDetail"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/search:
    post:
      tags:
      - Search
      summary: Search current documents with keyword and semantic retrieval
      operationId: searchDocuments
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SearchRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/SearchPage"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/invitations:
    get:
      tags:
      - Invitations
      summary: List Tenant invitations manageable by the current IAM administrator
      operationId: listInvitations
      parameters:
      - name: status
        in: query
        required: false
        schema:
          type: string
          enum:
          - PENDING
          - ACCEPTED
          - EXPIRED
          - REVOKED
      - name: email
        in: query
        required: false
        schema:
          type: string
      - name: sort
        in: query
        required: false
        schema:
          type: string
          default: CREATED_AT_DESC
          enum:
          - CREATED_AT_DESC
          - CREATED_AT_ASC
          - EMAIL_ASC
          - EMAIL_DESC
      - name: page
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 0
          minimum: 0
      - name: size
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 20
          maximum: 100
          minimum: 1
      responses:
        "200":
          description: A bounded page of invitation lifecycle records without plaintext
            secrets
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvitationPage"
        "400":
          description: Invalid invitation list query
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: No accepted authentication is present
        "403":
          description: The actor lacks USERS_MANAGE authority
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
      security:
      - browserSession: []
      - bearerAuth: []
    post:
      tags:
      - Invitations
      summary: Create one Tenant member invitation
      operationId: createInvitation
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateInvitationRequest"
        required: true
      responses:
        "201":
          description: Invitation created; the invitation URL is returned only in
            this response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IssuedInvitation"
        "400":
          description: Invalid email
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: No accepted authentication is present
        "403":
          description: The actor lacks USERS_MANAGE authority or the same-origin header
            is missing
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "409":
          description: A conflicting pending invitation already exists
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "503":
          description: Keycloak recipient activation is temporarily unavailable
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
      security:
      - browserSession: []
      - bearerAuth: []
  /api/invitations/{invitationId}/rotate:
    post:
      tags:
      - Invitations
      summary: Replace the secret of a pending invitation
      operationId: rotateInvitation
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      - name: invitationId
        in: path
        description: Invitation identifier.
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          description: Invitation rotated; the replacement URL is returned only in
            this response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IssuedInvitation"
        "401":
          description: No accepted authentication is present
        "403":
          description: The actor lacks USERS_MANAGE authority or the same-origin header
            is missing
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "410":
          description: Invitation is no longer pending and available
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
      security:
      - browserSession: []
      - bearerAuth: []
  /api/invitations/{invitationId}/revoke:
    post:
      tags:
      - Invitations
      summary: Revoke a pending invitation
      operationId: revokeInvitation
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      - name: invitationId
        in: path
        description: Invitation identifier.
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "204":
          description: Invitation revoked
        "401":
          description: No accepted authentication is present
        "403":
          description: The actor lacks USERS_MANAGE authority or the same-origin header
            is missing
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "410":
          description: Invitation is no longer pending and available
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
      security:
      - browserSession: []
      - bearerAuth: []
  /api/groups:
    get:
      tags:
      - Groups
      summary: List Groups visible to the current actor
      operationId: listGroups
      parameters:
      - name: search
        in: query
        required: false
        schema:
          type: string
          maxLength: 200
          minLength: 0
      - name: page
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 0
          minimum: 0
      - name: size
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 20
          maximum: 100
          minimum: 1
      responses:
        "200":
          description: "A bounded, server-authorized Group page"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GroupPage"
        "401":
          description: No accepted authentication is present
        "403":
          description: The actor has no global or managed-Group read authority
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
      security:
      - bearerAuth: []
      - browserSession: []
    post:
      tags:
      - Groups
      summary: Create an ordinary Group
      operationId: createGroup
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateGroupRequest"
        required: true
      responses:
        "201":
          description: Group created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GroupSummary"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/groups/{groupId}/rename:
    post:
      tags:
      - Groups
      summary: Rename an ordinary Group
      operationId: renameGroup
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      - name: groupId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RenameGroupRequest"
        required: true
      responses:
        "200":
          description: Group renamed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GroupSummary"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/groups/{groupId}/members:
    get:
      tags:
      - Groups
      summary: List members of one visible Group
      operationId: listGroupMembers
      parameters:
      - name: groupId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: search
        in: query
        required: false
        schema:
          type: string
          maxLength: 200
          minLength: 0
      - name: page
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 0
          minimum: 0
      - name: size
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 20
          maximum: 100
          minimum: 1
      responses:
        "200":
          description: A bounded Group member page
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GroupMemberPage"
      security:
      - bearerAuth: []
      - browserSession: []
    post:
      tags:
      - Groups
      summary: Add Tenant members to a Group
      operationId: addGroupMembers
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      - name: groupId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddGroupMembersRequest"
        required: true
      responses:
        "204":
          description: Members added idempotently
      security:
      - bearerAuth: []
      - browserSession: []
  /api/groups/{groupId}/members/{actorId}/remove:
    post:
      tags:
      - Groups
      summary: Remove one member from a Group
      operationId: removeGroupMember
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      - name: groupId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: actorId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "204":
          description: Member removed
      security:
      - bearerAuth: []
      - browserSession: []
  /api/groups/{groupId}/members/{actorId}/remove-manager:
    post:
      tags:
      - Groups
      summary: Remove a manager from an ordinary Group
      operationId: removeGroupManager
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      - name: groupId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: actorId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "204":
          description: Manager flag removed
      security:
      - bearerAuth: []
      - browserSession: []
  /api/groups/{groupId}/members/{actorId}/assign-manager:
    post:
      tags:
      - Groups
      summary: Assign a manager to an ordinary Group
      operationId: assignGroupManager
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      - name: groupId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: actorId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "204":
          description: Manager assigned
      security:
      - bearerAuth: []
      - browserSession: []
  /api/groups/{groupId}/delete:
    post:
      tags:
      - Groups
      summary: Delete an ordinary Group
      operationId: deleteGroup
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      - name: groupId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "204":
          description: Group links and grants deleted
      security:
      - bearerAuth: []
      - browserSession: []
  /api/groups/{groupId}/capabilities:
    post:
      tags:
      - Groups
      summary: Replace an ordinary Group's explicit capability grants
      operationId: replaceGroupCapabilities
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      - name: groupId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReplaceGroupCapabilitiesRequest"
        required: true
      responses:
        "204":
          description: Explicit grants replaced
      security:
      - bearerAuth: []
      - browserSession: []
  /api/chat/sessions:
    get:
      tags:
      - Chat
      summary: List the actor's private chat sessions
      operationId: listChatSessions
      parameters:
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 0
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 30
      responses:
        "400":
          description: Invalid request or cursor
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "403":
          description: Tenant membership or CSRF requirement not met
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: Conversation or message not accessible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: Authentication required
        "200":
          description: Owned sessions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChatSession"
      security:
      - bearerAuth: []
      - browserSession: []
    post:
      tags:
      - Chat
      summary: Create a private chat session
      operationId: createChatSession
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateChatSession"
        required: true
      responses:
        "400":
          description: Invalid request or cursor
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "403":
          description: Tenant membership or CSRF requirement not met
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: Conversation or message not accessible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: Authentication required
        "201":
          description: Created private session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatSession"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/chat/sessions/{sessionId}/messages:
    get:
      tags:
      - Chat
      summary: Read the selected chat branch after a message cursor
      operationId: getChatHistory
      parameters:
      - name: sessionId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: after
        in: query
        required: false
        schema:
          type: string
          format: uuid
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 50
      responses:
        "400":
          description: Invalid request or cursor
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "403":
          description: Tenant membership or CSRF requirement not met
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: Conversation or message not accessible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: Authentication required
        "200":
          description: Selected branch history
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChatMessage"
      security:
      - bearerAuth: []
      - browserSession: []
    post:
      tags:
      - Chat
      summary: Reserve and execute a chat reply in the background
      operationId: sendChatMessage
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      - name: sessionId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Send"
        required: true
      responses:
        "400":
          description: Invalid request or cursor
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "403":
          description: Tenant membership or CSRF requirement not met
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: Conversation or message not accessible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "409":
          description: Conflicting request or active reply
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "503":
          description: Chat capacity exhausted or provider unavailable
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: Authentication required
        "202":
          description: Reserved reply
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Accepted"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/chat/sessions/{sessionId}/messages/{assistantMessageId}/cancel:
    post:
      tags:
      - Chat
      summary: Request Stop; read history for the committed terminal outcome
      operationId: cancelChatMessage
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      - name: sessionId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: assistantMessageId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "400":
          description: Invalid request or cursor
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "403":
          description: Tenant membership or CSRF requirement not met
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: Conversation or message not accessible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "409":
          description: Conflicting request or active reply
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "503":
          description: Chat capacity exhausted or provider unavailable
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: Authentication required
        "202":
          description: Stop requested; history contains the committed outcome
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Cancellation"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/chat/providers:
    get:
      tags:
      - Chat models
      summary: List Tenant provider configuration with credentials redacted
      operationId: listChatProviders
      responses:
        "400":
          description: Invalid configuration
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "403":
          description: "Model management, Tenant membership or CSRF requirement not\
            \ met"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: Resource not accessible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "409":
          description: Stale revision or duplicate model
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "503":
          description: "Provider, encryption key or client capacity unavailable"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: Authentication required
        "200":
          description: Successful result
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProviderView"
      security:
      - bearerAuth: []
      - browserSession: []
    post:
      tags:
      - Chat models
      summary: Create a Tenant provider; requires MODELS_MANAGE
      operationId: createChatProvider
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProviderInput"
        required: true
      responses:
        "400":
          description: Invalid configuration
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "403":
          description: "Model management, Tenant membership or CSRF requirement not\
            \ met"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: Resource not accessible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "409":
          description: Stale revision or duplicate model
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "503":
          description: "Provider, encryption key or client capacity unavailable"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: Authentication required
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProviderView"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/chat/providers/{providerId}/models:
    get:
      tags:
      - Chat models
      summary: List all configured models on a provider; requires model management
      operationId: listConfiguredChatModels
      parameters:
      - name: providerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "400":
          description: Invalid configuration
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "403":
          description: "Model management, Tenant membership or CSRF requirement not\
            \ met"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: Resource not accessible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "409":
          description: Stale revision or duplicate model
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "503":
          description: "Provider, encryption key or client capacity unavailable"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: Authentication required
        "200":
          description: Successful result
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Model"
      security:
      - bearerAuth: []
      - browserSession: []
    post:
      tags:
      - Chat models
      summary: Add a concrete model configuration to a Tenant provider
      operationId: createChatModel
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      - name: providerId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ModelInput"
        required: true
      responses:
        "400":
          description: Invalid configuration
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "403":
          description: "Model management, Tenant membership or CSRF requirement not\
            \ met"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: Resource not accessible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "409":
          description: Stale revision or duplicate model
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "503":
          description: "Provider, encryption key or client capacity unavailable"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: Authentication required
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Model"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/chat/models/{modelId}/validate:
    post:
      tags:
      - Chat models
      summary: Explicit bounded provider connectivity check; does not certify model
        capabilities
      operationId: validateChatModel
      parameters:
      - name: X-MemoryOS-CSRF
        in: header
        description: Same-origin non-simple request guard for browser-session mutations.
        required: true
        schema:
          type: string
          enum:
          - "1"
      - name: modelId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "400":
          description: Invalid configuration
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "403":
          description: "Model management, Tenant membership or CSRF requirement not\
            \ met"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: Resource not accessible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "409":
          description: Stale revision or duplicate model
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "503":
          description: "Provider, encryption key or client capacity unavailable"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: Authentication required
        "200":
          description: Successful result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Result"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/users:
    get:
      tags:
      - Users
      summary: List users manageable by the current IAM administrator
      operationId: listUsers
      parameters:
      - name: search
        in: query
        required: false
        schema:
          type: string
          maxLength: 200
          minLength: 0
      - name: status
        in: query
        required: false
        schema:
          type: string
          enum:
          - ACTIVE
          - INACTIVE
          - INVITED
      - name: role
        in: query
        required: false
        schema:
          type: string
          enum:
          - OWNER
          - MEMBER
      - name: groupId
        in: query
        required: false
        schema:
          type: string
          format: uuid
      - name: sort
        in: query
        required: false
        schema:
          type: string
          default: NAME_ASC
          enum:
          - NAME_ASC
          - NAME_DESC
          - EMAIL_ASC
          - EMAIL_DESC
          - STATUS_ASC
          - STATUS_DESC
          - ROLE_ASC
          - ROLE_DESC
      - name: page
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 0
          minimum: 0
      - name: size
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 20
          maximum: 100
          minimum: 1
      responses:
        "200":
          description: A bounded page of current memberships and eligible pending
            invitations
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserPage"
        "400":
          description: Invalid user directory query
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: No accepted authentication is present
        "403":
          description: The actor lacks USERS_MANAGE authority
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
      security:
      - browserSession: []
      - bearerAuth: []
  /api/sources:
    get:
      tags:
      - Sources
      summary: List Tenant sources
      operationId: listSources
      responses:
        "200":
          description: OK
          content:
            '*/*':
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SourceSummary"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/sources/{sourceId}:
    get:
      tags:
      - Sources
      summary: Get one source with current items
      operationId: getSource
      parameters:
      - name: sourceId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          description: OK
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/SourceDetail"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/sources/{sourceId}/items:
    get:
      tags:
      - Sources
      summary: List current source items
      operationId: listSourceItems
      parameters:
      - name: sourceId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          description: OK
          content:
            '*/*':
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SourceItem"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/sources/{sourceId}/index-attempts:
    get:
      tags:
      - Sources
      summary: List source indexing attempts
      operationId: listSourceIndexAttempts
      parameters:
      - name: sourceId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: size
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 50
          maximum: 100
          minimum: 1
      responses:
        "200":
          description: OK
          content:
            '*/*':
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SourceOperation"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/sources/group-options:
    get:
      tags:
      - Sources
      summary: List groups available for source association
      operationId: listSourceGroupOptions
      parameters:
      - name: search
        in: query
        required: false
        schema:
          type: string
          maxLength: 200
          minLength: 0
      - name: page
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 0
          minimum: 0
      - name: size
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 25
          maximum: 100
          minimum: 1
      responses:
        "200":
          description: OK
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/SourceGroupPage"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/source-operations/{operationId}:
    get:
      tags:
      - Sources
      summary: Get a durable source operation
      operationId: getSourceOperation
      parameters:
      - name: operationId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          description: OK
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/SourceOperation"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/search/documents/{documentId}:
    get:
      tags:
      - Search
      summary: Read current document passages around a search result
      operationId: getSearchDocument
      parameters:
      - name: documentId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: generation
        in: query
        required: true
        schema:
          type: string
          format: uuid
      - name: from
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 0
      responses:
        "200":
          description: OK
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/SearchDocument"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/invitations/current:
    get:
      tags:
      - Invitations
      summary: Return the redacted invitation landing context from the browser session
      operationId: getCurrentInvitation
      responses:
        "200":
          description: Active invitation continuation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CurrentInvitation"
        "410":
          description: No available invitation continuation exists
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
  /api/identity/me:
    get:
      tags:
      - Identity
      summary: Return the authenticated MemoryOS actor
      description: Accepts either an existing MemoryOS browser session or a valid
        bound bearer identity and returns the stable internal ActorId plus its durable
        Tenant authority projection.
      operationId: getCurrentIdentity
      responses:
        "200":
          description: The authenticated actor
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CurrentIdentity"
        "401":
          description: No accepted authentication is present
      security:
      - browserSession: []
      - bearerAuth: []
  /api/groups/{groupId}:
    get:
      tags:
      - Groups
      summary: Get one visible Group
      operationId: getGroup
      parameters:
      - name: groupId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          description: The server-authorized Group detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GroupSummary"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/groups/{groupId}/sources:
    get:
      tags:
      - Groups
      summary: List sources associated with one group
      operationId: listGroupSources
      parameters:
      - name: groupId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          description: OK
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/GroupSources"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/groups/{groupId}/candidates:
    get:
      tags:
      - Groups
      summary: List Tenant members eligible to join a Group
      operationId: listGroupCandidates
      parameters:
      - name: groupId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: search
        in: query
        required: false
        schema:
          type: string
          maxLength: 200
          minLength: 0
      - name: page
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 0
          minimum: 0
      - name: size
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 20
          maximum: 100
          minimum: 1
      responses:
        "200":
          description: A bounded candidate page
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GroupMemberPage"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/groups/capabilities:
    get:
      tags:
      - Groups
      summary: List the server-owned Group capability registry
      operationId: listGroupCapabilities
      responses:
        "200":
          description: Capability metadata for implemented consumers
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GroupCapabilities"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/chat/sessions/{sessionId}:
    get:
      tags:
      - Chat
      summary: Read an owned chat session
      operationId: getChatSession
      parameters:
      - name: sessionId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "400":
          description: Invalid request or cursor
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "403":
          description: Tenant membership or CSRF requirement not met
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: Conversation or message not accessible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: Authentication required
        "200":
          description: Owned session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatSession"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/chat/sessions/{sessionId}/messages/{assistantMessageId}/events:
    get:
      tags:
      - Chat
      summary: Read or resume reply events; reset requires persisted history
      description: "Events: text-delta, outcome, reset. Content event id is assistantMessageId:sequence.\
        \ Only outcome confirms a committed terminal state. Heartbeats are comments.\
        \ A reset has no event id."
      operationId: streamChatMessage
      parameters:
      - name: sessionId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: assistantMessageId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: Last-Event-ID
        in: header
        required: false
        schema:
          type: string
      - name: after
        in: query
        required: false
        schema:
          type: string
      responses:
        "400":
          description: Invalid request or cursor
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "403":
          description: Tenant membership or CSRF requirement not met
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: Conversation or message not accessible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "503":
          description: Chat capacity exhausted or provider unavailable
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: Authentication required
        "200":
          description: SSE frames; the schema describes each data payload
          content:
            text/event-stream:
              schema:
                oneOf:
                - $ref: "#/components/schemas/TextDeltaEvent"
                - $ref: "#/components/schemas/OutcomeEvent"
                - $ref: "#/components/schemas/ResetEvent"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/chat/provider-adapters:
    get:
      tags:
      - Chat models
      summary: List installed adapter types and credential requirements; requires
        model management
      operationId: listChatProviderAdapters
      responses:
        "400":
          description: Invalid configuration
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "403":
          description: "Model management, Tenant membership or CSRF requirement not\
            \ met"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: Resource not accessible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "409":
          description: Stale revision or duplicate model
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "503":
          description: "Provider, encryption key or client capacity unavailable"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: Authentication required
        "200":
          description: Successful result
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Descriptor"
      security:
      - bearerAuth: []
      - browserSession: []
  /api/chat/models:
    get:
      tags:
      - Chat models
      summary: List visible models authorized for this session or the builtin Persona
      operationId: listAvailableChatModels
      parameters:
      - name: sessionId
        in: query
        required: false
        schema:
          type: string
          format: uuid
      responses:
        "400":
          description: Invalid configuration
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "403":
          description: "Model management, Tenant membership or CSRF requirement not\
            \ met"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "404":
          description: Resource not accessible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "409":
          description: Stale revision or duplicate model
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "503":
          description: "Provider, encryption key or client capacity unavailable"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ApiProblem"
        "401":
          description: Authentication required
        "200":
          description: Successful result
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AvailableModel"
      security:
      - bearerAuth: []
      - browserSession: []
components:
  schemas:
    Change:
      type: object
      properties:
        action:
          type: string
          enum:
          - KEEP
          - REPLACE
          - REMOVE
        value:
          type: string
    ProviderInput:
      type: object
      properties:
        name:
          type: string
        adapterType:
          type: string
        baseUrl:
          type: string
        enabled:
          type: boolean
        isPublic:
          type: boolean
        groupIds:
          type: array
          items:
            type: string
            format: uuid
          uniqueItems: true
        personaIds:
          type: array
          items:
            type: string
            format: uuid
          uniqueItems: true
        credential:
          $ref: "#/components/schemas/Change"
    ProviderView:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        adapterType:
          type: string
        baseUrl:
          type: string
        enabled:
          type: boolean
        isPublic:
          type: boolean
        groupIds:
          type: array
          items:
            type: string
            format: uuid
          uniqueItems: true
        personaIds:
          type: array
          items:
            type: string
            format: uuid
          uniqueItems: true
        credentialConfigured:
          type: boolean
        revision:
          type: integer
          format: int64
    PersonaModel:
      type: object
      properties:
        personaId:
          type: string
          format: uuid
        modelConfigurationId:
          type: string
          format: uuid
        revision:
          type: integer
          format: int64
    Capabilities:
      type: object
      properties:
        streaming:
          type: boolean
        toolCalling:
          type: boolean
        vision:
          type: boolean
        reasoning:
          type: boolean
    ModelInput:
      type: object
      properties:
        modelName:
          type: string
        displayName:
          type: string
        visible:
          type: boolean
        settings:
          $ref: "#/components/schemas/ModelSettings"
    ModelSettings:
      type: object
      properties:
        contextWindow:
          type: integer
          format: int32
        maxOutputTokens:
          type: integer
          format: int32
        capabilities:
          $ref: "#/components/schemas/Capabilities"
        options:
          type: object
          additionalProperties: {}
        pricing:
          $ref: "#/components/schemas/Pricing"
    Pricing:
      type: object
      properties:
        inputPerMillion:
          type: number
          format: double
        outputPerMillion:
          type: number
          format: double
    Model:
      type: object
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        providerId:
          type: string
          format: uuid
        modelName:
          type: string
        displayName:
          type: string
        visible:
          type: boolean
        settings:
          $ref: "#/components/schemas/ModelSettings"
        revision:
          type: integer
          format: int64
    Default:
      type: object
      properties:
        modelConfigurationId:
          type: string
          format: uuid
        revision:
          type: integer
          format: int64
    ReplaceUserGroupsRequest:
      type: object
      additionalProperties: false
      properties:
        groupIds:
          type: array
          items:
            type: string
            format: uuid
          maxItems: 100
          maxLength: 100
          minItems: 0
      required:
      - groupIds
    InitiateSourceUploadRequest:
      type: object
      additionalProperties: false
      properties:
        filename:
          type: string
          maxLength: 255
          minLength: 0
        mediaType:
          type: string
          maxLength: 160
          minLength: 0
        sizeBytes:
          type: integer
          format: int64
          maximum: 10485760
          minimum: 1
        sha256:
          type: string
          minLength: 1
          pattern: "^[0-9a-f]{64}$"
      required:
      - filename
      - mediaType
      - sha256
      - sizeBytes
    SourceUploadAuthorization:
      type: object
      additionalProperties: false
      properties:
        uploadId:
          type: string
          format: uuid
        method:
          type: string
        uploadUrl:
          type: string
          format: uri
        requiredHeaders:
          type: object
          additionalProperties:
            type: string
        expiresAt:
          type: string
          format: date-time
      required:
      - expiresAt
      - method
      - requiredHeaders
      - uploadId
      - uploadUrl
    SourceItem:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        filename:
          type: string
        sha256:
          type: string
        sizeBytes:
          type: integer
          format: int64
        status:
          type: string
        uploadedAt:
          type: string
          format: date-time
        latestOperationId:
          type:
          - string
          - "null"
          format: uuid
        errorCode:
          type:
          - string
          - "null"
        searchStatus:
          type: string
          enum:
          - WAITING
          - INDEXING
          - READY
          - FAILED
      required:
      - errorCode
      - filename
      - id
      - latestOperationId
      - searchStatus
      - sha256
      - sizeBytes
      - status
      - uploadedAt
    SourceOperation:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        type:
          type: string
        status:
          type: string
        createdAt:
          type: string
          format: date-time
        completedAt:
          type:
          - string
          - "null"
          format: date-time
        errorCode:
          type:
          - string
          - "null"
      required:
      - completedAt
      - createdAt
      - errorCode
      - id
      - status
      - type
    SourceUploadReceipt:
      type: object
      additionalProperties: false
      properties:
        item:
          $ref: "#/components/schemas/SourceItem"
        operation:
          $ref: "#/components/schemas/SourceOperation"
      required:
      - item
      - operation
    UpdateSourceGroupsRequest:
      type: object
      additionalProperties: false
      properties:
        groupIds:
          type: array
          items:
            type: string
            format: uuid
          maxItems: 100
          maxLength: 100
          minItems: 0
          minLength: 1
      required:
      - groupIds
    CreateFileSourceRequest:
      type: object
      additionalProperties: false
      properties:
        name:
          type: string
          maxLength: 120
          minLength: 0
        groupIds:
          type:
          - array
          - "null"
          items:
            type: string
            format: uuid
          maxItems: 100
          minItems: 0
      required:
      - name
    SourceDetail:
      type: object
      additionalProperties: false
      properties:
        source:
          $ref: "#/components/schemas/SourceSummary"
        items:
          type: array
          items:
            $ref: "#/components/schemas/SourceItem"
      required:
      - items
      - source
    SourceSummary:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        type:
          type: string
        access:
          type: string
        status:
          type: string
        pendingWork:
          type: boolean
        documentCount:
          type: integer
          format: int64
        lastSucceededAt:
          type:
          - string
          - "null"
          format: date-time
        errorCode:
          type:
          - string
          - "null"
        actions:
          type: array
          items:
            type: string
      required:
      - access
      - actions
      - documentCount
      - errorCode
      - id
      - lastSucceededAt
      - name
      - pendingWork
      - status
      - type
    SearchRequest:
      type: object
      properties:
        query:
          type: string
        mediaTypes:
          type: array
          items:
            type: string
        updatedSince:
          type: string
          format: date-time
        page:
          type: integer
          format: int32
        pageSize:
          type: integer
          format: int32
    ChunkProvenance:
      type: object
      properties:
        ordinal:
          type: integer
          format: int32
        provenanceJson:
          type: string
      required:
      - ordinal
      - provenanceJson
    Result:
      type: object
      properties:
        documentId:
          type: string
          format: uuid
        generation:
          type: string
          format: uuid
        title:
          type: string
        mediaType:
          type: string
        updatedAt:
          type: string
          format: date-time
        score:
          type: number
          format: double
        sections:
          type: array
          items:
            $ref: "#/components/schemas/Section"
      required:
      - documentId
      - generation
      - mediaType
      - score
      - sections
      - title
      - updatedAt
    SearchPage:
      type: object
      properties:
        results:
          type: array
          items:
            $ref: "#/components/schemas/Result"
        page:
          type: integer
          format: int32
        hasMore:
          type: boolean
        candidateLimit:
          type: integer
          format: int32
      required:
      - candidateLimit
      - hasMore
      - page
      - results
    Section:
      type: object
      properties:
        startOrdinal:
          type: integer
          format: int32
        endOrdinal:
          type: integer
          format: int32
        matchingOrdinal:
          type: integer
          format: int32
        score:
          type: number
          format: double
        content:
          type: string
        provenance:
          type: array
          items:
            $ref: "#/components/schemas/ChunkProvenance"
      required:
      - content
      - endOrdinal
      - matchingOrdinal
      - provenance
      - score
      - startOrdinal
    CreateInvitationRequest:
      type: object
      additionalProperties: false
      properties:
        email:
          type: string
          format: email
          maxLength: 254
      required:
      - email
    Invitation:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        status:
          type: string
          enum:
          - PENDING
          - ACCEPTED
          - EXPIRED
          - REVOKED
        createdAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
        acceptedActorId:
          type:
          - string
          - "null"
          format: uuid
        acceptedAt:
          type:
          - string
          - "null"
          format: date-time
        revokedAt:
          type:
          - string
          - "null"
          format: date-time
      required:
      - createdAt
      - email
      - expiresAt
      - id
      - status
    IssuedInvitation:
      type: object
      additionalProperties: false
      properties:
        invitation:
          $ref: "#/components/schemas/Invitation"
        invitationUrl:
          type: string
          description: Relative same-origin capability URL returned only from create
            or rotate.
          pattern: ^/invite/
        delivery:
          type: string
          description: Observable delivery result for this issue or rotation operation.
          enum:
          - ACTIVATION_EMAIL_SENT
          - EXISTING_ACCOUNT
          - RECOVERY_LINK_ONLY
      required:
      - delivery
      - invitation
      - invitationUrl
    CreateGroupRequest:
      type: object
      additionalProperties: false
      properties:
        name:
          type: string
          maxLength: 200
          minLength: 0
      required:
      - name
    GroupSummary:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        systemKey:
          oneOf:
          - $ref: "#/components/schemas/GroupSystemKey"
          - type: "null"
        memberCount:
          type: integer
          format: int64
          minimum: 0
        managerCount:
          type: integer
          format: int64
          minimum: 0
        capabilities:
          type: array
          items:
            type: string
            enum:
            - IAM_ADMIN
            - USERS_MANAGE
            - GROUPS_READ
            - GROUPS_MANAGE
            - SOURCES_READ
            - SOURCES_MANAGE
            - SOURCES_DELETE
            - MODELS_MANAGE
        actions:
          type: array
          items:
            type: string
      required:
      - actions
      - capabilities
      - id
      - managerCount
      - memberCount
      - name
      - systemKey
    GroupSystemKey:
      type: string
      enum:
      - ADMIN
      - BASIC
    RenameGroupRequest:
      type: object
      additionalProperties: false
      properties:
        name:
          type: string
          maxLength: 200
          minLength: 0
      required:
      - name
    AddGroupMembersRequest:
      type: object
      additionalProperties: false
      properties:
        actorIds:
          type: array
          items:
            type: string
            format: uuid
          maxItems: 100
          maxLength: 100
          minItems: 0
          minLength: 1
      required:
      - actorIds
    ReplaceGroupCapabilitiesRequest:
      type: object
      additionalProperties: false
      properties:
        capabilities:
          type: array
          items:
            type: string
            enum:
            - IAM_ADMIN
            - USERS_MANAGE
            - GROUPS_READ
            - GROUPS_MANAGE
            - SOURCES_READ
            - SOURCES_MANAGE
            - SOURCES_DELETE
            - MODELS_MANAGE
          maxItems: 7
          maxLength: 7
          minItems: 0
      required:
      - capabilities
    CreateChatSession:
      type: object
      properties:
        title:
          type: string
          maxLength: 200
          minLength: 0
      required:
      - title
    ChatSession:
      type: object
      properties:
        id:
          type: string
          format: uuid
        personaId:
          type: string
          format: uuid
        rootMessageId:
          type: string
          format: uuid
        title:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
      - createdAt
      - id
      - personaId
      - rootMessageId
      - title
      - updatedAt
    Send:
      type: object
      properties:
        parentMessageId:
          type: string
          format: uuid
        clientRequestId:
          type: string
          format: uuid
        text:
          type: string
          maxLength: 32000
          minLength: 0
        modelConfigurationId:
          type: string
          format: uuid
      required:
      - clientRequestId
      - parentMessageId
      - text
    Accepted:
      type: object
      properties:
        userMessageId:
          type: string
          format: uuid
        assistantMessageId:
          type: string
          format: uuid
        modelConfigurationId:
          type: string
          format: uuid
        fallbackReason:
          type: string
      required:
      - assistantMessageId
      - userMessageId
    Cancellation:
      type: object
      properties:
        assistantMessageId:
          type: string
          format: uuid
        status:
          type: string
          enum:
          - RUNNING
          - COMPLETED
          - CANCELED
          - FAILED
      required:
      - assistantMessageId
      - status
    AccountType:
      type: string
      enum:
      - STANDARD
    TenantMembershipRole:
      type: string
      enum:
      - OWNER
      - MEMBER
    UserCounts:
      type: object
      additionalProperties: false
      properties:
        active:
          type: integer
          format: int64
          minimum: 0
        inactive:
          type: integer
          format: int64
          minimum: 0
        invited:
          type: integer
          format: int64
          minimum: 0
      required:
      - active
      - inactive
      - invited
    UserGroup:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        systemKey:
          oneOf:
          - $ref: "#/components/schemas/GroupSystemKey"
          - type: "null"
      required:
      - id
      - name
      - systemKey
    UserListItem:
      type: object
      additionalProperties: false
      properties:
        actorId:
          type:
          - string
          - "null"
          format: uuid
          description: Stable Actor identifier for a membership row; null for an invitation
            row.
        invitationId:
          type:
          - string
          - "null"
          format: uuid
          description: Invitation identifier for an invited row; null for a membership
            row.
        displayName:
          type:
          - string
          - "null"
        email:
          type:
          - string
          - "null"
        emailVerified:
          type:
          - boolean
          - "null"
        profileIssuer:
          type:
          - string
          - "null"
        role:
          oneOf:
          - $ref: "#/components/schemas/TenantMembershipRole"
          - type: "null"
        accountType:
          oneOf:
          - $ref: "#/components/schemas/AccountType"
          - type: "null"
        status:
          type: string
          enum:
          - ACTIVE
          - INACTIVE
          - INVITED
        groups:
          type: array
          items:
            $ref: "#/components/schemas/UserGroup"
        invitationExpiresAt:
          type:
          - string
          - "null"
          format: date-time
          description: Invitation expiry for an invited row; null for a membership
            row.
      required:
      - accountType
      - actorId
      - displayName
      - email
      - emailVerified
      - groups
      - invitationExpiresAt
      - invitationId
      - profileIssuer
      - role
      - status
    UserPage:
      type: object
      additionalProperties: false
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/UserListItem"
        page:
          type: integer
          format: int32
          minimum: 0
        size:
          type: integer
          format: int32
          maximum: 100
          minimum: 1
        totalItems:
          type: integer
          format: int64
          minimum: 0
        totalPages:
          type: integer
          format: int64
          minimum: 0
        counts:
          $ref: "#/components/schemas/UserCounts"
      required:
      - counts
      - items
      - page
      - size
      - totalItems
      - totalPages
    SourceGroup:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        systemKey:
          oneOf:
          - $ref: "#/components/schemas/GroupSystemKey"
          - type: "null"
      required:
      - id
      - name
      - systemKey
    SourceGroupList:
      type: object
      additionalProperties: false
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/SourceGroup"
      required:
      - items
    SourceGroupPage:
      type: object
      additionalProperties: false
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/SourceGroup"
        page:
          type: integer
          format: int32
          minimum: 0
        size:
          type: integer
          format: int32
          maximum: 100
          minimum: 1
        totalItems:
          type: integer
          format: int64
          minimum: 0
        totalPages:
          type: integer
          format: int64
          minimum: 0
      required:
      - items
      - page
      - size
      - totalItems
      - totalPages
    Passage:
      type: object
      properties:
        ordinal:
          type: integer
          format: int32
        content:
          type: string
        provenanceJson:
          type: string
      required:
      - content
      - ordinal
      - provenanceJson
    SearchDocument:
      type: object
      properties:
        documentId:
          type: string
          format: uuid
        generation:
          type: string
          format: uuid
        title:
          type: string
        passages:
          type: array
          items:
            $ref: "#/components/schemas/Passage"
        firstOrdinal:
          type: integer
          format: int32
        totalChunks:
          type: integer
          format: int32
        hasMore:
          type: boolean
      required:
      - documentId
      - firstOrdinal
      - generation
      - hasMore
      - passages
      - title
      - totalChunks
    InvitationPage:
      type: object
      additionalProperties: false
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Invitation"
        page:
          type: integer
          format: int32
          minimum: 0
        size:
          type: integer
          format: int32
          maximum: 100
          minimum: 1
        totalItems:
          type: integer
          format: int64
          minimum: 0
        totalPages:
          type: integer
          format: int64
          minimum: 0
      required:
      - items
      - page
      - size
      - totalItems
      - totalPages
    CurrentInvitation:
      type: object
      additionalProperties: false
      properties:
        tenantDisplayName:
          type: string
        expiresAt:
          type: string
          format: date-time
        continueUrl:
          type: string
          pattern: ^/invite/continue
      required:
      - continueUrl
      - expiresAt
      - tenantDisplayName
    CurrentIdentity:
      type: object
      additionalProperties: false
      properties:
        actorId:
          type: string
          format: uuid
          description: Stable internal MemoryOS actor identifier.
        tenant:
          description: "Active Tenant context, or null when the actor has no active\
            \ Tenant membership."
          oneOf:
          - $ref: "#/components/schemas/CurrentTenant"
          - type: "null"
        capabilities:
          type: array
          description: Expanded global capabilities backed by current server enforcement.
          items:
            type: string
            enum:
            - IAM_ADMIN
            - USERS_MANAGE
            - GROUPS_READ
            - GROUPS_MANAGE
            - SOURCES_READ
            - SOURCES_MANAGE
            - SOURCES_DELETE
            - MODELS_MANAGE
        scopedCapabilities:
          type: array
          description: Eligible capabilities available only within resources managed
            by this actor.
          items:
            type: string
            enum:
            - IAM_ADMIN
            - USERS_MANAGE
            - GROUPS_READ
            - GROUPS_MANAGE
            - SOURCES_READ
            - SOURCES_MANAGE
            - SOURCES_DELETE
            - MODELS_MANAGE
        authorizationVersion:
          type: integer
          format: int64
          description: Monotonic Tenant IAM revision used only to invalidate private
            client data.
          minimum: 0
      required:
      - actorId
      - authorizationVersion
      - capabilities
      - scopedCapabilities
      - tenant
    CurrentTenant:
      type: object
      additionalProperties: false
      properties:
        displayName:
          type: string
          description: Display name of the actor's active Tenant.
        role:
          type: string
          description: Stable Tenant membership role used for presentation.
          enum:
          - OWNER
          - MEMBER
      required:
      - displayName
      - role
    GroupPage:
      type: object
      additionalProperties: false
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/GroupSummary"
        page:
          type: integer
          format: int32
          minimum: 0
        size:
          type: integer
          format: int32
          maximum: 100
          minimum: 1
        totalItems:
          type: integer
          format: int64
          minimum: 0
        totalPages:
          type: integer
          format: int64
          minimum: 0
      required:
      - items
      - page
      - size
      - totalItems
      - totalPages
    GroupSources:
      type: object
      additionalProperties: false
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/SourceSummary"
      required:
      - items
    GroupMember:
      type: object
      additionalProperties: false
      properties:
        actorId:
          type: string
          format: uuid
        displayName:
          type:
          - string
          - "null"
        email:
          type:
          - string
          - "null"
        accountType:
          type: string
          enum:
          - STANDARD
        status:
          type: string
          enum:
          - ACTIVE
          - INACTIVE
        isManager:
          type: boolean
        protectedOwner:
          type: boolean
      required:
      - accountType
      - actorId
      - displayName
      - email
      - isManager
      - protectedOwner
      - status
    GroupMemberPage:
      type: object
      additionalProperties: false
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/GroupMember"
        page:
          type: integer
          format: int32
          minimum: 0
        size:
          type: integer
          format: int32
          maximum: 100
          minimum: 1
        totalItems:
          type: integer
          format: int64
          minimum: 0
        totalPages:
          type: integer
          format: int64
          minimum: 0
      required:
      - items
      - page
      - size
      - totalItems
      - totalPages
    GroupCapabilities:
      type: object
      additionalProperties: false
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/GroupCapability"
      required:
      - items
    GroupCapability:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
          enum:
          - IAM_ADMIN
          - USERS_MANAGE
          - GROUPS_READ
          - GROUPS_MANAGE
          - SOURCES_READ
          - SOURCES_MANAGE
          - SOURCES_DELETE
          - MODELS_MANAGE
        label:
          type: string
        description:
          type: string
        editable:
          type: boolean
        implies:
          type: array
          items:
            type: string
            enum:
            - IAM_ADMIN
            - USERS_MANAGE
            - GROUPS_READ
            - GROUPS_MANAGE
            - SOURCES_READ
            - SOURCES_MANAGE
            - SOURCES_DELETE
            - MODELS_MANAGE
      required:
      - description
      - editable
      - id
      - implies
      - label
    ChatMessage:
      type: object
      properties:
        id:
          type: string
          format: uuid
        sessionId:
          type: string
          format: uuid
        parentMessageId:
          type:
          - string
          - "null"
          format: uuid
        latestChildMessageId:
          type:
          - string
          - "null"
          format: uuid
        role:
          type: string
          enum:
          - USER
          - ASSISTANT
        content:
          type: string
        status:
          type: string
          enum:
          - COMPLETED
          - RUNNING
          - CANCELED
          - FAILED
        createdAt:
          type: string
          format: date-time
        finishedAt:
          type:
          - string
          - "null"
          format: date-time
      required:
      - content
      - createdAt
      - finishedAt
      - id
      - latestChildMessageId
      - parentMessageId
      - role
      - sessionId
      - status
    TextDeltaEvent:
      type: object
      properties:
        assistantMessageId:
          type: string
          format: uuid
        sequence:
          type: integer
          format: int64
        text:
          type: string
      required:
      - assistantMessageId
      - sequence
      - text
    OutcomeEvent:
      type: object
      properties:
        assistantMessageId:
          type: string
          format: uuid
        sequence:
          type: integer
          format: int64
        status:
          type: string
          enum:
          - COMPLETED
          - CANCELED
          - FAILED
        failureCode:
          type:
          - string
          - "null"
      required:
      - assistantMessageId
      - failureCode
      - sequence
      - status
    ResetEvent:
      type: object
      properties:
        assistantMessageId:
          type: string
          format: uuid
        reason:
          type: string
          enum:
          - BUFFER_MISSING
          - BUFFER_GAP
          - BUFFER_EXPIRED
      required:
      - assistantMessageId
      - reason
    Descriptor:
      type: object
      properties:
        type:
          type: string
        credentialRequirement:
          type: string
          enum:
          - REQUIRED
          - OPTIONAL
          - NONE
    AvailableModel:
      type: object
      properties:
        id:
          type: string
          format: uuid
        providerId:
          type: string
          format: uuid
        providerName:
          type: string
        modelName:
          type: string
        displayName:
          type: string
        capabilities:
          $ref: "#/components/schemas/Capabilities"
        contextWindow:
          type: integer
          format: int32
        maxOutputTokens:
          type: integer
          format: int32
        pricing:
          $ref: "#/components/schemas/Pricing"
        isDefault:
          type: boolean
    ApiProblem:
      type: object
      additionalProperties: false
      properties:
        type:
          type: string
          format: uri
          description: Stable problem type for capability failures; omitted means
            RFC 9457 `about:blank`.
        title:
          type: string
          description: Short human-readable problem category.
        status:
          type: integer
          format: int32
          description: HTTP status code.
        detail:
          type: string
          description: Safe human-readable fallback detail.
        instance:
          type: string
          format: uri-reference
          description: Request path that produced the problem.
        code:
          type: string
          description: Stable capability-prefixed code; present only for expected
            capability failures.
          pattern: "^[A-Z][A-Z0-9_]*$"
      required:
      - detail
      - instance
      - status
      - title
  securitySchemes:
    browserSession:
      type: apiKey
      description: HttpOnly JDBC-backed Spring Security session cookie.
      name: SESSION
      in: cookie
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
