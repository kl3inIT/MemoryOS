# MemoryOS login theme: keycloak.v2 markup and behaviour with a centered brand card (see css/memoryos.css).
parent=keycloak.v2
import=common/keycloak

styles=css/styles.css css/memoryos.css

# The card is designed for a light page; keep it light when the operating system prefers dark.
darkMode=false
