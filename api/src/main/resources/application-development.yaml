arconia:
  dev:
    services:
      postgresql:
        port: 55432
      # One Redis container for the API and the worker in development.
      redis:
        port: 56379

logging:
  level:
    root: INFO
    io.memoryos: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: INFO
    org.springframework.jdbc.core.JdbcTemplate: DEBUG
    org.springframework.transaction: DEBUG
